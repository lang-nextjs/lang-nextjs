---
phase: v1.3-02
slug: edge-runtime-adapter
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-02
---

# Phase v1.3-02 — Validation Strategy

> Template created by `/nf:plan-phase v1.3-02` (step 5.5) after research.
> Populated by `/nf:plan-phase v1.3-02` (step 11.5) after plan-checker approval.
> Governs feedback sampling during `/nf:execute-phase v1.3-02`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (per-package vitest.config.ts) + pnpm typecheck (tsc --noEmit) |
| **Config file** | packages/edge/vitest.config.ts (new — created in Wave 0) |
| **Quick run command** | `pnpm --filter @deepagents-nextjs/edge test run` |
| **Full suite command** | `pnpm turbo test typecheck build --filter=@deepagents-nextjs/edge` |
| **Estimated runtime** | ~30–60 seconds (unit tests only; wrangler smoke is manual) |
| **CI pipeline** | .github/workflows/ci.yml — exists; edge package added in this phase |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `pnpm --filter @deepagents-nextjs/edge test run`
- **After every plan wave:** Run `pnpm turbo test typecheck build --filter=@deepagents-nextjs/edge`
- **Before `/nf:verify-work`:** Full suite + publint + attw must be green
- **Maximum acceptable task feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| v1.3-02-01-01 | 01 | 1 | EDGE-01,EDGE-03 | integration | `pnpm install --frozen-lockfile=false && pnpm --filter @deepagents-nextjs/edge typecheck` | ❌ W0 | ⬜ pending |
| v1.3-02-01-02 | 01 | 1 | EDGE-01,EDGE-03 | unit | `pnpm --filter @deepagents-nextjs/edge test run` | ❌ W0 | ⬜ pending |
| v1.3-02-02-01 | 02 | 2 | EDGE-02 | unit | `pnpm --filter @deepagents-nextjs/edge test run` | ❌ W0 | ⬜ pending |
| v1.3-02-03-01 | 03 | 3 | EDGE-03,EDGE-04 | smoke | `grep -c 'EXPERIMENTAL' packages/edge/README.md && grep -c 'TTFB' packages/edge/README.md` | ❌ W0 | ⬜ pending |
| v1.3-02-03-02 | 03 | 3 | EDGE-04 | smoke | `pnpm --filter @deepagents-nextjs/edge build && pnpm --filter @deepagents-nextjs/edge exec publint && pnpm --filter @deepagents-nextjs/edge exec attw --pack .` | ❌ W0 | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

New package `packages/edge` needs scaffolding before tests can run:

- `packages/edge/package.json` — workspace package with test + typecheck + build scripts
- `packages/edge/vitest.config.ts` — per-package vitest config (no root workspace.ts)
- `pnpm install` from repo root — resolves workspace:* links

Scaffold stubs (handler body throws `new Error('not implemented')`) allow typecheck to pass before TDD plans implement real logic — same pattern as v1.2-04 (sveltekit).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cloudflare Workers TTFB < 5s in preview deployment | EDGE-02 | Requires live wrangler preview + network measurement | Run `wrangler dev --port 8787`, stream 10 SSE frames from mock backend, measure TTFB in DevTools |
| Deno Deploy handler streams tokens live in browser | EDGE-01 | Visual streaming verification | Deploy to Deno Deploy staging, open URL in browser, verify tokens appear incrementally |

---

## Validation Sign-Off

Updated by `nf-plan-checker` when plans are approved:

- [x] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [x] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [x] Wave 0 test files cover all MISSING references (pnpm install resolves workspace links)
- [x] No watch-mode flags in any automated command (`vitest run`, not `vitest`)
- [x] Feedback latency per task: < 60s ✅
- [x] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** approved on 2026-05-02

---

## Execution Tracking

Updated during `/nf:execute-phase v1.3-02`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | TBD | — | — | — | scaffold |
| 1 | TBD | TBD | — | — | ⬜ pending |

**Phase validation complete:** pending
