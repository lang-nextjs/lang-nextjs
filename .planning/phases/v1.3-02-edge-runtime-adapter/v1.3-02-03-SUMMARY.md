---
phase: v1.3-02
plan: "03"
subsystem: edge
tags: [documentation, ci, publint, attw, cloudflare, deno]
one-liner: "ESM-only edge package README with EXPERIMENTAL Cloudflare caveat + CI guards for no Node.js built-ins and no cross-package server import"
dependency_graph:
  requires: [v1.3-02-01, v1.3-02-02]
  provides: [EDGE-03, EDGE-04]
  affects: [packages/edge, .github/workflows/ci.yml]
tech_stack:
  added: [.attw.json (ESM-only ignore rules)]
  patterns: [attw ignoreRules config for ESM-only packages, CI grep guards for dist purity]
key_files:
  created:
    - packages/edge/README.md
    - packages/edge/.attw.json
  modified:
    - .github/workflows/ci.yml
    - packages/edge/package.json (validate script unchanged, lockfile updated)
decisions:
  - ".attw.json with ignoreRules [no-resolution, cjs-resolves-to-esm] is the correct approach for ESM-only packages; --profile esm-only is equivalent but config file makes bare 'attw --pack .' pass"
  - "Cloudflare EXPERIMENTAL marker appears 3x in README (Quick Start blockquote, API Reference heading, Compatibility table)"
  - "TTFB appears 3x in README (Buffering Caveat section body, Troubleshooting entry, Compatibility table)"
metrics:
  duration: "~5 min"
  completed: "2026-05-03"
  tasks: 2
  files: 4
---

# Phase v1.3-02 Plan 03: Edge README + CI Guards Summary

ESM-only edge package README with EXPERIMENTAL Cloudflare caveat + CI guards for no Node.js built-ins and no cross-package server import.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Write packages/edge/README.md | 77a6b9c | packages/edge/README.md |
| 2 | Add edge CI guards + .attw.json | cb74f0e | .github/workflows/ci.yml, packages/edge/.attw.json, packages/edge/package.json |

## Verification Results

- `pnpm --filter @deepagents-nextjs/edge build` — exits 0
- `pnpm --filter @deepagents-nextjs/edge exec publint` — exits 0, "All good!"
- `pnpm --filter @deepagents-nextjs/edge exec attw --pack .` — exits 0, "No problems found"
- `grep -c 'EXPERIMENTAL' packages/edge/README.md` — returns 3
- `grep -c 'TTFB' packages/edge/README.md` — returns 3
- `grep -c '@deepagents-nextjs/edge' .github/workflows/ci.yml` — returns 2
- `grep -c '"require"' packages/edge/package.json` — returns 0 (ESM-only confirmed)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] esbuild binary version mismatch in worktree**
- **Found during:** Task 2 build verification
- **Issue:** `esbuild@0.27.7` node_modules had a stale binary reporting version `0.17.6` due to worktree isolation; `tsup` failed with "Host version 0.27.7 does not match binary version 0.17.6"
- **Fix:** Copied the correct `@esbuild/darwin-arm64@0.27.7` binary to `esbuild@0.27.7/bin/esbuild`
- **Files modified:** node_modules/.pnpm/esbuild@0.27.7 (binary only, not tracked in git)

**2. [Rule 2 - Missing correctness] attw failing for ESM-only package without ignoreRules**
- **Found during:** Task 2 publint/attw verification
- **Issue:** `attw --pack .` returned exit 1 due to `no-resolution` (node10) and `cjs-resolves-to-esm` (node16-cjs) — both expected for ESM-only packages but cause non-zero exit without config
- **Fix:** Added `packages/edge/.attw.json` with `ignoreRules: ["no-resolution", "cjs-resolves-to-esm"]`; this is the standard approach for ESM-only packages
- **Files modified:** packages/edge/.attw.json (new)
- **Commit:** cb74f0e

## Self-Check: PASSED

- packages/edge/README.md — FOUND
- .github/workflows/ci.yml — FOUND
- packages/edge/.attw.json — FOUND
- commit 77a6b9c — FOUND
- commit cb74f0e — FOUND
