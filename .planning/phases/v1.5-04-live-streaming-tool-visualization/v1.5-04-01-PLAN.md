---
phase: v1.5-04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/open-swe/app/api/open-swe/runs/[runId]/stream/route.ts
  - apps/open-swe/app/api/open-swe/runs/[runId]/stream/route.test.ts
  - apps/open-swe/lib/hooks/useRunStream.ts
  - apps/open-swe/lib/hooks/useRunStream.test.ts
  - apps/open-swe/lib/hooks/useToolState.ts
  - apps/open-swe/lib/hooks/useToolState.test.ts
  - apps/open-swe/app/runs/[runId]/page.test.tsx
  - apps/open-swe/components/ToolCard.test.tsx
autonomous: true
requirements:
  - DASH-03
formal_artifacts: none

must_haves:
  truths:
    - "GET /api/open-swe/runs/[runId]/stream returns 200 with Content-Type: text/event-stream when LANGGRAPH_PLATFORM_URL is set and the upstream responds with a stream"
    - "GET /api/open-swe/runs/[runId]/stream returns 400 when threadId query param is absent"
    - "GET /api/open-swe/runs/[runId]/stream returns 502 when LANGGRAPH_PLATFORM_URL env var is unset"
    - "All Wave 0 test files exist and fail (RED) before any hook or component implementation is written"
  artifacts:
    - path: "apps/open-swe/app/api/open-swe/runs/[runId]/stream/route.ts"
      provides: "SSE proxy route handler"
      exports: ["GET"]
    - path: "apps/open-swe/app/api/open-swe/runs/[runId]/stream/route.test.ts"
      provides: "RED integration tests for SSE proxy route"
      contains: "describe('GET /api/open-swe/runs/[runId]/stream'"
    - path: "apps/open-swe/lib/hooks/useRunStream.ts"
      provides: "Stub file (empty export) so test imports resolve"
      contains: "export function useRunStream"
    - path: "apps/open-swe/lib/hooks/useRunStream.test.ts"
      provides: "RED unit tests for useRunStream hook"
      contains: "describe('useRunStream'"
    - path: "apps/open-swe/lib/hooks/useToolState.ts"
      provides: "Stub file (empty export) so test imports resolve"
      contains: "export function useToolState"
    - path: "apps/open-swe/lib/hooks/useToolState.test.ts"
      provides: "RED unit tests for useToolState hook"
      contains: "describe('useToolState'"
    - path: "apps/open-swe/app/runs/[runId]/page.test.tsx"
      provides: "RED integration tests for RunDetail page"
      contains: "describe('RunDetail'"
    - path: "apps/open-swe/components/ToolCard.test.tsx"
      provides: "RED unit tests for ToolCard component"
      contains: "describe('ToolCard'"
  key_links:
    - from: "apps/open-swe/app/api/open-swe/runs/[runId]/stream/route.ts"
      to: "LANGGRAPH_PLATFORM_URL/threads/{threadId}/runs/{runId}/stream"
      via: "fetch() in GET handler"
      pattern: "fetch.*threads.*stream"
    - from: "apps/open-swe/app/api/open-swe/runs/[runId]/stream/route.ts"
      to: "apps/open-swe/lib/types.ts"
      via: "relative import"
      pattern: "from.*types"
---

<objective>
Wave 0 RED test stubs + SSE proxy route implementation for `GET /api/open-swe/runs/[runId]/stream`.

Purpose: Establish the full test surface (5 RED test files) and the SSE proxy route before any React hooks or UI components are built. Also creates minimal stub files for useRunStream.ts and useToolState.ts so that test imports resolve at module load time. Plan 02 turns these stubs GREEN.

Output: 5 failing test files + stub hook files + 1 working SSE route handler.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v1.5-04-live-streaming-tool-visualization/v1.5-04-RESEARCH.md
@apps/open-swe/lib/types.ts
@apps/open-swe/lib/langgraph-client.ts
@apps/open-swe/app/api/open-swe/runs/route.ts
@apps/open-swe/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub hook files + 5 Wave 0 RED test stubs</name>
  <files>
    apps/open-swe/lib/hooks/useRunStream.ts
    apps/open-swe/lib/hooks/useToolState.ts
    apps/open-swe/app/api/open-swe/runs/[runId]/stream/route.test.ts
    apps/open-swe/lib/hooks/useRunStream.test.ts
    apps/open-swe/lib/hooks/useToolState.test.ts
    apps/open-swe/app/runs/[runId]/page.test.tsx
    apps/open-swe/components/ToolCard.test.tsx
  </files>
  <action>
CRITICAL ORDER: Create stub source files FIRST (steps 1-2), then test files (steps 3-7). Test imports resolve at module load time — stubs must exist before test files are written.

**Step 1: Create `apps/open-swe/lib/hooks/useRunStream.ts` (stub)**

```typescript
// Stub — implemented in Plan 02 (v1.5-04-02)
export function useRunStream(_options: {
  runId: string;
  threadId: string;
  enabled?: boolean;
}): never {
  throw new Error("useRunStream: not implemented");
}
```

**Step 2: Create `apps/open-swe/lib/hooks/useToolState.ts` (stub)**

```typescript
// Stub — implemented in Plan 02 (v1.5-04-02)
export function useToolState(_events: unknown[]): never {
  throw new Error("useToolState: not implemented");
}
```

**Step 3: Create `apps/open-swe/app/api/open-swe/runs/[runId]/stream/route.test.ts`**

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

vi.mock('../../../../../lib/langgraph-client');

describe('GET /api/open-swe/runs/[runId]/stream', () => {
  beforeEach(() => {
    vi.unstubAllEnvs();
  });

  it('returns 400 when threadId query param is missing', async () => {
    throw new Error('not implemented');
  });

  it('returns 502 when LANGGRAPH_PLATFORM_URL is not set', async () => {
    throw new Error('not implemented');
  });

  it('returns 200 text/event-stream when upstream responds with SSE body', async () => {
    throw new Error('not implemented');
  });

  it('returns 502 when upstream fetch fails', async () => {
    throw new Error('not implemented');
  });

  it('stream isolation: runId from params is included in upstream URL', async () => {
    throw new Error('not implemented');
  });
});
```

Note: `GET` from `'./route'` is NOT imported at the top of this file — the route file does not exist yet at Wave 0. Import `GET` dynamically inside each test body once route.ts exists (Plan 01 Task 2). This avoids top-level import resolution failure.

**Step 4: Create `apps/open-swe/lib/hooks/useRunStream.test.ts`**

```typescript
// @vitest-environment jsdom
import { describe, it, vi, beforeEach, afterEach } from 'vitest';

describe('useRunStream', () => {
  beforeEach(() => {
    // Stub global.EventSource — not available in any env until jsdom is set up
    (global as unknown as Record<string, unknown>).EventSource = vi.fn();
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  it('connects to the correct URL with runId and threadId', async () => {
    throw new Error('not implemented');
  });

  it('updates events state when a message event arrives', async () => {
    throw new Error('not implemented');
  });

  it('calls EventSource.close() on unmount', async () => {
    throw new Error('not implemented');
  });

  it('sets status to error when EventSource fires error event', async () => {
    throw new Error('not implemented');
  });
});
```

**Step 5: Create `apps/open-swe/lib/hooks/useToolState.test.ts`**

```typescript
// @vitest-environment jsdom
import { describe, it } from 'vitest';

describe('useToolState', () => {
  it('returns empty array for empty events', async () => {
    throw new Error('not implemented');
  });

  it('returns pending tool call when tool-input-start event arrives', async () => {
    throw new Error('not implemented');
  });

  it('upgrades to completed when tool-output-available matches toolCallId', async () => {
    throw new Error('not implemented');
  });

  it('does not mutate completed state when duplicate tool-output-available arrives', async () => {
    throw new Error('not implemented');
  });

  it('tracks multiple concurrent tool calls independently', async () => {
    throw new Error('not implemented');
  });

  it('converges correctly when tool-output-available arrives before tool-input-start (out-of-order)', async () => {
    // Edge case: events may arrive out of order. After processing both events in any order,
    // the final state for the toolCallId must be status: 'completed' with output populated.
    throw new Error('not implemented');
  });
});
```

**Step 6: Create `apps/open-swe/app/runs/[runId]/page.test.tsx`**

```typescript
// @vitest-environment jsdom
import { describe, it, vi } from 'vitest';

vi.mock('../../../lib/hooks/useRunStream', () => ({ useRunStream: vi.fn() }));
vi.mock('../../../lib/hooks/useToolState', () => ({ useToolState: vi.fn() }));
vi.mock('next/navigation', () => ({
  useParams: vi.fn(() => ({ runId: 'test-run-id' })),
  useSearchParams: vi.fn(() => new URLSearchParams('threadId=test-thread-id')),
}));

describe('RunDetail', () => {
  it('renders loading state while connecting', async () => {
    throw new Error('not implemented');
  });

  it('renders streamed text delta events as concatenated text', async () => {
    throw new Error('not implemented');
  });

  it('renders ToolCard for each tool call in state', async () => {
    throw new Error('not implemented');
  });

  it('closes EventSource on unmount (no memory leak)', async () => {
    throw new Error('not implemented');
  });

  it('shows error state when threadId is missing from query string', async () => {
    throw new Error('not implemented');
  });
});
```

**Step 7: Create `apps/open-swe/components/ToolCard.test.tsx`**

```typescript
// @vitest-environment jsdom
import { describe, it } from 'vitest';

describe('ToolCard', () => {
  it('renders tool name in pending state', async () => {
    throw new Error('not implemented');
  });

  it('renders completed state with expand button', async () => {
    throw new Error('not implemented');
  });

  it('shows input and output payload when expanded', async () => {
    throw new Error('not implemented');
  });

  it('does not show payload when not expanded', async () => {
    throw new Error('not implemented');
  });
});
```

Use relative imports only — NO `@/` alias (apps/open-swe tsconfig has no path aliases; Vitest does not read tsconfig paths). All imports must be relative paths.

Commit message: `test(v1.5-04-01): add Wave 0 RED stubs — stream route + hooks + components`
  </action>
  <verify>
    `pnpm --filter open-swe test -- --run 2>&1 | tail -20`

    All test files must be found. Tests must FAIL (RED). Acceptable failure modes:
    - `Error: not implemented` (expected)
    - Module not found for route file (expected — route.ts doesn't exist yet after this task)

    NOT acceptable:
    - No test files found
    - Module not found for useRunStream.ts or useToolState.ts (stubs must exist after step 1-2)
  </verify>
  <done>
    5 test files + 2 stub hook files exist. Test suite runs and reports failures (not "no test files found"). `pnpm --filter open-swe test -- --run` exits with non-zero code but with ≥5 test files discovered. Stub hook imports resolve cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement GET /api/open-swe/runs/[runId]/stream route</name>
  <files>
    apps/open-swe/app/api/open-swe/runs/[runId]/stream/route.ts
  </files>
  <action>
Create `apps/open-swe/app/api/open-swe/runs/[runId]/stream/route.ts`.

This is the SSE proxy route. It validates inputs, fetches the upstream LangGraph Platform run stream, and pipes it back to the browser as `text/event-stream`.

```typescript
import { NextRequest } from "next/server";

export const dynamic = "force-dynamic";

const STREAM_TIMEOUT_MS = 30_000; // initial connect timeout only; stream itself is unbounded

export async function GET(
  request: NextRequest,
  { params }: { params: { runId: string } }
): Promise<Response> {
  const platformUrl = process.env.LANGGRAPH_PLATFORM_URL;
  if (!platformUrl) {
    return new Response(
      JSON.stringify({ error: "LANGGRAPH_PLATFORM_URL is not configured" }),
      { status: 502, headers: { "Content-Type": "application/json" } }
    );
  }

  const threadId = request.nextUrl.searchParams.get("threadId");
  if (!threadId) {
    return new Response(
      JSON.stringify({ error: "threadId query param is required" }),
      { status: 400, headers: { "Content-Type": "application/json" } }
    );
  }

  const { runId } = params;

  const controller = new AbortController();
  const timeoutId = setTimeout(() => controller.abort(), STREAM_TIMEOUT_MS);

  let upstreamResponse: Response;
  try {
    const upstreamUrl = `${platformUrl}/threads/${threadId}/runs/${runId}/stream`;
    const apiKey = process.env.LANGGRAPH_API_KEY;

    upstreamResponse = await fetch(upstreamUrl, {
      method: "GET",
      signal: controller.signal,
      headers: {
        Accept: "text/event-stream",
        ...(apiKey && { "X-Api-Key": apiKey }),
      },
    });

    clearTimeout(timeoutId);

    if (!upstreamResponse.ok) {
      const text = await upstreamResponse.text().catch(() => "");
      return new Response(
        JSON.stringify({ error: "LangGraph Platform error", detail: text }),
        { status: 502, headers: { "Content-Type": "application/json" } }
      );
    }

    if (!upstreamResponse.body) {
      return new Response(null, { status: 204 });
    }
  } catch (err) {
    clearTimeout(timeoutId);
    if (err instanceof Error && err.name === "AbortError") {
      return new Response(
        JSON.stringify({ error: "LangGraph Platform request timed out" }),
        { status: 502, headers: { "Content-Type": "application/json" } }
      );
    }
    console.error("[open-swe/stream] upstream fetch error:", err);
    return new Response(
      JSON.stringify({ error: "Failed to connect to LangGraph Platform" }),
      { status: 502, headers: { "Content-Type": "application/json" } }
    );
  }

  // Pipe the upstream SSE stream directly to the browser.
  // The upstream LangGraph Platform already emits astream_events v2 format.
  // openSweAdapter normalization is applied client-side via useRunStream.
  // This keeps the route stateless and avoids per-request transform state.
  //
  // Stream-end detection: LangGraph Platform sends `data: [DONE]` as an empty
  // sentinel event to signal end-of-stream. This route forwards it as-is.
  // The client-side useRunStream hook must listen for the 'done' custom event
  // or detect the `[DONE]` sentinel in the message handler.
  return new Response(upstreamResponse.body, {
    status: 200,
    headers: {
      "Content-Type": "text/event-stream",
      "Cache-Control": "no-cache, no-transform",
      "X-Accel-Buffering": "no",
    },
  });
}
```

Key decisions:
- Pass upstream SSE body through unchanged — don't apply openSweAdapter transforms server-side. The client-side `useRunStream` hook handles event parsing and normalization. This keeps the route stateless (no transform closure state per request).
- `threadId` passed as query param (Option A from research — accept the UX tradeoff; simple for v1.5-04 scope).
- STREAM_TIMEOUT_MS only applies to the initial fetch connect, NOT to the streaming body read (unbounded stream).
- Relative import — NO `@/` alias.
- `export const dynamic = "force-dynamic"` — required for SSE in Next.js App Router (same as runs/route.ts).
- Stream-end: LangGraph Platform sends `data: [DONE]` sentinel; route forwards it unchanged; hook detects it.

Commit message: `feat(v1.5-04-01): implement GET /api/open-swe/runs/[runId]/stream SSE proxy route (DASH-03)`
  </action>
  <verify>
    1. TypeScript typecheck: `pnpm --filter open-swe exec tsc --noEmit 2>&1 | head -20` — no errors
    2. Route file exists with correct exports: `grep -n "export const dynamic\|export async function GET" apps/open-swe/app/api/open-swe/runs/\[runId\]/stream/route.ts`
    3. Confirm Content-Type and stream-end comment present: check file directly
    4. Test suite still runs (files discovered): `pnpm --filter open-swe test -- --run 2>&1 | grep "test files"`
  </verify>
  <done>
    `apps/open-swe/app/api/open-swe/runs/[runId]/stream/route.ts` exists, typechecks clean, exports GET with `dynamic = "force-dynamic"`, includes stream-end ([DONE] sentinel) comment. Route test stubs transition from "module not found" to "not implemented" errors (correct RED).
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `pnpm --filter open-swe test -- --run` shows 5 test files discovered, all tests failing (RED)
- `pnpm --filter open-swe exec tsc --noEmit` passes cleanly
- `apps/open-swe/app/api/open-swe/runs/[runId]/stream/route.ts` exists with GET export, `dynamic = "force-dynamic"`, and `Content-Type: text/event-stream` header
- `apps/open-swe/lib/hooks/useRunStream.ts` and `useToolState.ts` exist as stubs (imports resolve)
- No `@/` path aliases used anywhere in new files
- Git log shows 2 commits: wave-0 stubs + route implementation
</verification>

<success_criteria>
- 5 Wave 0 test files exist and are discovered by Vitest
- All tests fail RED (not "no test files found")
- Hook stub files exist so test imports resolve at module load time
- SSE route returns 400 on missing threadId, 502 on missing env var, 200 text/event-stream when upstream is healthy
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/v1.5-04-live-streaming-tool-visualization/v1.5-04-01-SUMMARY.md`
</output>
