---
phase: v1.0-01
plan: "03"
subsystem: infra
tags: [tsup, vitest, react, jsdom, dual-esm-cjs, publint, peerDependencies]

requires:
  - phase: v1.0-01-01
    provides: pnpm workspace, turbo.json, tsconfig.base.json
provides:
  - packages/react skeleton with dual ESM+CJS build passing publint
  - React as peerDependency (not bundled — prevents duplicate React instances)
  - VERSION export skeleton (placeholder for Phase v1.0-03 implementation)
  - Vitest smoke tests in jsdom environment (2/2 passing)
  - tsup external: react, react-dom, ai, @ai-sdk/react, zod
affects:
  - v1.0-01-04
  - v1.0-03 (react hook implementation)

tech-stack:
  added: [react@19.2.5, react-dom@19.2.5, @types/react@19.2.14, jsdom@25.0.1]
  patterns:
    - "peerDependencies + devDependencies for react — peer for consumers, dev for build tools"
    - "jsdom in devDependencies — installed before vitest loads jsdom environment"
    - "Same type:commonjs + outExtension pattern as server package"

key-files:
  created:
    - packages/react/package.json
    - packages/react/tsconfig.json
    - packages/react/tsup.config.ts
    - packages/react/vitest.config.ts
    - packages/react/src/index.ts
    - packages/react/src/index.test.ts
  modified: []

key-decisions:
  - "react in peerDependencies AND devDependencies — peer for consumer semantics, dev for tsup/tsc to resolve jsx-runtime"
  - "jsdom in devDependencies — vitest loads the jsdom environment at test time, must be installed"
  - "tsup external list includes future peer deps (ai, @ai-sdk/react, zod) — prevents accidental bundling"
  - "jsx: react-jsx in tsconfig — enables JSX transform for future React component implementation"

patterns-established:
  - "react package: same type:commonjs + outExtension pattern as server"
  - "vitest environment: jsdom for react package"
  - "external array: include all intended peer deps proactively"

requirements-completed: [PKG-02]

duration: 3min
completed: 2026-04-29
---

# Phase v1.0-01 Plan 03: React Package Skeleton Summary

**@deepagents-nextjs/react skeleton with React as peerDependency, jsdom vitest environment, tsup externalization of react/react-dom/ai/zod, and publint-clean dual ESM+CJS exports**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-29T06:55:00Z
- **Completed:** 2026-04-29T06:58:00Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- packages/react with react/react-dom in peerDependencies (prevents duplicate React in consumer apps)
- tsup external list: react, react-dom, ai, @ai-sdk/react, zod
- jsdom in devDependencies for vitest jsdom environment
- Vitest jsdom environment config, 2/2 smoke tests passing
- publint passes with zero errors

## Task Commits

1. **Task 1: react package.json, tsconfig, tsup config** - `899a024` (feat)
2. **Task 2: react source skeleton, vitest config, and build smoke** - `8409657` (feat)

## Files Created/Modified

- `packages/react/package.json` - type:commonjs, peerDeps react+react-dom, devDeps react+jsdom
- `packages/react/tsconfig.json` - extends tsconfig.base.json, jsx:react-jsx
- `packages/react/tsup.config.ts` - external array, outExtension, format esm+cjs
- `packages/react/vitest.config.ts` - jsdom environment, src/**/*.test.{ts,tsx}
- `packages/react/src/index.ts` - VERSION='0.1.0' skeleton
- `packages/react/src/index.test.ts` - 2 smoke tests

## Decisions Made

- Applied same `type:commonjs` + `outExtension` fix as server package (same root cause: tsup + no type field = publint failure)
- Proactively added `ai`, `@ai-sdk/react`, `zod` to external array even though not yet dependencies — prevents accidental bundling when Phase v1.0-03 adds them

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Applied same outExtension fix as server package**
- **Found during:** Task 2 (react source skeleton and build smoke)
- **Issue:** Same tsup extension mismatch as server package — publint would fail without the fix
- **Fix:** Added `"type": "commonjs"` to package.json and `outExtension` to tsup.config.ts (same pattern as server)
- **Files modified:** packages/react/package.json, packages/react/tsup.config.ts
- **Verification:** `pnpm dlx publint packages/react` exits 0 with "All good!"
- **Committed in:** `8409657` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Same fix required as server. No scope creep.

## Issues Encountered

None beyond the auto-fixed tsup extension issue (same root cause as Plan 02).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- packages/react skeleton is publish-ready (publint clean, dual artifacts)
- React peerDependency topology correctly established before any feature code
- Ready for Phase v1.0-03 to add useDeepAgentsChat hook

---
*Phase: v1.0-01-monorepo-foundation*
*Completed: 2026-04-29*
