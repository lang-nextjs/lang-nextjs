---
phase: v1.0-05
slug: ci-and-publishing
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-29
---

# Phase v1.0-05 — Validation Strategy

> Template created by `/nf:plan-phase v1.0-05` (step 5.5) after research.
> Populated by `/nf:plan-phase v1.0-05` (step 11.5) after plan-checker approval.
> Governs feedback sampling during `/nf:execute-phase v1.0-05`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Manual + smoke (no automated test suite — infrastructure files only) |
| **Config file** | none — no test framework config needed |
| **Quick run command** | `pnpm build && cd packages/server && npx publint && npx @arethetypeswrong/cli --pack . && cd ../../packages/react && npx publint && npx @arethetypeswrong/cli --pack .` |
| **Full suite command** | Same as quick run |
| **Estimated runtime** | ~2 min |
| **CI pipeline** | `.github/workflows/ci.yml` — needs creation |

---

## Nyquist Sampling Rate

- **After every task commit:** Run `pnpm build` to confirm no regressions
- **After every plan wave:** Run full validation smoke (publint + attw for both packages)
- **Before `/nf:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Verify Command | Status |
|---------|------|------|-------------|-----------|----------------|--------|
| v1.0-05-01-01 | 01 | 1 | PKG-03 | manual | `ls .changeset/config.json` | ⬜ pending |
| v1.0-05-01-02 | 01 | 1 | PKG-04 | smoke | `cd packages/server && npx publint && npx @arethetypeswrong/cli --pack .` | ⬜ pending |
| v1.0-05-02-01 | 02 | 2 | PKG-04 | manual | `cat .github/workflows/ci.yml` | ⬜ pending |
| v1.0-05-03-01 | 03 | 3 | PKG-03 | manual | `cat .github/workflows/publish.yml` | ⬜ pending |

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed. This phase adds only configuration files (YAML workflows, JSON Changesets config) — no application code requiring pre-existing test stubs.

---

## Manual-Only Verifications

- **Changesets config validity**: `cat .changeset/config.json` — visual inspection of `fixed`, `access`, `baseBranch` fields
- **CI workflow YAML syntax**: `npx js-yaml .github/workflows/ci.yml` exits 0
- **Publish workflow YAML syntax**: `npx js-yaml .github/workflows/publish.yml` exits 0
- **OIDC trust on npm**: One-time manual step on npm.dev — configure OIDC trusted publisher for the GitHub repo (cannot be automated in CI)

---

## Validation Sign-Off

- [ ] Quick run command exits 0 for both packages
- [ ] `publint` exits 0 for `@deepagents-nextjs/server`
- [ ] `publint` exits 0 for `@deepagents-nextjs/react`
- [ ] `attw --pack .` exits 0 for `@deepagents-nextjs/server`
- [ ] `attw --pack .` exits 0 for `@deepagents-nextjs/react`
- [ ] `.changeset/config.json` has correct `fixed`, `access`, `baseBranch`
- [ ] `ci.yml` YAML is valid
- [ ] `publish.yml` YAML is valid

---

## Execution Tracking

| Wave | Started | Completed | Notes |
|------|---------|-----------|-------|
| 1 | - | - | Changesets init + validation tooling |
| 2 | - | - | CI workflow |
| 3 | - | - | Publish workflow |
