---
phase: v1.2-05
plan: "01"
subsystem: framework-adapter
tags: [remix, typescript, sse, scaffold, vitest, tsup]

# Dependency graph
requires:
  - phase: v1.2-04
    provides: SseFrameAccumulator pattern, stub handler/store pattern, package scaffold shape
provides:
  - packages/remix package scaffold with 9 files (4 config + 5 src)
  - SseFrameAccumulator copied locally (no cross-package dep)
  - createDeepAgentsHandler throw-stub (ready for Plan 02 TDD)
  - useDeepAgentsChat throw-stub (ready for Plan 03 TDD)
  - TypeScript typecheck passes clean
affects: [v1.2-05-02, v1.2-05-03]

# Tech tracking
tech-stack:
  added: ["@remix-run/node ^2.17.4", "@remix-run/react ^2.17.4", "@testing-library/react ^16.0.0"]
  patterns: [throw-stub pattern for pre-TDD scaffolds, SseFrameAccumulator local copy pattern, dual ESM/CJS exports with outExtension]

key-files:
  created:
    - packages/remix/package.json
    - packages/remix/tsconfig.json
    - packages/remix/tsup.config.ts
    - packages/remix/vitest.config.ts
    - packages/remix/src/accumulator.ts
    - packages/remix/src/types.ts
    - packages/remix/src/handler.ts
    - packages/remix/src/hook.ts
    - packages/remix/src/index.ts
  modified: []

key-decisions:
  - "SseFrameAccumulator copied to remix package (not imported from server) — prevents Next.js peerDep from leaking to Remix consumers; mirrors v1.2-04 sveltekit decision"
  - "getToken receives ActionFunctionArgs (not RequestEvent) — Remix-specific type; copying SvelteKit types verbatim would be wrong"
  - "DeepAgentsChatResult exported as named type from types.ts — consumers import return type directly without re-declaring intersection"
  - "vitest.config.ts has globals: true + jsdom environment — required for Plan 03 hook tests using vi.stubGlobal and renderHook"
  - "tsup external: ['@remix-run/node', '@remix-run/react', 'react'] — prevents two React instances in consumer bundles"

patterns-established:
  - "Throw-stub pattern: stubs throw Error with plan reference (Plan 02/03) until TDD plans implement real code"
  - "Local accumulator copy: paste accumulator.ts verbatim into each adapter package with comment indicating source"

requirements-completed: [FWK-02]

# Metrics
duration: 2min
completed: 2026-05-02
---

# Phase v1.2-05 Plan 01: Remix Adapter Scaffold Summary

**packages/remix scaffold with 9 files — SseFrameAccumulator copy, Remix-typed interfaces, and throw-stubs that pass typecheck clean before TDD plans run**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-02T11:36:09Z
- **Completed:** 2026-05-02T11:38:50Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments
- Created full packages/remix package with CJS/ESM dual exports, Remix peerDependencies, and @testing-library/react devDependency
- Copied SseFrameAccumulator verbatim from sveltekit package (zero cross-package deps — prevents Next.js leaking to Remix consumers)
- Defined Remix-specific types: RemixHandlerOptions (with ActionFunctionArgs), DeepAgentsState, DeepAgentsChatResult
- Created createDeepAgentsHandler and useDeepAgentsChat throw-stubs — typecheck passes, Plans 02+03 implement real code via TDD
- pnpm --filter @deepagents-nextjs/remix typecheck exits 0 with no errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Create package scaffold (package.json, tsconfig, tsup, vitest)** - `6017921` (chore)
2. **Task 2: Create src files — accumulator copy, types, stubs, index** - `3772981` (feat)

**Plan metadata:** _(docs commit follows)_

## Files Created/Modified
- `packages/remix/package.json` - Package manifest with @deepagents-nextjs/remix name, CJS type, dual exports, Remix peerDeps
- `packages/remix/tsconfig.json` - Identical to sveltekit tsconfig (extends tsconfig.base.json, DOM lib)
- `packages/remix/tsup.config.ts` - Build config with external: [@remix-run/node, @remix-run/react, react]
- `packages/remix/vitest.config.ts` - Test config with globals: true and jsdom environment
- `packages/remix/src/accumulator.ts` - SseFrameAccumulator copied verbatim, zero cross-package imports
- `packages/remix/src/types.ts` - RemixHandlerOptions, DeepAgentsState, DeepAgentsChatResult, SseAdapter
- `packages/remix/src/handler.ts` - createDeepAgentsHandler throw-stub
- `packages/remix/src/hook.ts` - useDeepAgentsChat throw-stub
- `packages/remix/src/index.ts` - Public barrel exports

## Decisions Made
- SseFrameAccumulator copied locally (not imported from server package) — mirrors v1.2-04 sveltekit decision; prevents Next.js peerDep leaking to Remix consumers
- getToken type uses ActionFunctionArgs from @remix-run/node (not SvelteKit's RequestEvent) — Remix-specific; verbatim copy would break types
- DeepAgentsChatResult exported as named type — consumers type props without re-declaring the DeepAgentsState & { start: () => void } intersection
- vitest.config.ts uses globals: true + jsdom — required for Plan 03 hook tests that mock global.fetch and use renderHook

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- pnpm install required --no-frozen-lockfile flag (new package added to monorepo, lockfile update needed). Non-blocking — standard for first install of a new workspace package.
- Peer dependency warnings for react 18 vs 19 in @remix-run/react — non-breaking; devDependency has react@^18.3.1, Remix expects react@^19. Stubs typecheck fine; Plans 02+03 will handle test environment setup.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- packages/remix scaffold is complete — Plans 02 and 03 can proceed with TDD implementation
- createDeepAgentsHandler stub ready for Plan 02 RED phase
- useDeepAgentsChat stub ready for Plan 03 RED phase
- Blocker still active: STR-01 (stream reconnection via experimental_resume) depends on AI SDK bugs #6502 and #9707 — check upstream status before closing v1.2-05

---
*Phase: v1.2-05-remix-adapter*
*Completed: 2026-05-02*
