---
phase: v1.2-05
plan: "02"
subsystem: api
tags: [remix, sse, streaming, handler, tdd, vitest]

# Dependency graph
requires:
  - phase: v1.2-05-01
    provides: Remix package scaffold with throw-stub handler.ts and types.ts
  - phase: v1.2-04
    provides: SvelteKit handler as near-verbatim template for adaptation

provides:
  - createDeepAgentsHandler factory for Remix (ActionFunctionArgs → Response)
  - 14 handler unit tests covering all required + 2 bonus cases

affects:
  - v1.2-05-03 (hook TDD — will complete the Remix package)
  - v1.2-05-04 (integration / phase closeout)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - TDD RED→GREEN with stub-throws forcing all tests to fail in RED phase
    - assertNodeRuntime() inside handler body (not module top-level) — safe import in any env
    - getToken fail-open: absent=forward all, null/empty=no header, string=Bearer inject, throw=propagate
    - HOP_BY_HOP Set filtering for proxy header safety
    - SseFrameAccumulator streaming pipeline identical to SvelteKit handler

key-files:
  created:
    - packages/remix/src/handler.test.ts
  modified:
    - packages/remix/src/handler.ts

key-decisions:
  - "handler.test.ts uses makeArgs() helper (ActionFunctionArgs shape) instead of makeEvent() — mirrors SvelteKit pattern with Remix-specific naming"
  - "14 tests shipped (12 required + 2 bonus: x-vercel-ai-ui-message-stream forwarding and getToken rejection propagation) — both were free to add and increase coverage"
  - "hook.test.ts pre-existing failure (react-dom@19/react@18 mismatch) is out of scope — confirmed pre-existing by git stash verification"

patterns-established:
  - "Remix handler mirrors SvelteKit handler exactly except: ActionFunctionArgs not RequestEvent; @remix-run/node not @sveltejs/kit; console prefix [deepagents/remix] not [deepagents/sveltekit]"

requirements-completed:
  - FWK-02

# Metrics
duration: 2min
completed: 2026-05-02
---

# Phase v1.2-05 Plan 02: Remix Handler TDD Summary

**createDeepAgentsHandler for Remix — 14-test TDD suite (RED→GREEN) with full SSE proxy, auth policy, and Node.js runtime guard**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-02T11:41:40Z
- **Completed:** 2026-05-02T11:43:58Z
- **Tasks:** 2 (RED commit + GREEN commit; no REFACTOR needed)
- **Files modified:** 2

## Accomplishments

- 14 failing tests written and committed (RED phase verified: all 14 fail on stub)
- Full `createDeepAgentsHandler` implementation shipped — near-verbatim SvelteKit port with Remix-specific types
- All 14 tests pass (GREEN phase verified)
- Verification checks pass: ActionFunctionArgs imported, no SvelteKit imports, assertNodeRuntime inside handler body

## Task Commits

1. **Task 1 (RED): handler.test.ts — 14 failing tests** - `7ad3fbb` (test)
2. **Task 2 (GREEN): createDeepAgentsHandler full implementation** - `71b90be` (feat)

## Files Created/Modified

- `packages/remix/src/handler.test.ts` — 14 handler unit tests (makeArgs/makeFetchResponse helpers + full describe suite)
- `packages/remix/src/handler.ts` — Full implementation replacing throw-stub; ActionFunctionArgs, HOP_BY_HOP, assertNodeRuntime, getToken policy, SseFrameAccumulator pipeline

## Decisions Made

- 14 tests shipped (12 required + 2 bonus) — `x-vercel-ai-ui-message-stream` forwarding and `getToken` rejection propagation were essentially free to add and increase coverage materially
- `makeArgs()` named to match Remix semantics (`args` vs `event`) but otherwise identical shape to SvelteKit's `makeEvent()` — `args.request` is the same Web API Request interface
- No REFACTOR commit needed — implementation is a clean direct port with only the required substitutions

## Deviations from Plan

None — plan executed exactly as written. The 2 bonus tests were explicitly invited by the plan ("Bonus if time permits").

## Issues Encountered

- `hook.test.ts` fails with react-dom@19/react@18 version mismatch — confirmed pre-existing via `git stash` check (21 failures before any changes). Out of scope for this plan; logged for awareness.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `handler.ts` is production-ready; Plan 03 (hook TDD) can proceed immediately
- `hook.ts` still has a throw-stub — Plan 03 replaces it with the full Remix hook using `useDeepAgentsChat`
- The pre-existing `hook.test.ts` failure (react-dom version mismatch) will need resolution in Plan 03 or as a separate fix before the hook tests can run

## Self-Check: PASSED

- `packages/remix/src/handler.ts` — FOUND
- `packages/remix/src/handler.test.ts` — FOUND
- `.planning/phases/v1.2-05-remix-adapter/v1.2-05-02-SUMMARY.md` — FOUND
- Commit `7ad3fbb` (RED: test) — FOUND
- Commit `68a8c73` (GREEN: feat) — FOUND

---
*Phase: v1.2-05-remix-adapter*
*Completed: 2026-05-02*
