# Plan v1.0-02-03 Summary

**Phase:** v1.0-02 — Server Package
**Plan:** 03 — createDeepAgentsHandler factory
**Status:** COMPLETE
**Completed:** 2026-04-29

## What Was Built

The primary public API: `createDeepAgentsHandler()` handler factory + public API barrel (`index.ts`) + full integration test suite.

## Key Files

### Created
- `packages/server/src/handler.ts` — `createDeepAgentsHandler()` factory. Composes SseFrameAccumulator + transforms pipeline + SSE proxy with getToken auth, header forwarding, and error handling. Ports stsfront/next-app/app/api/chat/stream/route.ts.

### Updated
- `packages/server/src/index.ts` — Replaced VERSION skeleton with real public API barrel: exports `createDeepAgentsHandler`, `DeepAgentsHandlerOptions`, `defaultTransforms`, `SseFrame`, `SseTransform`
- `packages/server/src/index.test.ts` — Updated smoke tests for new public API
- `packages/server/src/handler.test.ts` — All 13 todos replaced with passing integration tests
- `packages/server/package.json` — Added `next: >=14.0.0` to peerDependencies, `next: ^15.0.0` to devDependencies
- `packages/server/tsconfig.json` — Added `"DOM", "DOM.Iterable"` to lib (required for Web API types: Headers, fetch, ReadableStream)

## Deviations

- **tsconfig lib needed DOM, DOM.Iterable**: The v1.0-01 skeleton only had `"lib": ["ES2020"]`. handler.ts uses Web API globals (Headers, fetch, ReadableStream, TextEncoder/TextDecoder). Added `"DOM"` and `"DOM.Iterable"` to packages/server/tsconfig.json. This is correct for Next.js App Router server handlers and does not affect the tsconfig.base.json shared config.

## Verification

- All 28 vitest tests GREEN (4 test files: accumulator, transforms, handler, index)
- `tsc --noEmit` exits 0
- `next` in peerDependencies + devDependencies

## Self-Check: PASSED

- [x] createDeepAgentsHandler returns a POST function (factory pattern)
- [x] Response: Content-Type: text/event-stream, Cache-Control: no-cache no-transform, X-Accel-Buffering: no
- [x] x-vercel-ai-ui-message-stream forwarded from backend
- [x] Hop-by-hop headers dropped (host, content-length, transfer-encoding, connection)
- [x] getToken: string → Bearer injection, null → no header, throws → propagates, absent → forward all
- [x] 502 on backend unreachable, controller.error on mid-stream
- [x] @ts-expect-error duplex comment preserved
- [x] SseFrameAccumulator + defaultTransforms composed in pipeline
