---
phase: v1.0-02
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/server/src/accumulator.test.ts
  - packages/server/src/transforms.test.ts
  - packages/server/src/handler.test.ts
autonomous: true
requirements:
  - SRV-01
  - SRV-04
  - SRV-05
  - SRV-06
formal_artifacts: none

must_haves:
  truths:
    - "Three test stub files exist in `packages/server/src/`: `accumulator.test.ts`, `transforms.test.ts`, `handler.test.ts`"
    - "`cd packages/server && npx vitest run` exits 0 with all tests reported as TODO/skipped (no failures)"
    - "Each stub file imports its not-yet-implemented module and uses `it.todo()` for all test cases"
  artifacts:
    - path: "packages/server/src/accumulator.test.ts"
      provides: "Vitest stubs for SseFrameAccumulator covering SRV-05"
      contains: "it.todo"
    - path: "packages/server/src/transforms.test.ts"
      provides: "Vitest stubs for defaultTransforms covering SRV-04"
      contains: "it.todo"
    - path: "packages/server/src/handler.test.ts"
      provides: "Vitest stubs for createDeepAgentsHandler covering SRV-01, SRV-02, SRV-06"
      contains: "it.todo"
  key_links:
    - from: "accumulator.test.ts"
      to: "packages/server/src/accumulator.ts (Wave 1)"
      via: "import { SseFrameAccumulator } from './accumulator'"
      pattern: "SseFrameAccumulator"
    - from: "transforms.test.ts"
      to: "packages/server/src/transforms.ts (Wave 1)"
      via: "import { defaultTransforms } from './transforms'"
      pattern: "defaultTransforms"
    - from: "handler.test.ts"
      to: "packages/server/src/handler.ts (Wave 2)"
      via: "import { createDeepAgentsHandler } from './handler'"
      pattern: "createDeepAgentsHandler"
---

<objective>
Create Wave 0 test scaffolding for the server package: three test stub files covering SseFrameAccumulator (SRV-05), defaultTransforms (SRV-04), and createDeepAgentsHandler (SRV-01, SRV-02, SRV-06).

Purpose: Nyquist compliance requires test files to exist before implementation begins. These stubs use `it.todo()` so vitest exits 0 immediately, but define the exact test cases the implementation must satisfy. Wave 1 and Wave 2 will fill in the implementations; the test structure is locked here.

Output: Three test files that vitest can discover and run successfully (all todos = no failures).
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/v1.0-02-server-package/v1.0-02-CONTEXT.md
@.planning/phases/v1.0-02-server-package/v1.0-02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: accumulator.test.ts — SseFrameAccumulator stubs (SRV-05)</name>
  <files>
    packages/server/src/accumulator.test.ts
  </files>
  <action>
Create `packages/server/src/accumulator.test.ts` with Vitest stubs covering the SseFrameAccumulator contract.

Import from `./accumulator` (module doesn't exist yet — that's fine for stubs, import is not evaluated until tests run).

Include these `it.todo()` stubs:
1. `push() returns complete frames split by \\n\\n boundary`
2. `push() keeps incomplete frame in buffer`
3. `push() handles frame split across two chunks (TCP split edge case)` — this is the critical SRV-05 case: first chunk ends mid-frame, second chunk completes it
4. `push() handles multiple complete frames in one chunk`
5. `flush() returns remaining buffer content and clears buffer`
6. `flush() returns empty array when buffer is empty`

Example structure:
```typescript
import { describe, it } from 'vitest'

describe('SseFrameAccumulator', () => {
  it.todo('push() returns complete frames split by \\n\\n boundary')
  it.todo('push() keeps incomplete frame in buffer')
  it.todo('push() handles frame split across two chunks (TCP split edge case)')
  it.todo('push() handles multiple complete frames in one chunk')
  it.todo('flush() returns remaining buffer content and clears buffer')
  it.todo('flush() returns empty array when buffer is empty')
})
```

Do NOT import vitest's `expect` — todos don't use it yet. Import only `describe` and `it`.
  </action>
  <verify>
    cd /Users/jonathanborduas/code/deepagents-nextjs/packages/server && npx vitest run --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|todo|skip)"
  </verify>
  <done>
    `packages/server/src/accumulator.test.ts` exists with 6 it.todo() stubs. `npx vitest run` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: transforms.test.ts + handler.test.ts — stubs for SRV-04, SRV-01, SRV-02, SRV-06</name>
  <files>
    packages/server/src/transforms.test.ts
    packages/server/src/handler.test.ts
  </files>
  <action>
Create two test stub files:

**transforms.test.ts** — covers SRV-04 (defaultTransforms strips messageId):
```typescript
import { describe, it } from 'vitest'

describe('defaultTransforms', () => {
  it.todo('strips messageId from finish SSE frames')
  it.todo('passes through non-finish frames unmodified')
  it.todo('passes through [DONE] lines unmodified')
  it.todo('passes through non-JSON data lines unmodified')
  it.todo('null return from transform drops the frame')
  it.todo('user transforms are appended after defaultTransforms')
})
```

**handler.test.ts** — covers SRV-01, SRV-02, SRV-03, SRV-06:
```typescript
import { describe, it } from 'vitest'

describe('createDeepAgentsHandler', () => {
  it.todo('returns a POST function when called with backendUrl')
  it.todo('proxies SSE stream with Content-Type: text/event-stream')
  it.todo('sets Cache-Control: no-cache, no-transform in response')
  it.todo('sets X-Accel-Buffering: no in response')
  it.todo('forwards x-vercel-ai-ui-message-stream header from backend')
  it.todo('forwards client headers except hop-by-hop (host, content-length, transfer-encoding, connection)')
  it.todo('returns 502 when backend fetch throws (unreachable)')
  it.todo('returns backend status when backend has no body')
  it.todo('injects Authorization: Bearer token when getToken returns a string')
  it.todo('does not set Authorization header when getToken returns null')
  it.todo('propagates getToken error without swallowing it')
  it.todo('applies transforms pipeline to SSE frames')
  it.todo('calls controller.error() and logs on mid-stream ReadableStream error (SRV-06)')
})
```

Both files import only `describe` and `it` from vitest.
  </action>
  <verify>
    cd /Users/jonathanborduas/code/deepagents-nextjs/packages/server && npx vitest run --reporter=verbose 2>&1 | tail -20
  </verify>
  <done>
    `transforms.test.ts` and `handler.test.ts` both exist with `it.todo()` stubs. `npx vitest run` exits 0 across all 3 test files (existing skeleton test + 2 new stubs). Total: all tests pass or are todo.
  </done>
</task>

</tasks>

<verification>

## Plan Verification

Run after all tasks complete:

```bash
cd /Users/jonathanborduas/code/deepagents-nextjs/packages/server && npx vitest run --reporter=verbose
```

Expected: exits 0, reports todos for the 3 new stub files plus the existing skeleton test passing.

```bash
ls packages/server/src/
```

Expected: `accumulator.test.ts`, `transforms.test.ts`, `handler.test.ts` present alongside existing `index.ts` and `index.test.ts`.

</verification>
