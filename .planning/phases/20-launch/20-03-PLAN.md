---
phase: 20-launch
plan: 03
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified:
  - .changeset/v16-observability-resilience-shutdown.md
  - docs/MIGRATION-v1.6.md
  - apps/open-swe/vercel.json
autonomous: true
requirements: [OPS-02, OPS-05]
formal_artifacts: none

must_haves:
  truths:
    - "A staged changeset declares a minor bump for the @deepagents-nextjs packages describing the additive observability, resilience, and onShutdown/graceful-shutdown features as non-breaking"
    - "A migration/release note documents the v1.5 -> v1.6 upgrade as a drop-in, backward-compatible change: existing code runs unchanged; new options (observability, resilience, graceful shutdown, error-reporting via onError) are all opt-in additive"
    - "No publish is performed: changeset version / npm publish are NOT run (CI publish is billing-blocked); the deliverable is staged changeset(s) + written notes only"
    - "The bare /health reference in apps/open-swe/vercel.json is normalized to /api/health to match the actual route at apps/open-swe/app/api/health"
  artifacts:
    - path: ".changeset/v16-observability-resilience-shutdown.md"
      provides: "Minor-bump changeset for the fixed @deepagents-nextjs/server + @deepagents-nextjs/react group (and framework packages) summarizing the additive v1.6 surface"
      contains: "minor"
    - path: "docs/MIGRATION-v1.6.md"
      provides: "v1.6 What's New + Upgrade Path (non-breaking) release/migration notes referencing observability/resilience/shutdown/error-reporting"
      min_lines: 40
      contains: "non-breaking"
  key_links:
    - from: ".changeset/v16-observability-resilience-shutdown.md"
      to: "@deepagents-nextjs/server"
      via: "changeset frontmatter package:minor entry"
      pattern: "@deepagents-nextjs/server.*minor"
    - from: "docs/MIGRATION-v1.6.md"
      to: "docs/ERROR-REPORTING.md"
      via: "markdown link to the OPS-02 error-reporting guide"
      pattern: "ERROR-REPORTING"
---

<objective>
Stage the v1.6.0 release WITHOUT publishing: create a changeset declaring the additive observability / resilience / graceful-shutdown / error-reporting surface as a non-breaking minor bump, and write `docs/MIGRATION-v1.6.md` documenting the drop-in upgrade path. Also normalize the pre-existing bare `/health` reference in `apps/open-swe/vercel.json` to `/api/health` (the actual route lives at `apps/open-swe/app/api/health`), a tiny cleanup flagged in Phase 19.

CRITICAL (locked constraint): Do NOT run `pnpm changeset version`, `pnpm changeset publish`, or `npm publish`. CI publish is currently billing-blocked. The deliverable is STAGED changeset file(s) + written release/migration notes. Actual versioning/publish happens later when CI is unblocked.

This plan is Wave 2 because the notes reference the final shipped surface from Plan 01 (error-reporting docs) and Plan 02 (proven E2E flows).

Purpose: close out v1.6 with a publish-ready, reviewer-friendly release artifact set without tripping the billing-blocked publish gate.
Output: one changeset markdown file, one migration/release notes doc, and the vercel.json path fix.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.changeset/config.json
@.changeset/edge-cloudflare-stream-timeout.md
@apps/open-swe/vercel.json

# Reference the final shipped surface (Plans 01 + 02 must complete first):
@.planning/phases/20-launch/20-01-PLAN.md
@.planning/phases/20-launch/20-02-PLAN.md
# Phase 18/19 feature summaries describe the additive options being released:
@.planning/phases/18-observability-health-resilience/18-01-SUMMARY.md
@.planning/phases/18-observability-health-resilience/18-03-SUMMARY.md
@.planning/phases/19-graceful-shutdown-deploy-docs/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage the v1.6 minor-bump changeset + write docs/MIGRATION-v1.6.md (no publish)</name>
  <files>.changeset/v16-observability-resilience-shutdown.md, docs/MIGRATION-v1.6.md</files>
  <action>
Part A — `.changeset/v16-observability-resilience-shutdown.md`: Create a changeset following the exact format of `.changeset/edge-cloudflare-stream-timeout.md` (YAML frontmatter mapping each package to a bump level, then a markdown body). The bump is `minor` for every affected package — these are additive, non-breaking features and the packages are pre-1.0 (currently 0.1.0), but per the locked constraint we declare a MINOR bump to signal the v1.6 additive feature set. Include the packages that gained the v1.6 surface:
```
---
"@deepagents-nextjs/server": minor
"@deepagents-nextjs/react": minor
"@deepagents-nextjs/edge": minor
"@deepagents-nextjs/sveltekit": minor
"@deepagents-nextjs/remix": minor
---
```
(Note: server + react are a fixed group per .changeset/config.json `fixed`, so both must appear or both move together — include both explicitly.) The body summarizes the additive surface in 1–2 short paragraphs: vendor-neutral `observability` lifecycle hooks (onRequest/onFetchStart/onFetchEnd/onStreamStart/onError/onStreamEnd) with edge-safe timing; `resilience` config (consumer-store-backed rate limit → 429, circuit breaker → 503, per-request timeout, backpressure, configurable retry); and Node graceful shutdown via `createGracefulShutdown()` (SIGTERM → readiness 503 → drain). State explicitly: "All options are opt-in and additive — existing handlers are unaffected. No new runtime dependencies; edge-runtime compatibility preserved. Error reporting (Sentry/Datadog) wires through the onError hook with no vendor SDK bundled — see docs/ERROR-REPORTING.md and docs/MIGRATION-v1.6.md."

Part B — `docs/MIGRATION-v1.6.md`: Write release/migration notes with two sections (per 20-RESEARCH.md Open Question 4 recommendation):
1. **What's New in v1.6** — bullet the additive feature groups with a one-line description and the option/export name each ships under: Observability hooks (`observability` option), Health/readiness probes (`createHealthProbe`/`createReadinessProbe`), Resilience (`resilience` option: rate limit 429, breaker 503, timeout, retry), Graceful shutdown (`createGracefulShutdown`), Error reporting (via `observability.onError` — link to `./ERROR-REPORTING.md`). Reference that all three production flows are E2E-tested (OPS-05).
2. **Upgrade Path (v1.5 → v1.6)** — state clearly this is a NON-BREAKING, drop-in upgrade: existing `createDeepAgentsHandler({ backendUrl })` calls work unchanged; every new capability is opt-in via additive options; no public types were removed or changed; no new runtime dependencies were added; edge-runtime compatibility is preserved. Include a tiny "before/after" showing the same handler call works with zero changes, plus one opt-in example adding `observability` + `resilience`.

Do NOT run `pnpm changeset version`, `pnpm changeset publish`, or `npm publish` anywhere in this task. Creating the `.changeset/*.md` file IS the staging deliverable.
  </action>
  <verify>
Run: `test -f .changeset/v16-observability-resilience-shutdown.md`;
`grep -E '@deepagents-nextjs/server.*minor' .changeset/v16-observability-resilience-shutdown.md` matches;
`grep -E '@deepagents-nextjs/react' .changeset/v16-observability-resilience-shutdown.md` matches (fixed-group partner present);
`grep -iE 'observability|resilience|shutdown' .changeset/v16-observability-resilience-shutdown.md` matches;
`grep -iE 'non-breaking|additive|opt-in' docs/MIGRATION-v1.6.md` matches;
`grep -E 'ERROR-REPORTING' docs/MIGRATION-v1.6.md` matches;
`test $(wc -l < docs/MIGRATION-v1.6.md) -ge 40`;
`pnpm changeset status 2>/dev/null | grep -iE 'minor|@deepagents-nextjs' || echo "changeset status not run/available — file presence already verified"` (status read-only; never run `version`).
  </verify>
  <done>A staged minor-bump changeset for the v1.6 packages exists (server + react fixed group + framework packages), describing the additive non-breaking surface and pointing to the error-reporting/migration docs; docs/MIGRATION-v1.6.md (>= 40 lines) documents the drop-in v1.5→v1.6 upgrade. No version/publish command was run.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize bare /health to /api/health in apps/open-swe/vercel.json</name>
  <files>apps/open-swe/vercel.json</files>
  <action>
In `apps/open-swe/vercel.json`, the `traffic.split` array references `{ "path": "/health", "vitality": 100 }` but the actual route is served at `/api/health` (`apps/open-swe/app/api/health/route.ts`); the sibling readiness entry already correctly uses `/api/ready`. PRE-FLIGHT: first confirm the real route path exists with `ls apps/open-swe/app/api/health` (it does) before editing. Then change the `/health` path value to `/api/health`. Make NO other changes to the file (preserve the rollback/errorThreshold block, regions, build commands, and the `/api/ready` and `/*` entries verbatim). This is the tiny Phase-19-flagged cleanup; keep it surgical.
  </action>
  <verify>
Run: `ls apps/open-swe/app/api/health` (route exists — pre-flight);
`grep -E '"/api/health"' apps/open-swe/vercel.json` matches;
`! grep -E '"path": "/health"' apps/open-swe/vercel.json` (bare /health gone — zero matches);
`node -e "JSON.parse(require('fs').readFileSync('apps/open-swe/vercel.json','utf8')); console.log('valid json')"` prints `valid json` (file still parses).
  </verify>
  <done>apps/open-swe/vercel.json references /api/health (matching the real route) instead of the bare /health; the rest of the file is unchanged and still valid JSON.</done>
</task>

</tasks>

<verification>
- A staged changeset file declares the v1.6 additive surface as a minor bump for the @deepagents-nextjs packages (server+react fixed group included).
- docs/MIGRATION-v1.6.md documents the non-breaking, drop-in v1.5→v1.6 upgrade and links the error-reporting guide.
- No `changeset version` / publish command was executed (publish is billing-blocked; staging only).
- apps/open-swe/vercel.json uses /api/health.
</verification>

<success_criteria>
ROADMAP Phase 20 success criterion 3 staged: v1.6.0 release is prepared with changelog/migration notes documenting the additive observability/resilience/onShutdown options as non-breaking — staged as changeset + notes, with actual publish deferred until CI is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/20-launch/20-03-SUMMARY.md`
</output>
