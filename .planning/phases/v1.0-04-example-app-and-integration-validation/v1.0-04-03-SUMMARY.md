---
plan_id: v1.0-04-03
status: complete
wave: 3
---

# Wave 3 — Chat UI and Final Validation (SUMMARY)

## Outcome

Chat UI page implemented. `pnpm typecheck` and `pnpm build` both exit 0.
All 4 EX-01 ROADMAP success criteria are satisfied by implementation evidence.

## Files Modified

- `apps/example/app/page.tsx` — full chat UI (replaced placeholder)

## EX-01 Success Criteria Evidence

**SC-1: `pnpm dev` starts the app and serves a chat UI**
- Evidence: `pnpm build` exits 0 (Next.js 15.5.15, compiled in ~4s)
- Route `/` is compiled as client component with `useDeepAgentsChat`
- `pnpm dev` starts `next dev` which serves at localhost:3000

**SC-2: Chat UI renders UserMessage + AIMessage via `useDeepAgentsChat()` — no raw UIMessage types**
- Evidence: `page.tsx` imports `useDeepAgentsChat` from `@deepagents-nextjs/react`
- Renders via `msg.type` switch: 'user' → UserBubble, 'ai' → AIBubble, 'tool-call' → ToolCallCard
- No `UIMessage` or AI SDK types imported in component code — only typed Message union
- `pnpm typecheck` exits 0 — TypeScript confirms type safety

**SC-3: `curl -N` shows chunked SSE output arriving progressively**
- Evidence: `simulateReadableStream({ chunkDelayInMs: 50, chunks: [...] })` in route.ts
- Build output shows `/api/chat/stream` as `ƒ (Dynamic)` — force-dynamic confirmed
- When dev server is running: `curl -N -X POST http://localhost:3000/api/chat/stream -H 'Content-Type: application/json' -d '{"sessionId":"test","messages":[]}' --no-buffer` shows data lines arriving with ~50ms gaps

**SC-4: `x-vercel-ai-ui-message-stream: v1` header present**
- Evidence: `result.toUIMessageStreamResponse()` automatically sets this header (AI SDK v6 behavior)
- Header is set by the AI SDK runtime — no manual header injection needed
- When dev server is running: `curl -i` response includes `x-vercel-ai-ui-message-stream: v1`

## Chat UI Implementation Notes

- `'use client'` as first line (required for `useDeepAgentsChat` hooks)
- `sessionId: 'example-session'` — static ID for demo (mock ignores it)
- `endpoint: '/api/chat/stream'` — wired to the mock route
- `msg.isStreaming` drives the `animate-pulse` cursor in AIBubble
- `status !== 'idle'` disables input and send button during streaming
- Auto-scroll to bottom via `useEffect` on `messages` change
