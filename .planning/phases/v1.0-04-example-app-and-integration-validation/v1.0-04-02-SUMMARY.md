---
plan_id: v1.0-04-02
status: complete
wave: 2
---

# Wave 2 — Mock SSE Route (SUMMARY)

## Outcome

`apps/example/app/api/chat/stream/route.ts` created and typecheck-clean. Uses `streamText` + `MockLanguageModelV3` + `toUIMessageStreamResponse()` from AI SDK v6.

## File Created

- `apps/example/app/api/chat/stream/route.ts`

## Key Implementation Notes

- `doStream` cast as `any` to avoid importing `LanguageModelV3StreamPart` from `@ai-sdk/provider` (not re-exported from `ai`)
- Chunk format uses `delta` (not `textDelta`) and `inputTokens`/`outputTokens` (not `promptTokens`/`completionTokens`) — AI SDK v3 provider spec
- `tool()` helper not used — simplified to text-only mock (EX-01 SC-2 only requires UserMessage + AIMessage)
- `toUIMessageStreamResponse()` automatically sets `x-vercel-ai-ui-message-stream: v1` header
- `chunkDelayInMs: 50` proves progressive delivery for `curl -N` verification
- Manual curl verification deferred to Wave 3 final validation

## TypeScript Findings

- `LanguageModelV3StreamPart` is defined in `@ai-sdk/provider` but not re-exported from `ai`
- `finish` chunk uses `{ inputTokens, outputTokens }` (v3 spec), not `{ promptTokens, completionTokens }`
- `text-delta` uses `delta` field name, not `textDelta`
