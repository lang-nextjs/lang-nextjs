---
plan_id: v1.0-04-01
status: complete
wave: 1
---

# Wave 1 — Example App Scaffold (SUMMARY)

## Outcome

Next.js 15 app scaffold created in `apps/example/`. `pnpm build` exits 0. Also fixed `.gitignore` to exclude `.next/` build artifacts.

## Files Created

- `apps/example/package.json` — Next.js 15, React 19, Tailwind v4, ai v6; removed @deepagents-nextjs/server dep
- `apps/example/next.config.ts` — minimal NextConfig
- `apps/example/tsconfig.json` — extends tsconfig.base.json, jsx: preserve (Next.js auto-added allowJs: true, noEmit: true)
- `apps/example/postcss.config.mjs` — @tailwindcss/postcss plugin for Tailwind v4
- `apps/example/app/globals.css` — @import "tailwindcss" (v4 syntax)
- `apps/example/app/layout.tsx` — RootLayout with metadata
- `apps/example/app/page.tsx` — placeholder (replaced in Wave 3)
- `apps/example/next-env.d.ts` — auto-generated by Next.js
- `.gitignore` — added .next/ and out/ (build artifacts)

## Key Notes

- `pnpm install --no-frozen-lockfile` required (new deps not in lockfile)
- Next.js 15.5.15 build: compiled in 3.9s, 4 static pages generated
- `.next/` was accidentally committed, then removed via `git rm --cached` + .gitignore fix
