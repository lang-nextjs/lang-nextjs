---
phase: v1.2-01
verified: 2026-05-01T09:14:30Z
status: passed
score: 11/11 must-haves verified
---

# Phase v1.2-01: Backend Adapter API + Retry Policy Verification Report

**Phase Goal:** Consumers can pass named adapter bundles to createDeepAgentsHandler and configure retry policy, with zero breaking changes to existing call sites

**Verified:** 2026-05-01T09:14:30Z  
**Status:** PASSED — All must-haves verified. Goal achieved.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Existing createDeepAgentsHandler({ backendUrl }) works unchanged — deepagentsAdapter applied automatically as default | ✓ VERIFIED | handler.ts imports deepagentsAdapter as default (line 13), applies via `effectiveAdapter = options.adapter ?? deepagentsAdapter` (line 137). Backward compatibility test in index.test.ts confirms createDeepAgentsHandler factory works. All 69 tests pass including existing handler tests. |
| 2 | Consumer can pass adapter: langGraphAdapter and LangGraph astream_events v2 frames normalized to AI SDK v6 wire format | ✓ VERIFIED | langGraphAdapter exported from adapters/langgraph.ts, re-exported from adapters/index.ts and index.ts. langGraphAdapter.transforms contains langGraphToAiSdk which maps on_chat_model_stream → text-delta (AI SDK v6 format). 15 langgraph tests pass including on_chat_model_stream mapping, empty content drop, and event type filtering. |
| 3 | Consumer can pass adapter: langchainAdapter and LangChain native SSE frames normalized to AI SDK v6 wire format | ✓ VERIFIED | langchainAdapter exported from adapters/langchain.ts, re-exported from adapters/index.ts and index.ts. langchainAdapter.transforms contains langChainToAiSdk which maps: token → text-delta, message → finish (with intentional content drop), tool_call → tool-input-available. 13 langchain tests pass including deterministic tool call IDs (lc-search-0, lc-search-1). |
| 4 | createDeepAgentsHandler({ backendUrl, retry: { maxRetries: 3, initialDelayMs: 100 } }) retries with exponential backoff; mid-stream failures not retried | ✓ VERIFIED | fetchWithRetry helper in handler.ts implements exponential backoff: delayMs = initialDelayMs * Math.pow(2, attempt) (line 124). 6 retry tests pass: (1) no retry when option omitted, (2) retries up to maxRetries times, (3) exponential backoff timing, (4) succeeds on retry, (5) returns 502 when exhausted, (6) mid-stream failures do NOT trigger retries (only initial fetch() is retried). |
| 5 | defaultTransforms remains importable but marked @deprecated in JSDoc — no runtime removal | ✓ VERIFIED | defaultTransforms exported from transforms.ts (line 67) with @deprecated JSDoc comment (lines 56-65). Index.ts exports defaultTransforms (line 12). No console warnings at runtime. Tests verify defaultTransforms is importable and functional (transforms.test.ts and index.test.ts). |

**Score:** 11/11 must-haves verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| packages/server/src/adapters/deepagents.ts | SseAdapter interface + deepagentsAdapter const + SseTransform import | ✓ VERIFIED | File exists. Exports SseAdapter interface (lines 9-12) and deepagentsAdapter const (lines 19-22). Imports stripMessageIdTransform from transforms (line 2). deepagentsAdapter.name === 'deepagents', transforms: [stripMessageIdTransform]. |
| packages/server/src/adapters/langgraph.ts | langGraphAdapter with LangGraph → AI SDK v6 transform | ✓ VERIFIED | File exists. Exports langGraphAdapter const (lines 77-80) with name: 'langgraph'. Implements langGraphToAiSdk transform mapping on_chat_model_stream → text-delta, drops other event types. |
| packages/server/src/adapters/langchain.ts | langchainAdapter with LangChain → AI SDK v6 transform + createLangchainTransform factory | ✓ VERIFIED | File exists. Exports createLangchainTransform() factory (lines 118-131) with tool call counter, exports langchainAdapter singleton (lines 142-145). Implements token → text-delta, message → finish, tool_call → tool-input-available with deterministic toolCallIds (lc-{toolName}-{index}). |
| packages/server/src/adapters/index.ts | Re-exports adapters | ✓ VERIFIED | File exists. Re-exports deepagentsAdapter, SseAdapter, langGraphAdapter, langchainAdapter, createLangchainTransform (lines 1-4). |
| packages/server/src/handler.ts | Extended DeepAgentsHandlerOptions + fetchWithRetry + default adapter logic | ✓ VERIFIED | File exists. DeepAgentsHandlerOptions includes adapter?: SseAdapter (line 26) and retry?: { maxRetries?, initialDelayMs? } (lines 43-48). fetchWithRetry implemented (lines 111-130). Line 137 applies default: `effectiveAdapter = options.adapter ?? deepagentsAdapter`. |
| packages/server/src/transforms.ts | @deprecated JSDoc on defaultTransforms + stripMessageIdTransform export | ✓ VERIFIED | File exists. stripMessageIdTransform exported (line 23). defaultTransforms exported with @deprecated JSDoc (lines 54-67). No runtime warning. |
| packages/server/src/index.ts | Public exports of adapters + handler + defaultTransforms | ✓ VERIFIED | File exists. Exports createDeepAgentsHandler, DeepAgentsHandlerOptions, defaultTransforms, SseFrame, SseTransform, deepagentsAdapter, SseAdapter, langGraphAdapter, langchainAdapter, createLangchainTransform (lines 10-18). |
| packages/server/src/__fixtures__/langgraph-astream-events-v2.json | LangGraph event fixture | ✓ VERIFIED | File exists (8.1K). Valid JSON. Contains on_chain_start, on_chain_end, on_chat_model_start, on_chat_model_end, on_chat_model_stream events. Used by langgraph.test.ts. |
| packages/server/src/__fixtures__/langchain-native-sse.json | LangChain native SSE fixture | ✓ VERIFIED | File exists (1.1K). Valid JSON. Contains token, tool_call, message frames. Used by langchain.test.ts. |
| packages/server/src/adapters/deepagents.test.ts | deepagentsAdapter unit tests | ✓ VERIFIED | File exists (2.2K). 7 tests verify: name='deepagents', transforms array length, messageId strip on finish frames, passthrough for other frames, [DONE] frames, invalid JSON. All pass. |
| packages/server/src/adapters/langgraph.test.ts | LangGraph adapter TDD tests | ✓ VERIFIED | File exists (5.6K). 15 tests verify: name='langgraph', on_chat_model_stream mapping, empty content drop, other event types dropped. Uses fixture as ground truth. All pass. |
| packages/server/src/adapters/langchain.test.ts | LangChain adapter TDD tests | ✓ VERIFIED | File exists (7.5K). 13 tests verify: name='langchain', token frames, tool_call frames, message frames (content intentionally dropped), deterministic toolCallIds. Uses fixture as ground truth. All pass. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| packages/server/src/handler.ts | packages/server/src/adapters/deepagents.ts | import deepagentsAdapter as default | ✓ WIRED | Line 13: `import { deepagentsAdapter }`. Line 137: `effectiveAdapter = options.adapter ?? deepagentsAdapter`. Used in allTransforms pipeline. |
| packages/server/src/adapters/deepagents.ts | packages/server/src/transforms.ts | import stripMessageIdTransform | ✓ WIRED | Line 2: `import { stripMessageIdTransform }`. Line 21: used in deepagentsAdapter.transforms array. |
| packages/server/src/adapters/index.ts | packages/server/src/adapters/deepagents.ts | re-export deepagentsAdapter | ✓ WIRED | Lines 1-2 re-export deepagentsAdapter and SseAdapter. |
| packages/server/src/adapters/index.ts | packages/server/src/adapters/langgraph.ts | re-export langGraphAdapter | ✓ WIRED | Line 3 re-exports langGraphAdapter from './langgraph'. |
| packages/server/src/adapters/index.ts | packages/server/src/adapters/langchain.ts | re-export langchainAdapter | ✓ WIRED | Line 4 re-exports langchainAdapter and createLangchainTransform from './langchain'. |
| packages/server/src/index.ts | packages/server/src/adapters/deepagents.ts | public export deepagentsAdapter | ✓ WIRED | Lines 15-16 export deepagentsAdapter and SseAdapter type. |
| packages/server/src/index.ts | packages/server/src/adapters/langgraph.ts | public export langGraphAdapter | ✓ WIRED | Line 17 exports langGraphAdapter. |
| packages/server/src/index.ts | packages/server/src/adapters/langchain.ts | public export langchainAdapter | ✓ WIRED | Line 18 exports langchainAdapter and createLangchainTransform. |
| packages/server/src/index.ts | packages/server/src/transforms.ts | public export defaultTransforms | ✓ WIRED | Line 12 exports defaultTransforms. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| ADAPT-01 | v1.2-01-02-PLAN.md | Consumer can pass `adapter` option to createDeepAgentsHandler. Adapter type is { name, transforms }. Pipeline is [...adapter.transforms, ...options.transforms]. | ✓ SATISFIED | DeepAgentsHandlerOptions.adapter?: SseAdapter (handler.ts lines 26). effectiveAdapter logic line 137. allTransforms pipeline line 138. deepagentsAdapter is default. |
| ADAPT-02 | v1.2-01-02-PLAN.md | deepagentsAdapter exported from @deepagents-nextjs/server. Used as default when adapter omitted. defaultTransforms kept as @deprecated alias. | ✓ SATISFIED | deepagentsAdapter exported from index.ts line 15. Default applied in handler.ts line 137. defaultTransforms exported with @deprecated JSDoc. |
| ADAPT-03 | v1.2-01-03-PLAN.md | langGraphAdapter exported. Normalizes LangGraph astream_events v2 SSE to AI SDK v6 wire format. | ✓ SATISFIED | langGraphAdapter exported from index.ts line 17. Implements on_chat_model_stream → text-delta mapping. 15 tests pass. |
| ADAPT-04 | v1.2-01-04-PLAN.md | langchainAdapter exported. Normalizes LangChain native SSE to AI SDK v6 wire format. | ✓ SATISFIED | langchainAdapter exported from index.ts line 18. Implements token → text-delta, message → finish, tool_call → tool-input-available. 13 tests pass. |
| STR-02 | v1.2-01-02-PLAN.md | createDeepAgentsHandler accepts optional retry policy. Retries on connection failure with exponential backoff. Mid-stream failures do not retry. | ✓ SATISFIED | retry?: { maxRetries?, initialDelayMs? } in DeepAgentsHandlerOptions (handler.ts lines 43-48). fetchWithRetry with exponential backoff (lines 111-130). 6 retry tests pass including mid-stream non-retry verification. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| (none) | — | — | — | No TODO, FIXME, XXX, HACK, placeholder, or console.log-only implementations found. |

### Formal Verification

No formal verification artifacts defined for this phase.

### Backward Compatibility Check

✓ **ZERO BREAKING CHANGES CONFIRMED**

- Existing call `createDeepAgentsHandler({ backendUrl })` works identically to v1.1 because deepagentsAdapter is applied by default (line 137 of handler.ts: `options.adapter ?? deepagentsAdapter`)
- deepagentsAdapter.transforms === defaultTransforms (both = [stripMessageIdTransform])
- All existing handler tests pass (12 tests in handler.test.ts)
- All existing transforms tests pass (6 tests in transforms.test.ts)
- Public API exports unchanged: createDeepAgentsHandler, DeepAgentsHandlerOptions, SseFrame, SseTransform, defaultTransforms remain at identical import paths
- New exports (deepagentsAdapter, langGraphAdapter, langchainAdapter, SseAdapter, createLangchainTransform) are additive only

### Test Summary

**Full test suite: 69 tests PASSED**

- transforms.test.ts: 6 tests ✓
- adapters/deepagents.test.ts: 7 tests ✓
- adapters/langgraph.test.ts: 15 tests ✓
- adapters/langchain.test.ts: 13 tests ✓
- accumulator.test.ts: 6 tests ✓
- index.test.ts: 3 tests ✓
- handler.test.ts: 19 tests ✓ (includes 6 new retry tests)

No test failures. No regressions.

### Build Verification

✓ TypeScript compilation: SUCCESS  
✓ ESM build (dist/index.mjs): 9.01 KB ✓  
✓ CJS build (dist/index.js): 9.21 KB ✓  
✓ Type definitions (dist/index.d.ts): 7.62 KB ✓

### Fixtures Validation

✓ packages/server/src/__fixtures__/langgraph-astream-events-v2.json: Valid JSON (8.1K)  
✓ packages/server/src/__fixtures__/langchain-native-sse.json: Valid JSON (1.1K)

Both fixtures used as ground truth in TDD tests. All fixture-based tests pass.

## Summary

Phase v1.2-01 achieves its goal. All 11 must-haves verified:

1. **Backward compatibility:** Existing code using createDeepAgentsHandler({ backendUrl }) continues to work without any changes. deepagentsAdapter is applied automatically.

2. **Adapter system:** Named adapters (deepagentsAdapter, langGraphAdapter, langchainAdapter) are exported and wired into the handler. Pipeline correctly sequences adapter transforms before user transforms.

3. **Retry policy:** Exponential backoff retry logic with configurable maxRetries and initialDelayMs. Mid-stream failures do NOT trigger retries (only connection-level fetch failures). Default is maxRetries=0 (no retry when option omitted).

4. **Deprecation:** defaultTransforms remains importable but marked @deprecated in JSDoc. No runtime warning. No removal.

5. **Testing:** 69 tests pass covering all adapters, retry logic, and backward compatibility. Fixtures (LangGraph astream_events v2, LangChain native SSE) are valid and used as ground truth in TDD tests.

6. **Public API:** All new exports are present and functional. Zero breaking changes to existing exports.

**Status: READY FOR MERGE**

---
*Verified: 2026-05-01T09:14:30Z*  
*Verifier: Claude (nf-verifier)*
