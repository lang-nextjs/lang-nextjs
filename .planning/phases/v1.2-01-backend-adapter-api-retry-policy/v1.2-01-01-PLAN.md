---
phase: v1.2-01
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/__fixtures__/langgraph-astream-events-v2.json
  - packages/server/src/__fixtures__/langchain-native-sse.json
autonomous: true
requirements:
  - ADAPT-03
  - ADAPT-04
formal_artifacts: none

must_haves:
  truths:
    - "Fixture file packages/server/src/__fixtures__/langgraph-astream-events-v2.json exists and contains at least 3 real LangGraph astream_events v2 SSE frame payloads captured from the FastAPI backend"
    - "Fixture file packages/server/src/__fixtures__/langchain-native-sse.json exists and contains at least 3 real LangChain native SSE frame payloads captured from a backend emitting LangChain format"
  artifacts:
    - path: "packages/server/src/__fixtures__/langgraph-astream-events-v2.json"
      provides: "Real LangGraph frame samples for adapter TDD"
      contains: "on_llm_stream"
    - path: "packages/server/src/__fixtures__/langchain-native-sse.json"
      provides: "Real LangChain frame samples for adapter TDD"
  key_links:
    - from: "packages/server/src/__fixtures__/langgraph-astream-events-v2.json"
      to: "packages/server/src/adapters/langgraph.test.ts"
      via: "import in test file"
      pattern: "__fixtures__/langgraph"
    - from: "packages/server/src/__fixtures__/langchain-native-sse.json"
      to: "packages/server/src/adapters/langchain.test.ts"
      via: "import in test file"
      pattern: "__fixtures__/langchain"
---

<objective>
Capture real SSE frame shapes from the existing FastAPI (LangGraph) and Django backends into JSON fixture files before implementing adapters.

Purpose: ADAPT-03 and ADAPT-04 transforms must map real frame shapes — not documentation assumptions. Implementing against unvalidated frame shapes leads to silent mismatches against live backends.
Output: Two JSON fixture files in packages/server/src/__fixtures__/ containing real frame arrays from each backend format.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v1.2-01-backend-adapter-api-retry-policy/v1.2-01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture LangGraph astream_events v2 fixture from FastAPI backend</name>
  <files>packages/server/src/__fixtures__/langgraph-astream-events-v2.json</files>
  <action>
    Start the FastAPI backend (the one using LangGraph) and capture raw SSE frames by streaming a chat request.

    Step-by-step:
    1. `cd /Users/jonathanborduas/code/deepagents-nextjs/apps/example` (or wherever docker-compose.yml lives for backends)
    2. Run `docker compose up -d` (or whichever compose file starts the FastAPI backend). Wait for healthy status.
    3. Identify the FastAPI backend's SSE endpoint (likely http://localhost:3002/stream or similar). Check docker-compose.yml or the FastAPI app for the correct port and path.
    4. Send a streaming POST request and capture raw SSE frames:
       ```
       curl -N -s -X POST http://localhost:<FASTAPI_PORT>/<stream_path> \
         -H 'Content-Type: application/json' \
         -d '{"message": "hello", "session_id": "fixture-capture"}' 2>&1 | head -100
       ```
    5. Parse the output: each SSE frame is on its own line(s) in `data: {json}\n\n` format. Extract the JSON payloads from `data:` lines.
    6. Create the `__fixtures__/` directory if it does not exist.
    7. Write `packages/server/src/__fixtures__/langgraph-astream-events-v2.json` as a JSON array of the raw parsed event objects (the JSON object after `data: `). Include at minimum:
       - At least one `on_llm_stream` event
       - At least one `on_llm_end` or `on_chain_end` event
       - The `[DONE]` terminator entry as `{"done": true}` or a raw string entry `"[DONE]"` if present
       - Any other distinct event types observed
    8. If the backend is unreachable or returns unexpected format, fall back to constructing representative fixture data from the RESEARCH.md frame shape documentation (MEDIUM-HIGH confidence level from research). Document this in a comment at the top of the file as `"_source": "research-docs"` vs `"_source": "live-capture"`.

    Format of the fixture file:
    ```json
    {
      "_source": "live-capture | research-docs",
      "_captured": "2026-05-01",
      "_backend": "FastAPI + LangGraph",
      "frames": [
        {"type": "on_llm_stream", "ns": ["graph", "agent"], "data": {"chunk": {"content": "Hello"}}},
        ...
      ]
    }
    ```

    Do NOT use placeholder data like `{"type": "example"}`. Use real shapes from either live capture or the research documentation's validated field mappings.
  </action>
  <verify>
    1. `ls packages/server/src/__fixtures__/langgraph-astream-events-v2.json` — file exists
    2. `node -e "const f = require('./packages/server/src/__fixtures__/langgraph-astream-events-v2.json'); console.log(f.frames.length)"` — prints a number >= 3
    3. Check at least one frame has `type: "on_llm_stream"` or similar LangGraph event type
  </verify>
  <done>packages/server/src/__fixtures__/langgraph-astream-events-v2.json exists, is valid JSON, has a `frames` array with ≥ 3 entries including at least one streaming token frame (on_llm_stream or equivalent)</done>
</task>

<task type="auto">
  <name>Task 2: Capture LangChain native SSE fixture (Django or synthetic from research docs)</name>
  <files>packages/server/src/__fixtures__/langchain-native-sse.json</files>
  <action>
    The Django backend may emit LangChain native SSE format. Attempt to capture from it; if not available or if it already uses AI SDK v6 format, construct representative fixtures from RESEARCH.md validated field mappings.

    Step-by-step:
    1. Check if the Django backend is running (typically port 3001). If not, try `docker compose up -d`.
    2. Send a streaming POST request to the Django SSE endpoint and capture output:
       ```
       curl -N -s -X POST http://localhost:3001/stream/ \
         -H 'Content-Type: application/json' \
         -d '{"message": "hello", "session_id": "fixture-capture"}' 2>&1 | head -100
       ```
    3. If the Django backend already returns AI SDK v6 format (start/text-delta/finish events), the LangChain native SSE fixture must be constructed from research docs. In that case, set `"_source": "research-docs"` in the fixture.
    4. LangChain native SSE format (from RESEARCH.md):
       - `event: token\ndata: {"type": "token", "text": "Hello"}`
       - `event: message\ndata: {"type": "message", "content": [{"type": "text", "text": "Hello"}]}`
       - `event: tool_call\ndata: {"type": "tool_call", "tool_name": "search", "tool_input": {"query": "weather"}}`
    5. Write `packages/server/src/__fixtures__/langchain-native-sse.json` in the same format as Plan 01 Task 1:
    ```json
    {
      "_source": "live-capture | research-docs",
      "_captured": "2026-05-01",
      "_backend": "LangChain native SSE",
      "frames": [
        {"_event": "token", "type": "token", "text": "Hello"},
        {"_event": "token", "type": "token", "text": " world"},
        {"_event": "message", "type": "message", "content": [{"type": "text", "text": "Hello world"}]}
      ]
    }
    ```
    Include `_event` field to capture the SSE `event:` type alongside the `data:` payload.
    Include at minimum:
    - ≥ 2 token frames
    - ≥ 1 message/end frame
    - ≥ 1 tool_call frame (if available; otherwise synthesize from research docs)

    Do NOT conflate with LangGraph format. LangChain native format is event-based SSE (event: token, event: message), while LangGraph uses data-only JSON with a `type` field.
  </action>
  <verify>
    1. `ls packages/server/src/__fixtures__/langchain-native-sse.json` — file exists
    2. `node -e "const f = require('./packages/server/src/__fixtures__/langchain-native-sse.json'); console.log(f.frames.length)"` — prints a number >= 3
    3. At least one frame has a `_event` field (token, message, or tool_call)
  </verify>
  <done>packages/server/src/__fixtures__/langchain-native-sse.json exists, is valid JSON, has a `frames` array with ≥ 3 entries, at least one with `_event: "token"` and one with `_event: "message"`</done>
</task>

</tasks>

<verification>
- Both fixture files exist: `ls packages/server/src/__fixtures__/*.json`
- Both are valid JSON: `node -e "require('./packages/server/src/__fixtures__/langgraph-astream-events-v2.json'); require('./packages/server/src/__fixtures__/langchain-native-sse.json'); console.log('valid')"`
- LangGraph fixture has `on_llm_stream` or similar token frame
- LangChain fixture has `_event` field distinguishing SSE event types
</verification>

<success_criteria>
Both fixture files exist with valid JSON, contain ≥ 3 real or research-validated frame samples each, and capture distinct frame shapes from LangGraph (data-only JSON with `type` field) and LangChain native SSE (event+data pairs).
</success_criteria>

<output>
After completion, create `.planning/phases/v1.2-01-backend-adapter-api-retry-policy/v1.2-01-01-SUMMARY.md`
</output>
