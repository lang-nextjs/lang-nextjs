---
phase: v1.1-01
plan: 03
subsystem: testing
tags: [playwright, e2e, sse, createDeepAgentsHandler, nextjs, typescript]

# Dependency graph
requires:
  - phase: v1.0-04
    provides: apps/example chat UI and mock SSE route (MockLanguageModelV3)
  - phase: v1.1-01-02
    provides: createDeepAgentsHandler implementation in @deepagents-nextjs/server
provides:
  - apps/example route.ts proxies to real DeepAgents backend via createDeepAgentsHandler
  - apps/example route.mock.ts preserves original MockLanguageModelV3 mock for reference
  - Playwright E2E test suite (e2e/chat.spec.ts) with 5 assertions
  - playwright.config.ts backend-agnostic via PLAYWRIGHT_BASE_URL
affects: [v1.1-01-04, ci-workflows, docker-compose]

# Tech tracking
tech-stack:
  added: ["@playwright/test ^1.44.0"]
  patterns:
    - "Route conditional: BACKEND_URL present -> real handler; absent -> 503 JSON response"
    - "Mock preserved in route.mock.ts alongside production route.ts for easy swap"
    - "PLAYWRIGHT_BASE_URL env var makes E2E suite backend-agnostic (Django vs FastAPI)"

key-files:
  created:
    - apps/example/app/api/chat/stream/route.mock.ts
    - e2e/chat.spec.ts
    - playwright.config.ts
  modified:
    - apps/example/app/api/chat/stream/route.ts
    - apps/example/package.json
    - package.json

key-decisions:
  - "Route returns 503 JSON (not throw) when BACKEND_URL unset — avoids Next.js cold start crash"
  - "NextRequest type used in route.ts POST signature to match createDeepAgentsHandler parameter type"
  - "E2E suite uses PLAYWRIGHT_BASE_URL env var so same spec runs against Django (3001) and FastAPI (3002)"
  - "route.mock.ts preserved verbatim from original route.ts — no diff, easy reference and swap"

patterns-established:
  - "Pattern: E2E spec checks finish frame messageId absence to prove defaultTransforms pipeline ran"
  - "Pattern: Playwright config retries=1 in CI only, list + html reporters"

requirements-completed: [E2E-03, E2E-04]

# Metrics
duration: 4min
completed: 2026-04-29
---

# Phase v1.1-01 Plan 03: E2E Route + Playwright Suite Summary

**createDeepAgentsHandler wired into apps/example with 503 fallback, plus 5-assertion Playwright E2E suite proving SSE delivery and defaultTransforms messageId strip**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-29T13:44:55Z
- **Completed:** 2026-04-29T13:48:50Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- apps/example route.ts now proxies to real DeepAgents backend via `createDeepAgentsHandler` when `BACKEND_URL` is set; returns 503 JSON otherwise
- Original MockLanguageModelV3 mock preserved verbatim in `route.mock.ts` for local development
- Playwright E2E suite in `e2e/chat.spec.ts` with 5 assertions: HTTP 200 + SSE content-type, text-delta frames, finish frame has no `messageId` (defaultTransforms proof), no error frames, UI renders AIMessage
- `playwright.config.ts` at repo root uses `PLAYWRIGHT_BASE_URL` env var (defaults to localhost:3000) — same spec targets Django or FastAPI compose stacks
- `@playwright/test` added to root devDependencies; `e2e` script added to root package.json

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend example route to use createDeepAgentsHandler** - `3a9bc0e` (feat)
2. **Task 2: Playwright E2E suite and root config** - `bec557a` (feat)

**Plan metadata:** (docs commit, see below)

## Files Created/Modified
- `apps/example/app/api/chat/stream/route.ts` - Real handler using createDeepAgentsHandler; 503 when BACKEND_URL absent
- `apps/example/app/api/chat/stream/route.mock.ts` - Original MockLanguageModelV3 mock preserved unchanged
- `apps/example/package.json` - Added @deepagents-nextjs/server workspace:* dependency
- `e2e/chat.spec.ts` - 5-test Playwright suite: SSE delivery + defaultTransforms messageId strip
- `playwright.config.ts` - PLAYWRIGHT_BASE_URL-configurable, chromium project, 60s timeout
- `package.json` (root) - Added @playwright/test devDep and e2e script

## Decisions Made
- Route returns 503 JSON when BACKEND_URL unset rather than throwing at module load — avoids Next.js cold start crash during `pnpm dev` without a backend
- Used `NextRequest` (not `Request`) in route POST signature to match `createDeepAgentsHandler` parameter type (auto-fixed per Rule 1)
- PLAYWRIGHT_BASE_URL env var makes E2E suite backend-agnostic — same spec runs against Django (port 3001) and FastAPI (port 3002) compose stacks

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed NextRequest type mismatch in route.ts**
- **Found during:** Task 1 (typecheck verification step)
- **Issue:** Plan's route.ts template used `Request` as POST parameter type, but `createDeepAgentsHandler` returns a function accepting `NextRequest` — typecheck failed with TS2345
- **Fix:** Added `import { type NextRequest } from 'next/server'` and changed POST signature to `(request: NextRequest)`
- **Files modified:** apps/example/app/api/chat/stream/route.ts
- **Verification:** `pnpm --filter example typecheck` exits 0
- **Committed in:** 3a9bc0e (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — type mismatch bug)
**Impact on plan:** Essential for typecheck to pass. No scope creep.

## Issues Encountered
None beyond the Rule 1 auto-fix above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- E2E infrastructure ready: `pnpm e2e` runs Playwright against any Next.js frontend URL
- Plan 04 (Docker Compose stacks) will start the Django and FastAPI backends so E2E tests can actually pass
- The `PLAYWRIGHT_BASE_URL` hook is in place for CI job parameterization

---
*Phase: v1.1-01-reference-backend-stacks*
*Completed: 2026-04-29*
