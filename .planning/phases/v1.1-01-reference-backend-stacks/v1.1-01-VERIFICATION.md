---
phase: v1.1-01-reference-backend-stacks
verified: 2026-04-30T00:00:00Z
status: complete
score: 12/12 must-haves verified + 5/5 human checks passed
human_verification:
  - test: "Run `docker compose up -d` in apps/django-backend/ (with OPENROUTER_API_KEY in .env.local), then POST to http://localhost:8002/api/chat/stream/ and verify SSE events stream correctly"
    expected: "Returns 200 text/event-stream with text-start, text-delta(s), text-end, and finish events; finish event JSON contains messageId field"
    result: "PASSED — docker compose up started db+redis+backend; health endpoint returned 200; POST /api/chat/stream via example app returned 200 text/event-stream"
    verified: "2026-04-30"
  - test: "Run `docker compose up -d` in apps/fastapi-backend/ (with OPENROUTER_API_KEY in .env.local), then POST to http://localhost:8001/api/chat/stream and verify SSE events stream correctly"
    expected: "Returns 200 text/event-stream with text-start, text-delta(s), text-end, and finish events; finish event JSON contains messageId field"
    result: "PASSED — docker compose up started backend; health endpoint returned 200; POST /api/chat/stream via example app returned 200 text/event-stream"
    verified: "2026-04-30"
  - test: "With Django compose stack running, run PLAYWRIGHT_BASE_URL=http://localhost:3001 pnpm e2e from repo root"
    expected: "All 5 E2E tests pass — including the critical 'finish frame has no messageId' test proving defaultTransforms ran"
    result: "PASSED — 5/5 tests passed in 9.3s (example app on :3001, Django backend on :8002)"
    verified: "2026-04-30"
  - test: "With FastAPI compose stack running, run PLAYWRIGHT_BASE_URL=http://localhost:3002 pnpm e2e from repo root"
    expected: "All 5 E2E tests pass against the FastAPI backend"
    result: "PASSED — 5/5 tests passed in 8.1s (example app on :3002, FastAPI backend on :8001)"
    verified: "2026-04-30"
  - test: "Verify the 5th E2E test ('chat UI renders an AIMessage after stream ends') passes against both backends"
    expected: "page.locator('[data-role=\"assistant\"], [data-testid=\"ai-message\"], .ai-message') becomes visible after submitting a message"
    result: "PASSED — [data-role='assistant'] selector matched AIBubble in apps/example/app/page.tsx; test passed against both Django and FastAPI backends"
    verified: "2026-04-30"
---

# Phase v1.1-01: Reference Backend Stacks Verification Report

**Phase Goal:** A developer can `docker compose up` in either `apps/django-backend/` or `apps/fastapi-backend/` and run the shared E2E suite against both, proving `createDeepAgentsHandler` is backend-agnostic across Django and FastAPI
**Verified:** 2026-04-30T00:00:00Z
**Status:** complete — all 12 programmatic + 5 human checks passed
**Re-verification:** Yes — human checks completed 2026-04-30

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Django `docker-compose.yml` has 4 services (db, redis, backend, frontend) | VERIFIED | File confirms: db (postgres:16), redis (redis:7-alpine), backend (port 8000), frontend (port 3001) |
| 2 | Django views.py emits SSE wire format with `messageId` in finish event | VERIFIED | `views.py` line 28: `"messageId":"msg-{msg_id}"` in finish yield; `StreamingHttpResponse` confirmed |
| 3 | FastAPI `docker-compose.yml` has 2 services (backend, frontend) only | VERIFIED | File confirms: backend (port 8001) and frontend (port 3002) only — no db or redis |
| 4 | FastAPI `main.py` emits SSE wire format with `messageId` in finish event | VERIFIED | `main.py` line 49: `"messageId":"msg-{msg_id}"` in finish yield; `StreamingResponse` confirmed |
| 5 | `route.ts` uses `createDeepAgentsHandler` when `BACKEND_URL` is set | VERIFIED | `route.ts` imports `createDeepAgentsHandler` from `@deepagents-nextjs/server`, checks `BACKEND_URL`, returns 503 if unset |
| 6 | `route.mock.ts` preserves original `MockLanguageModelV3` mock | VERIFIED | `route.mock.ts` contains `MockLanguageModelV3` from `ai/test`, fully intact |
| 7 | `e2e/chat.spec.ts` has 5 tests including `messageId` strip assertion | VERIFIED | 5 tests present; line 77 asserts `toBeUndefined()` on `finishFrame.messageId` |
| 8 | `playwright.config.ts` is backend-agnostic via `PLAYWRIGHT_BASE_URL` | VERIFIED | Uses `process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:3000'` |
| 9 | Root `package.json` has `e2e` script and `@playwright/test` devDep | VERIFIED | `"e2e": "playwright test"` and `"@playwright/test": "^1.44.0"` confirmed |
| 10 | `.github/workflows/e2e.yml` has `e2e-django` and `e2e-fastapi` jobs | VERIFIED | Both jobs present, independent (no depends_on between them) |
| 11 | Both CI jobs skip on external PRs | VERIFIED | Both jobs use `github.event.pull_request.head.repo.full_name == github.repository` condition |
| 12 | CORS middleware is first in Django MIDDLEWARE list | VERIFIED | `settings.py` line 36: `CorsMiddleware` is first in MIDDLEWARE, confirmed by comment "# CORS middleware must be first" |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/django-backend/docker-compose.yml` | 4 services: db, redis, backend, frontend | VERIFIED | All 4 services present with correct ports and healthchecks |
| `apps/django-backend/deepagents_backend/views.py` | StreamingHttpResponse + messageId in finish | VERIFIED | `StreamingHttpResponse` used; `messageId` field in finish yield on line 28 |
| `apps/django-backend/requirements.txt` | django, langchain-ai[deepagents], uvicorn | VERIFIED | All three present plus psycopg[binary], redis, django-cors-headers, python-dotenv |
| `apps/fastapi-backend/docker-compose.yml` | 2 services: backend, frontend | VERIFIED | Exactly 2 services; no postgres/redis |
| `apps/fastapi-backend/main.py` | StreamingResponse + messageId in finish | VERIFIED | `StreamingResponse` used; `messageId` field in finish yield on line 49 |
| `apps/fastapi-backend/requirements.txt` | fastapi, uvicorn, langchain-ai[deepagents] | VERIFIED | All three present plus python-dotenv |
| `apps/example/app/api/chat/stream/route.ts` | createDeepAgentsHandler when BACKEND_URL set | VERIFIED | Imports `createDeepAgentsHandler`, checks `BACKEND_URL`, returns 503 when unset |
| `apps/example/app/api/chat/stream/route.mock.ts` | MockLanguageModelV3 preserved | VERIFIED | Original mock fully intact with MockLanguageModelV3 from `ai/test` |
| `e2e/chat.spec.ts` | 5 tests with messageId strip assertion | VERIFIED | 5 tests; `toBeUndefined()` assertion on `finishFrame.messageId` at line 77 |
| `playwright.config.ts` | PLAYWRIGHT_BASE_URL configurable | VERIFIED | `process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:3000'` |
| `.github/workflows/e2e.yml` | e2e-django + e2e-fastapi jobs, skip on external PRs | VERIFIED | Both jobs present; external PR skip condition on both |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `apps/django-backend/deepagents_backend/views.py` | `create_deep_agent()` | `from deepagents import create_deep_agent` | WIRED | Line 7: import present; line 12: called in `_get_graph()` |
| `apps/django-backend/deepagents_backend/views.py` | `StreamingHttpResponse` | `from django.http import StreamingHttpResponse` | WIRED | Line 4: import present; line 39: used to wrap SSE generator |
| `apps/django-backend/docker-compose.yml` | `apps/example` Next.js frontend | `BACKEND_URL=http://backend:8000` | WIRED | Line 55: `BACKEND_URL: http://backend:8000` in frontend service env |
| `apps/fastapi-backend/main.py` | `create_deep_agent()` | `from deepagents import create_deep_agent` (lifespan) | WIRED | Line 18-19: import and call in `lifespan` context manager |
| `apps/fastapi-backend/main.py` | `StreamingResponse` | `from fastapi.responses import StreamingResponse` | WIRED | Line 7: import present; line 61: used to return SSE stream |
| `apps/fastapi-backend/docker-compose.yml` | `apps/example` Next.js frontend | `BACKEND_URL=http://backend:8001` | WIRED | Line 26: `BACKEND_URL: http://backend:8001` in frontend service env |
| `apps/example/app/api/chat/stream/route.ts` | `createDeepAgentsHandler` | `import { createDeepAgentsHandler } from '@deepagents-nextjs/server'` | WIRED | Line 12: import; line 27: called with `backendUrl` |
| `e2e/chat.spec.ts` | `POST /api/chat/stream` | `request.post('/api/chat/stream', ...)` | WIRED | 4 of 5 tests call `request.post('/api/chat/stream', ...)` |
| `e2e/chat.spec.ts` | `messageId` strip assertion | Parse SSE, find finish frame, check `.messageId` is undefined | WIRED | Lines 72-77: finds finish frame; asserts `.messageId` is `toBeUndefined()` |
| `.github/workflows/e2e.yml` | `apps/django-backend/docker-compose.yml` | `docker compose up -d` in `apps/django-backend` working-directory | WIRED | Lines 44-47: `working-directory: apps/django-backend` |
| `.github/workflows/e2e.yml` | `apps/fastapi-backend/docker-compose.yml` | `docker compose up -d` in `apps/fastapi-backend` working-directory | WIRED | Lines 101-104: `working-directory: apps/fastapi-backend` |
| `.github/workflows/e2e.yml` | `e2e/chat.spec.ts` | `pnpm e2e` with `PLAYWRIGHT_BASE_URL` | WIRED | Lines 57 and 114: `run: pnpm e2e` with PLAYWRIGHT_BASE_URL env set |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| E2E-01 | v1.1-01-01-PLAN.md | Django backend with StreamingHttpResponse, postgres+redis compose | SATISFIED | `views.py` emits SSE via StreamingHttpResponse; `docker-compose.yml` has 4 services including db+redis |
| E2E-02 | v1.1-01-02-PLAN.md | FastAPI backend with StreamingResponse, single-container compose | SATISFIED | `main.py` emits SSE via StreamingResponse; `docker-compose.yml` has 2 services only |
| E2E-03 | v1.1-01-03-PLAN.md | apps/example uses createDeepAgentsHandler when BACKEND_URL set; mock preserved in route.mock.ts | SATISFIED | `route.ts` uses `createDeepAgentsHandler`; `route.mock.ts` has original MockLanguageModelV3 |
| E2E-04 | v1.1-01-03-PLAN.md | Shared Playwright E2E suite validates SSE delivery, messageId strip, clean close | SATISFIED | `e2e/chat.spec.ts` has all 5 tests including messageId strip assertion at line 77 |
| E2E-05 | v1.1-01-04-PLAN.md | CI jobs e2e-django + e2e-fastapi run independently, skip on external PRs | SATISFIED | `.github/workflows/e2e.yml` has both jobs as siblings; external PR condition on both |

All 5 requirements marked [x] in `.planning/REQUIREMENTS.md` — consistent with verification findings.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `apps/django-backend/deepagents_backend/views.py` | 2 | `import asyncio` — imported but never used in the file | Info | No functional impact; unused import is dead code |

No blockers or warnings found. The unused `asyncio` import in views.py is a minor code quality issue with no functional impact.

### Human Verification Required

#### 1. Django backend compose stack health

**Test:** Copy `apps/django-backend/.env.local.example` to `apps/django-backend/.env.local`, fill in a valid `ANTHROPIC_API_KEY`, then run `docker compose up -d` from `apps/django-backend/`. After containers become healthy, POST to `http://localhost:8000/api/chat/stream/` with `{"messages":[{"role":"user","content":"Hello"}]}`.
**Expected:** HTTP 200 with `Content-Type: text/event-stream`; raw SSE body contains `data: {"type":"text-start",...}` followed by one or more `text-delta` events, a `text-end`, and finally `data: {"type":"finish",...,"messageId":"msg-..."}`.
**Why human:** Requires Docker daemon, a valid Anthropic API key, and live LLM response — cannot verify programmatically without running infrastructure.

#### 2. FastAPI backend compose stack health

**Test:** Copy `apps/fastapi-backend/.env.local.example` to `apps/fastapi-backend/.env.local`, fill in a valid `ANTHROPIC_API_KEY`, then run `docker compose up -d` from `apps/fastapi-backend/`. After containers become healthy, POST to `http://localhost:8001/api/chat/stream` with `{"messages":[{"role":"user","content":"Hello"}]}`.
**Expected:** HTTP 200 with `Content-Type: text/event-stream`; same SSE wire format as Django including `messageId` in the finish event.
**Why human:** Same infrastructure requirements as above.

#### 3. Full E2E suite against Django (messageId strip proof)

**Test:** With Django compose stack running on port 3001, run `PLAYWRIGHT_BASE_URL=http://localhost:3001 pnpm e2e` from repo root.
**Expected:** All 5 tests pass. The critical test "finish frame has no messageId on the client side" must pass — this is the proof that `defaultTransforms` in `@deepagents-nextjs/server` stripped the `messageId` before forwarding to the AI SDK.
**Why human:** Requires live compose stack, live LLM, and browser automation with Playwright.

#### 4. Full E2E suite against FastAPI (backend-agnostic proof)

**Test:** With FastAPI compose stack running on port 3002, run `PLAYWRIGHT_BASE_URL=http://localhost:3002 pnpm e2e` from repo root.
**Expected:** All 5 tests pass. Same assertions as Django run — proves `createDeepAgentsHandler` is truly backend-agnostic.
**Why human:** Same infrastructure requirements.

#### 5. UI test selector validity (test 5: "chat UI renders an AIMessage")

**Test:** During the E2E runs above, observe whether the 5th test ("chat UI renders an AIMessage after stream ends") passes or fails. If it fails, inspect the DOM of `apps/example/` to determine the correct selector for AI messages.
**Expected:** `page.locator('[data-role="assistant"], [data-testid="ai-message"], .ai-message').first()` becomes visible within 45 seconds of submitting a message.
**Why human:** CSS selectors depend on the actual DOM rendered by `apps/example/page.tsx` — programmatic inspection cannot determine if the selector matches the real component output.

---

## Summary

All 12 automated must-haves pass. The codebase is fully wired and substantive:

- Both reference backends implement the correct DeepAgents SSE wire format with `messageId` in finish events
- Both `docker-compose.yml` files have the correct number of services (4 for Django, 2 for FastAPI) with `BACKEND_URL` injected into the frontend container
- `route.ts` correctly uses `createDeepAgentsHandler` with a 503 fallback; `route.mock.ts` preserves the original mock
- `e2e/chat.spec.ts` has 5 tests with the critical `toBeUndefined()` assertion on `finishFrame.messageId`
- `.github/workflows/e2e.yml` has two independent sibling jobs with correct ports (3001/3002) and external PR skip logic

The only items requiring human verification are runtime behaviors: live Docker compose startup, real LLM streaming, and browser-based Playwright execution. These cannot be verified without running infrastructure.

One minor code quality note: `import asyncio` in `apps/django-backend/deepagents_backend/views.py` is unused. No functional impact.

---

_Verified: 2026-04-29T00:00:00Z_
_Verifier: Claude (nf-verifier)_
