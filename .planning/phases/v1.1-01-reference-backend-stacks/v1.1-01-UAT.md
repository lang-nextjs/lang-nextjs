---
status: passed
phase: v1.1-01-reference-backend-stacks
source: [v1.1-01-01-SUMMARY.md, v1.1-01-02-SUMMARY.md, v1.1-01-03-SUMMARY.md, v1.1-01-04-SUMMARY.md]
started: 2026-04-29T14:00:00Z
updated: 2026-04-29T19:30:00Z
---

## Tests

### 1. Django SSE wire format — text-start/delta/end/finish with messageId
expected: views.py emits text-start, text-delta, text-end, finish (with messageId field) in that order
result: pass
method: auto:inspect

### 2. Django compose — 4 services with correct ports and BACKEND_URL
expected: docker-compose.yml has db (postgres:16), redis (redis:7-alpine), backend (8000), frontend (3001), BACKEND_URL=http://backend:8000
result: pass
method: auto:inspect

### 3. FastAPI SSE wire format — same protocol as Django
expected: main.py emits identical text-start, text-delta, text-end, finish (with messageId) format
result: pass
method: auto:inspect

### 4. FastAPI compose — 2 services only, no postgres/redis
expected: docker-compose.yml has exactly backend (8001) and frontend (3002), no db or cache services
result: pass
method: auto:inspect

### 5. Example route uses createDeepAgentsHandler when BACKEND_URL set
expected: route.ts imports createDeepAgentsHandler from @deepagents-nextjs/server, uses BACKEND_URL, returns 503 JSON when absent
result: pass
method: auto:inspect

### 6. MockLanguageModelV3 mock preserved in route.mock.ts
expected: route.mock.ts contains original MockLanguageModelV3-based implementation unchanged
result: pass
method: auto:inspect

### 7. E2E suite has messageId strip assertion proving defaultTransforms ran
expected: e2e/chat.spec.ts asserts finishFrame.messageId toBeUndefined() with comment explaining defaultTransforms
result: pass
method: auto:inspect

### 8. CI workflow — two independent jobs with external PR skip
expected: .github/workflows/e2e.yml has e2e-django (port 3001) and e2e-fastapi (port 3002) as sibling jobs, both skip on external PRs via head.repo.full_name condition
result: pass
method: auto:inspect

### 9. Django compose stack starts healthy
expected: |
  cd apps/django-backend, copy .env.local.example to .env.local (add OPENROUTER_API_KEY),
  run docker compose up -d. Backend (8002), db, redis reach healthy state.
  curl http://localhost:8002/health/ returns 200. SSE stream emits full wire format.
result: pass
method: auto:live-docker
notes: |
  Runs on port 8002 (8000 occupied by stsback-caddy-1). Frontend omitted via compose profile
  (port 3001 also occupied locally). Backend + db + redis all healthy. SSE confirmed:
  text-start → text-delta ("Hi") → text-end → finish with messageId.
  LLM: liquid/lfm-2.5-1.2b-instruct:free via OpenRouter.

### 10. FastAPI compose stack starts healthy + SSE wire format confirmed
expected: |
  cd apps/fastapi-backend, docker compose up -d. Backend (8001) reaches healthy state.
  curl /health returns 200. SSE stream emits full wire format.
result: pass
method: auto:live-docker
notes: |
  Backend healthy on port 8001. Full SSE confirmed:
  text-start → text-delta ("Hi") → text-end → finish with messageId.
  LLM: liquid/lfm-2.5-1.2b-instruct:free via OpenRouter.

## Summary

total: 10
passed: 10
issues: 0
pending: 0
skipped: 0

## Gaps

[none yet]
