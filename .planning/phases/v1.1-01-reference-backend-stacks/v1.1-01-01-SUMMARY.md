---
phase: v1.1-01-reference-backend-stacks
plan: "01"
subsystem: infra
tags: [django, python, sse, langchain, docker, cors, uvicorn, postgres, redis]

# Dependency graph
requires: []
provides:
  - Django 5 async backend in apps/django-backend/ with SSE streaming via StreamingHttpResponse
  - DeepAgents SSE wire format: text-start, text-delta, text-end, finish (with messageId)
  - Docker Compose stack: postgres:16 + redis:7-alpine + backend (8000) + frontend (3001)
  - CORS configured for localhost:3001 and localhost:3002
  - .env.local.example documenting all required environment variables
affects: [v1.1-01-02, v1.1-01-03, v1.1-01-04]

# Tech tracking
tech-stack:
  added:
    - django==5.1.* (async Django project)
    - langchain-ai[deepagents] (create_deep_agent() for SSE streaming)
    - uvicorn[standard] (ASGI server)
    - psycopg[binary] (PostgreSQL adapter)
    - redis (Redis client)
    - django-cors-headers (CORS middleware)
    - python-dotenv (env file loading)
  patterns:
    - Async Django view with StreamingHttpResponse for SSE
    - Manual DATABASE_URL parser using urllib.parse (no dj-database-url dep)
    - CorsMiddleware placed FIRST in MIDDLEWARE list
    - Lazy graph initialization via _get_graph()

key-files:
  created:
    - apps/django-backend/requirements.txt
    - apps/django-backend/manage.py
    - apps/django-backend/deepagents_backend/__init__.py
    - apps/django-backend/deepagents_backend/settings.py
    - apps/django-backend/deepagents_backend/wsgi.py
    - apps/django-backend/deepagents_backend/asgi.py
    - apps/django-backend/deepagents_backend/views.py
    - apps/django-backend/deepagents_backend/urls.py
    - apps/django-backend/Dockerfile
    - apps/django-backend/docker-compose.yml
    - apps/django-backend/.env.local.example
  modified: []

key-decisions:
  - "Manual DATABASE_URL parser using urllib.parse — avoids dj-database-url dependency as specified in plan"
  - "CorsMiddleware first in MIDDLEWARE — required for CORS headers on all requests including preflight OPTIONS"
  - "messageId field intentionally included in finish event — proves defaultTransforms strips it client-side"
  - "Lazy _get_graph() initialization — avoids startup cost on import; graph created per-request"

patterns-established:
  - "SSE generator pattern: yield text-start -> yield text-delta (many) -> yield text-end -> yield finish"
  - "finish event carries messageId to exercise defaultTransforms stripping behavior"
  - "Docker Compose healthcheck chain: db healthy -> redis healthy -> backend healthy -> frontend starts"

requirements-completed: [E2E-01]

# Metrics
duration: 2min
completed: 2026-04-29
---

# Phase v1.1-01 Plan 01: Django Reference Backend Summary

**Async Django 5 SSE backend using create_deep_agent() with StreamingHttpResponse, emitting DeepAgents wire format (text-start/delta/end/finish with messageId), wired into a 4-service Docker Compose stack**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-29T13:44:55Z
- **Completed:** 2026-04-29T13:47:11Z
- **Tasks:** 2
- **Files modified:** 11

## Accomplishments
- Complete Django 5 project scaffold with async ASGI setup, CORS middleware, manual DATABASE_URL parser, and Redis cache configuration
- Async SSE view emitting exact DeepAgents wire format: text-start, text-delta (streamed from LangGraph astream_events), text-end, and finish (with messageId field for defaultTransforms testing)
- Docker Compose stack with 4 services (db, redis, backend, frontend), correct healthcheck chain, and BACKEND_URL=http://backend:8000 wired to frontend container

## Task Commits

Each task was committed atomically:

1. **Task 1: Django project scaffold and requirements** - `a8a908f` (feat)
2. **Task 2: SSE view, URL routing, Dockerfile, and Docker Compose** - `1ca59c4` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified
- `apps/django-backend/requirements.txt` - Python deps: django, langchain-ai[deepagents], uvicorn, psycopg, redis, django-cors-headers, python-dotenv
- `apps/django-backend/manage.py` - Standard Django management script
- `apps/django-backend/deepagents_backend/__init__.py` - Empty package marker
- `apps/django-backend/deepagents_backend/settings.py` - Settings with CORS, manual DB URL parser, Redis cache
- `apps/django-backend/deepagents_backend/asgi.py` - ASGI application entry point
- `apps/django-backend/deepagents_backend/wsgi.py` - WSGI application entry point
- `apps/django-backend/deepagents_backend/views.py` - Async chat_stream (SSE) and health views
- `apps/django-backend/deepagents_backend/urls.py` - Routes for /api/chat/stream/ and /health/
- `apps/django-backend/Dockerfile` - python:3.12-slim, uvicorn CMD
- `apps/django-backend/docker-compose.yml` - 4-service stack with healthchecks
- `apps/django-backend/.env.local.example` - Template for ANTHROPIC_API_KEY, DJANGO_SECRET_KEY, DATABASE_URL, REDIS_URL, BACKEND_URL

## Decisions Made
- Manual DATABASE_URL parser using `urllib.parse` — avoids dj-database-url dependency as specified in plan
- `messageId` field intentionally present in finish event to prove defaultTransforms strips it client-side
- Lazy `_get_graph()` creates graph per-call rather than at module import time

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required beyond copying `.env.local.example` to `.env.local` and filling in `ANTHROPIC_API_KEY`.

## Next Phase Readiness
- Django reference backend complete; ready for v1.1-01-02 (additional reference backends or integration testing)
- `docker compose up` in `apps/django-backend/` will reach healthy state once `.env.local` is populated with a real `ANTHROPIC_API_KEY`

---
*Phase: v1.1-01-reference-backend-stacks*
*Completed: 2026-04-29*

## Self-Check: PASSED

- All 11 files verified on disk
- Commit a8a908f: feat(v1.1-01-01): Django project scaffold and requirements - FOUND
- Commit 1ca59c4: feat(v1.1-01-01): SSE view, URL routing, Dockerfile, and Docker Compose - FOUND
