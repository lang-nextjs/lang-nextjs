---
phase: v1.5-05-mcp-tools
plan: 01
subsystem: MCP
tags: [tdd, red, testing, mcp-tools]
dependency_graph:
  requires: [MCP-01, MCP-02, MCP-03, MCP-04]
  provides: [MCP-tools-RED-stubs]
  affects: [v1.5-05-02, v1.5-05-03, v1.5-05-04]
tech_stack:
  added: []
  patterns: [TDD-RED, vitest mocking, MCP tool handler testing]
key_files:
  created: []
  modified:
    - packages/mcp/src/index.test.ts
decisions:
  - Task 1: Appended 4 new describe blocks (21 tests total) covering trigger_task, list_runs, get_run_status, cancel_run
metrics:
  duration: ~2 minutes
  tasks_completed: 1/1
  completed_date: 2026-05-05T08:53:43Z
---

# Phase v1.5-05 Plan 01: RED Test Stubs for MCP Tools Summary

Red test stubs for all 4 open-swe MCP tools (trigger_task, list_runs, get_run_status, cancel_run) appended to packages/mcp/src/index.test.ts. Tests intentionally fail (RED) because tools are not yet implemented in index.ts — Wave 0 TDD phase per MCP-01 through MCP-04 requirements.

## Task 1: Add 21 RED test stubs for all 4 open-swe MCP tools

**Status:** COMPLETE

Appended 4 new describe blocks to the end of packages/mcp/src/index.test.ts with 21 total test cases:

- **trigger_task tool** (6 tests):
  - Returns run_id immediately after POST to /api/open-swe/runs
  - Returns isError:true when task is empty string
  - Returns isError:true when task is whitespace only
  - Throws when platform returns 502
  - Does not include double slash when apiUrl has trailing slash
  - Returns isError:true on AbortError (timeout)

- **list_runs tool** (3 tests):
  - Returns structured list of runs with statuses
  - Returns empty array when no runs exist
  - Throws when platform returns 502

- **get_run_status tool** (6 tests):
  - Returns status and latest output snapshot for a valid runId
  - Returns isError:true when runId is empty string
  - Returns isError:true when runId is whitespace only
  - Encodes runId in URL to prevent path injection
  - Throws when platform returns 502
  - Returns isError:true on AbortError (timeout)

- **cancel_run tool** (6 tests):
  - Returns updated run with cancelled/failed status after POST to /cancel
  - Returns isError:true when runId is empty string
  - Returns isError:true when runId is whitespace only
  - POSTs to /api/open-swe/runs/{runId}/cancel endpoint
  - Throws when platform returns 502
  - Returns isError:true on AbortError (timeout)

**Verification:**
- `grep -c "trigger_task" packages/mcp/src/index.ts` returns 0 (tool not yet implemented)
- `pnpm --filter @deepagents-nextjs/mcp test -- --run` exits with non-zero (21 tests RED, 55 tests GREEN)
- Error message: "Cannot read properties of undefined (reading 'handler')" confirms tools don't exist yet
- All 4 describe blocks present
- No existing tests were modified

**Commit:** a90c32f

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- ✓ 21 new test cases in index.test.ts
- ✓ All 4 describe blocks appended correctly
- ✓ Test suite exits non-zero (RED)
- ✓ trigger_task, list_runs, get_run_status, cancel_run tool handlers reference non-existent tools
- ✓ Commit a90c32f verified in git log
- ✓ File /Users/jonathanborduas/code/deepagents-nextjs/packages/mcp/src/index.test.ts modified

