---
phase: 19-graceful-shutdown-deploy-docs
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - docs/DEPLOYMENT-RUNBOOK.md
  - docs/GRACEFUL-SHUTDOWN.md
  - apps/open-swe/vercel.json
  - .github/workflows/smoke-test-staging.yml
autonomous: true
requirements: [OPS-03, OPS-04]
formal_artifacts: none

must_haves:
  truths:
    - "A deployment runbook documents canary AND blue-green rollout procedures with concrete steps"
    - "The runbook documents Kubernetes liveness + readiness probe wiring (YAML snippet) tied to createHealthProbe/createReadinessProbe, including preStop/SIGTERM ordering"
    - "The runbook documents graceful-shutdown serverless limitations: Vercel ~500ms window and Cloudflare no-SIGTERM, with client-side resilience recommendation"
    - "The graceful-shutdown API (createGracefulShutdown) is documented with a working consumer integration example wiring isDraining() into createReadinessProbe"
    - "Phase 17 canary/blue-green infra (vercel.json + smoke-test-staging.yml) is formalized as a health-gated rollout that references the Phase 18 readiness probe"
  artifacts:
    - path: "docs/DEPLOYMENT-RUNBOOK.md"
      provides: "Canary/blue-green rollout, K8s probe wiring, serverless shutdown limits, health-gated rollout section"
      min_lines: 90
      contains: "Canary"
    - path: "docs/GRACEFUL-SHUTDOWN.md"
      provides: "createGracefulShutdown API + consumer integration example + platform constraints"
      min_lines: 50
      contains: "createGracefulShutdown"
    - path: "apps/open-swe/vercel.json"
      provides: "Formalized health-gated rollout config (traffic split + health-driven rollback), commented/aligned to readiness probe"
      contains: "rollback"
    - path: ".github/workflows/smoke-test-staging.yml"
      provides: "Health-gate step asserting readiness (/ready or /health draining behavior) as the rollout gate"
      contains: "readiness"
  key_links:
    - from: "docs/DEPLOYMENT-RUNBOOK.md"
      to: "packages/server/src/health.ts createReadinessProbe"
      via: "documented readiness-probe wiring + health-gated rollout reference"
      pattern: "createReadinessProbe"
    - from: "docs/GRACEFUL-SHUTDOWN.md"
      to: "packages/server/src/shutdown.ts createGracefulShutdown"
      via: "documented API + integration example matching the shipped signature"
      pattern: "createGracefulShutdown"
    - from: ".github/workflows/smoke-test-staging.yml"
      to: "readiness probe endpoint"
      via: "health-gate curl step that gates the rollout"
      pattern: "readiness|/ready"
---

<objective>
Document the deploy story and formalize the rollout as health-gated.
Write `docs/DEPLOYMENT-RUNBOOK.md` (canary + blue-green procedures, Kubernetes
liveness/readiness wiring with preStop+SIGTERM ordering, and serverless
graceful-shutdown limits: Vercel ~500ms, Cloudflare no-SIGTERM). Write
`docs/GRACEFUL-SHUTDOWN.md` documenting the `createGracefulShutdown` API shipped
in 19-01 with a consumer integration example. Formalize the Phase 17 canary
infra (vercel.json + smoke-test-staging.yml) as a health-gated rollout that
references the Phase 18 readiness probe.

Purpose: Closes OPS-03 (runbook + serverless limits) and OPS-04 (Phase 17 canary
formalized as health-gated rollout).
Output: two docs + light config edits wiring the smoke test/vercel config to the
readiness probe as the rollout gate.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-graceful-shutdown-deploy-docs/19-RESEARCH.md

# The shutdown API to document (shipped by 19-01) — match the real signature
@.planning/phases/19-graceful-shutdown-deploy-docs/19-01-SUMMARY.md
# Readiness probe the runbook + health-gate reference
@packages/server/src/health.ts
# Existing canary infra to formalize (OPS-04)
@apps/open-swe/vercel.json
@.github/workflows/smoke-test-staging.yml
# Doc format reference
@docs/TESTING.md
</context>

<constraints>
- Docs MUST match the ACTUAL createGracefulShutdown signature shipped in 19-01 — read 19-01-SUMMARY.md (and packages/server/src/shutdown.ts) first; do NOT invent options that don't exist.
- Zero new runtime dependencies. Config edits are light: vercel.json comments/alignment + one health-gate step in the existing workflow. Do NOT rewrite the workflow wholesale; add a gating step and keep the manual-trigger note intact.
- The smoke-test workflow's Vercel secrets are NOT provisioned (per its header comment). Do NOT enable pull_request triggers. The health-gate step must degrade gracefully (skip/clear message) if BASE_URL is empty, consistent with the existing manual-only posture.
- Serverless limits are HARD constraints — state Vercel ~500ms window and Cloudflare no-SIGTERM explicitly, with client-side reconnection as the recommended mitigation (graceful shutdown is best-effort/N-A there, NOT guaranteed).
- Runbook K8s section MUST show preStop hook BEFORE SIGTERM-driven drain and explain readiness flips to 503 first so the LB stops routing (RESEARCH Pitfall 1).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/GRACEFUL-SHUTDOWN.md (OPS-03 — shutdown API + serverless limits)</name>
  <files>docs/GRACEFUL-SHUTDOWN.md</files>
  <action>
    First read .planning/phases/19-graceful-shutdown-deploy-docs/19-01-SUMMARY.md
    and packages/server/src/shutdown.ts to get the EXACT exported API. Then write
    a doc with these sections (use grep-able H2 headings):
    - "## Overview" — Node-only, opt-in, drains in-flight SSE streams on SIGTERM.
    - "## API" — createGracefulShutdown(config) signature and the returned handle
      (isDraining, trackStream, releaseStream, dispose, installSignalHandlers) —
      copied from the shipped types, not invented.
    - "## Integration" — a complete TS example: instantiate shutdown, pass
      `isDraining: () => shutdown.isDraining()` into createReadinessProbe, wire
      trackStream/releaseStream via the handler observability hooks
      (onRequest/onStreamEnd), and install signal handlers ONCE at startup
      (process.once / installSignalHandlers — note MaxListeners pitfall).
    - "## Configuring the drain timeout" — drainTimeoutMs should be >= max request
      duration + margin; safety timeout guarantees exit.
    - "## Serverless limitations" — Vercel ~500ms window (streams truncated;
      best-effort only) and Cloudflare Workers (no SIGTERM; N/A) with
      client-side reconnection recommendation.
  </action>
  <verify>test -f docs/GRACEFUL-SHUTDOWN.md && grep -q "createGracefulShutdown" docs/GRACEFUL-SHUTDOWN.md && grep -qi "500ms" docs/GRACEFUL-SHUTDOWN.md && grep -qi "Cloudflare" docs/GRACEFUL-SHUTDOWN.md && grep -q "createReadinessProbe" docs/GRACEFUL-SHUTDOWN.md && echo OK</verify>
  <done>Doc exists (>=50 lines), documents the real createGracefulShutdown API, shows the createReadinessProbe wiring example, and states Vercel 500ms + Cloudflare no-SIGTERM limits with client-resilience recommendation.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/DEPLOYMENT-RUNBOOK.md (OPS-03 — canary/blue-green + K8s + serverless)</name>
  <files>docs/DEPLOYMENT-RUNBOOK.md</files>
  <action>
    Write the runbook with grep-able H2 headings:
    - "## Canary Rollout" — step-by-step: deploy green alongside blue, route a small
      traffic % (reference vercel.json split), watch health/metrics, promote or
      rollback. Include a verification command pattern (send N requests, check
      version header / error rate).
    - "## Blue-Green Rollout" — stand up green fully, health-gate, flip traffic,
      keep blue warm for instant rollback.
    - "## Kubernetes Probe Wiring" — YAML snippet for a Deployment with
      livenessProbe + readinessProbe (httpGet to the liveness/readiness routes
      backed by createHealthProbe/createReadinessProbe), a preStop hook + the
      terminationGracePeriodSeconds aligned with drainTimeoutMs, and a note that
      on SIGTERM readiness flips to 503 FIRST so the LB stops routing before drain
      (RESEARCH Pitfall 1). Reference docs/GRACEFUL-SHUTDOWN.md.
    - "## Serverless Graceful-Shutdown Limits" — Vercel ~500ms window, Cloudflare
      no-SIGTERM; graceful shutdown is best-effort/N-A; recommend client reconnection.
    - "## Health-Gated Rollout" (OPS-04) — explain that the canary/blue-green
      promotion is GATED on the Phase 18 readiness probe: the smoke-test workflow
      asserts readiness before promoting, and vercel.json health-driven rollback
      removes a bad release. Reference createReadinessProbe and the smoke-test
      workflow + vercel.json.
  </action>
  <verify>test -f docs/DEPLOYMENT-RUNBOOK.md && grep -qi "Canary" docs/DEPLOYMENT-RUNBOOK.md && grep -qi "Blue-Green" docs/DEPLOYMENT-RUNBOOK.md && grep -qi "readinessProbe" docs/DEPLOYMENT-RUNBOOK.md && grep -qi "preStop" docs/DEPLOYMENT-RUNBOOK.md && grep -qi "500ms" docs/DEPLOYMENT-RUNBOOK.md && grep -qi "Cloudflare" docs/DEPLOYMENT-RUNBOOK.md && grep -q "createReadinessProbe" docs/DEPLOYMENT-RUNBOOK.md && grep -qi "health-gated" docs/DEPLOYMENT-RUNBOOK.md && echo OK</verify>
  <done>Runbook exists (>=90 lines) covering canary + blue-green, K8s liveness/readiness + preStop/SIGTERM ordering, Vercel 500ms + Cloudflare no-SIGTERM limits, and a Health-Gated Rollout section referencing createReadinessProbe.</done>
</task>

<task type="auto">
  <name>Task 3: Formalize Phase 17 canary as health-gated rollout (OPS-04 — config + workflow)</name>
  <files>apps/open-swe/vercel.json, .github/workflows/smoke-test-staging.yml</files>
  <action>
    vercel.json: keep the existing traffic split + automatic rollback. Since
    vercel.json does not support comments, do NOT add JS comments; instead ensure
    the config expresses the health-gated intent (traffic split present, rollback
    {automatic:true,errorThreshold} present) and leave the explanatory prose to the
    runbook. If a "/ready" health path entry is appropriate alongside "/health" in
    the split, add it; otherwise leave split as-is. Do not break JSON validity.

    smoke-test-staging.yml: add a "Readiness gate" step to the smoke-test-staging
    job that curls the readiness endpoint (e.g. "$BASE_URL/ready" or the app's
    readiness route) and asserts 200 (ready) — this is the health-gate that
    formalizes promotion. The step MUST: skip with a clear message if BASE_URL is
    empty (consistent with the manual-only, unprovisioned-secrets posture in the
    file header); on a non-200 readiness response, fail the gate. Keep the
    workflow manual-trigger only (do NOT add pull_request). Add a brief comment
    line near the new step pointing to docs/DEPLOYMENT-RUNBOOK.md health-gated
    rollout section.
  </action>
  <verify>python3 -c "import json;json.load(open('apps/open-swe/vercel.json'))" && grep -qi "readiness" .github/workflows/smoke-test-staging.yml && grep -q "workflow_dispatch" .github/workflows/smoke-test-staging.yml && ! grep -q "pull_request" .github/workflows/smoke-test-staging.yml && echo OK</verify>
  <done>vercel.json still valid JSON with traffic split + health-driven rollback; smoke-test workflow has a readiness health-gate step that skips cleanly when BASE_URL is empty and fails on non-200; workflow remains manual-trigger only; references the runbook.</done>
</task>

</tasks>

<verification>
- docs/GRACEFUL-SHUTDOWN.md documents the shipped createGracefulShutdown API and serverless limits.
- docs/DEPLOYMENT-RUNBOOK.md covers canary, blue-green, K8s probe wiring (preStop/SIGTERM ordering), serverless limits, and a health-gated rollout section referencing createReadinessProbe.
- apps/open-swe/vercel.json remains valid JSON expressing the health-gated rollout (traffic split + automatic health-driven rollback).
- .github/workflows/smoke-test-staging.yml gains a readiness health-gate step, stays manual-trigger only, and degrades gracefully without provisioned secrets.
</verification>

<success_criteria>
OPS-03 satisfied: a deployment runbook documents canary/blue-green rollout, K8s
liveness/readiness wiring, and serverless graceful-shutdown limits (Vercel
~500ms, Cloudflare no-SIGTERM). OPS-04 satisfied: the Phase 17 canary/blue-green
infra is formalized as a health-gated rollout referencing the Phase 18 readiness
probe (runbook section + smoke-test readiness gate + vercel.json rollback).
</success_criteria>

<output>
After completion, create `.planning/phases/19-graceful-shutdown-deploy-docs/19-02-SUMMARY.md`
</output>
