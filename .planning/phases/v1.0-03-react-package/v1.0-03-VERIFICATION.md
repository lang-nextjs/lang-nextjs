---
phase: v1.0-03
status: approved
verified_by: nf-verifier
---

# Phase v1.0-03 Verification

## Requirement Checks

| Req | Status | Evidence |
|-----|--------|----------|
| RCT-01 | PASS | `hook.ts` exports `useDeepAgentsChat({ sessionId, endpoint, getToken? })` returning `{ messages, sendMessage, status, error }`. Transport wired via `DefaultChatTransport`, `getToken` → `Authorization: Bearer` header. 12/12 hook tests pass. |
| RCT-02 | PASS | `types.ts` defines `UserMessage \| AIMessage \| ToolCallMessage \| ErrorMessage` with `type` as discriminant. `converter.ts` maps UIMessage.parts[] → typed Message[]. `assertNever()` provides exhaustive check. |
| RCT-03 | PASS | `schemas.ts` exports PlanSchema, TaskSchema, FileSchema, ApprovalSchema, DataErrorSchema, parseDataPart(). All Zod objects (`.parse` verified). 17/17 schema tests pass. |
| RCT-04 | PASS | `pnpm exec publint` → "All good!". `tsup.config.ts` has `external: ['react','react-dom','ai','@ai-sdk/react','zod']` preventing peerDep bundling. |
| RCT-05 | PASS | `npx vitest run` → PASS (64) FAIL (0). Breakdown: types 7, schemas 17, converter 20, hook 12, index 8. `tsc --noEmit` exits 0. |

## Dist Artifacts

- [x] dist/index.js (CJS, 9.7 KB)
- [x] dist/index.mjs (ESM, 9.2 KB)
- [x] dist/index.d.ts (7.0 KB)
- [x] dist/index.d.mts (7.0 KB)

## Barrel Export Coverage

`index.ts` exports:
- `useDeepAgentsChat`, `UseDeepAgentsChatOptions`, `UseDeepAgentsChatReturn`
- `Message`, `UserMessage`, `AIMessage`, `ToolCallMessage`, `ErrorMessage`
- `generateId`, `assertNever`
- `PlanSchema`, `TaskSchema`, `FileSchema`, `ApprovalSchema`, `DataErrorSchema`, `PlanSubtaskSchema`
- `parseDataPart` + result types
- `partsToMessages`

## Verdict

APPROVED — all requirements met. Phase v1.0-03 is complete.

`@deepagents-nextjs/react` is structurally publish-ready with dual ESM/CJS build,
clean publint output, and 64/64 tests passing.
