---
plan_id: v1.0-03-03
status: complete
wave: 2
---

# Wave 2 — Types, Schemas, Converter, Hook (SUMMARY)

## Outcome

All 5 implementation files created and passing. Full vitest run: PASS (64) FAIL (0).

## Files Created

- `packages/react/src/types.ts` — 4-type Message union (UserMessage, AIMessage, ToolCallMessage, ErrorMessage) + generateId/assertNever
- `packages/react/src/schemas.ts` — 5 Zod schemas (PlanSchema, TaskSchema, FileSchema, ApprovalSchema, DataErrorSchema) + parseDataPart() via SCHEMA_MAP
- `packages/react/src/converter.ts` — partsToMessages() single-pass converter; handles text, dynamic-tool, tool-*, data-error, drops other data-* with console.warn
- `packages/react/src/hook.ts` — useDeepAgentsChat() wrapping @ai-sdk/react useChat + DefaultChatTransport; 'use client' directive; getToken → Authorization: Bearer header
- `packages/react/src/index.ts` — Complete barrel export (replaced VERSION skeleton)

## Test Files Completed

- `packages/react/src/types.test.ts` — 7 tests
- `packages/react/src/schemas.test.ts` — 17 tests
- `packages/react/src/converter.test.ts` — 20 tests
- `packages/react/src/hook.test.ts` — 12 tests
- `packages/react/src/index.test.ts` — 8 tests

## Key Fixes

1. **converter.ts edge case**: outStartIdx tracking prevents empty AIMessage bubble when message contains only data-error parts
2. **TypeScript UIMessage cast**: `as unknown as UIMessage` in test helpers (UIMessagePart is strict discriminated union)
3. **headers return type**: explicit `async (): Promise<Record<string, string>>` annotation in hook.ts
4. **vi.mock hoisting**: factory functions use inline `vi.fn()` only; mocked modules imported after vi.mock calls
