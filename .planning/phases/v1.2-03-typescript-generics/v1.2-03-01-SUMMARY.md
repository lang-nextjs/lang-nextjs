---
phase: v1.2-03
plan: "01"
subsystem: react
tags: [zod, generics, converter, typescript, partsToMessages, customSchemaMap]

# Dependency graph
requires:
  - phase: v1.2-02
    provides: test-utils and debug tooling used in test infrastructure
provides:
  - partsToMessages extended with optional customSchemaMap third parameter
  - Custom data-* part runtime parsing via ZodTypeAny schemas
  - customSchemaMap['data-error'] override of built-in DataErrorSchema
affects: [v1.2-03-02, useDeepAgentsChat TData generic hook]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "customSchemaMap?: Record<string, ZodTypeAny> — optional third param pattern for runtime schema injection"
    - "as unknown as Message cast — intentional widening for generic data-* parts; hook's TData narrows the type"
    - "Custom wins over built-in: customSchemaMap['data-error'] takes precedence over DataErrorSchema"

key-files:
  created: []
  modified:
    - packages/react/src/converter.ts

key-decisions:
  - "customSchemaMap wins over built-in DataErrorSchema when 'data-error' key is present — caller has explicit intent"
  - "Return type stays Message[]; the as unknown as Message cast is intentional — hook's TData generic provides narrowing at call site"
  - "Fail-open on custom schema parse failure: console.warn + drop, same policy as data-error"
  - "RED tests were already committed (d3b9ae7) as part of v1.2-03 hardening — Task 1 RED phase was pre-existing"

patterns-established:
  - "customSchemaMap third parameter: inject at call site, not module-level — avoids global mutation"
  - "Custom data-* entry shape: { type: partType, data: parsed.data } — consistent with data-plan/data-task shape convention"

requirements-completed: [DX-03]

# Metrics
duration: 5min
completed: 2026-05-01
---

# Phase v1.2-03 Plan 01: customSchemaMap in partsToMessages Summary

**partsToMessages extended with optional `customSchemaMap?: Record<string, ZodTypeAny>` third parameter enabling runtime custom data-* part parsing via injected Zod schemas**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-01T10:13:53Z
- **Completed:** 2026-05-01T10:15:30Z
- **Tasks:** 2 (TDD RED already committed; GREEN implemented)
- **Files modified:** 1

## Accomplishments

- Extended `partsToMessages` signature with `customSchemaMap?: Record<string, ZodTypeAny>` third parameter
- Custom data-* parts are parsed via `customSchema.safeParse(envelope.data)` and pushed as `{ type, data }` — fail-open on parse failure
- `customSchemaMap['data-error']` overrides the built-in `DataErrorSchema` when present — caller intent is explicit
- All 45 converter tests pass; 101 total react package tests pass; TypeScript compiles clean
- Zero regressions against existing v1.1 behaviour when `customSchemaMap` is undefined

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — failing tests for customSchemaMap** - `d3b9ae7` (test) — pre-existing from v1.2-03 hardening commit
2. **Task 2: GREEN — partsToMessages customSchemaMap implementation** - `66df08c` (feat)

**Plan metadata:** (this SUMMARY commit)

_Note: TDD RED phase was pre-committed as part of adversarial hardening (d3b9ae7). GREEN executed and committed here._

## Files Created/Modified

- `packages/react/src/converter.ts` — Added `ZodTypeAny` import; extended `partsToMessages` signature; replaced `data-*` branch with customSchemaMap-aware logic (custom wins for `data-error`; fallback to built-in; warn+drop for unknown)

## Decisions Made

- **customSchemaMap wins over built-in for `data-error`**: when caller passes `customSchemaMap['data-error']`, it signals explicit intent to override — custom schema takes precedence over `DataErrorSchema`
- **Return type stays `Message[]`**: the `as unknown as Message` cast is intentional; type narrowing belongs in the hook's `TData` generic (Plan 02), not in the converter
- **Fail-open on schema failure**: matches existing `data-error` policy — parse failure → `console.warn` + drop, never throws

## Deviations from Plan

None — plan executed exactly as written. RED tests were already pre-committed from the prior hardening pass; GREEN implemented the minimal converter change to make all tests pass.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `partsToMessages` runtime half of DX-03 is complete
- Ready for Plan 02: `useDeepAgentsChat<TData>` generic hook wiring — the `TData` generic will narrow the `{ type: string; data: unknown }` entries returned when `customSchemaMap` is provided
- No blockers

## Self-Check: PASSED

- FOUND: packages/react/src/converter.ts
- FOUND: .planning/phases/v1.2-03-typescript-generics/v1.2-03-01-SUMMARY.md
- FOUND commit: 66df08c (GREEN — feat)
- FOUND commit: d3b9ae7 (RED — test, pre-existing)

---
*Phase: v1.2-03-typescript-generics*
*Completed: 2026-05-01*
