---
phase: 21-blazing-api-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jonathanborduas/code/blazing/src/blazing_service/workspace/rest_api.py
  - /Users/jonathanborduas/code/blazing/src/blazing_service/server.py
  - /Users/jonathanborduas/code/blazing/tests/test_workspace_rest_api.py
autonomous: true
requirements:
  - BLZ-01

must_haves:
  truths:
    - "PR #81 is merged into blazing master branch"
    - "The workspace REST API code (rest_api.py, server.py router mounting) exists on master"
    - "All 56+ workspace REST API tests pass against the merged master branch"
  artifacts:
    - path: "/Users/jonathanborduas/code/blazing/src/blazing_service/workspace/rest_api.py"
      provides: "7-endpoint workspace REST API (create, get, delete, exec, list, capacity, health)"
      contains: "workspace_router"
    - path: "/Users/jonathanborduas/code/blazing/src/blazing_service/server.py"
      provides: "Router registration with WORKSPACE_API_ENABLED kill switch"
      contains: "include_router(workspace_router)"
    - path: "/Users/jonathanborduas/code/blazing/tests/test_workspace_rest_api.py"
      provides: "56+ tests covering all 7 endpoints and edge cases"
      contains: "test_"
  key_links:
    - from: "src/blazing_service/server.py"
      to: "src/blazing_service/workspace/rest_api.py"
      via: "include_router(workspace_router) + include_router(workspaces_router)"
      pattern: "include_router.*workspace_router"
  consumers:
    - artifact: "rest_api.py on master"
      consumed_by: "Phase 22 TypeScript Adapter Contract"
      integration: "Adapter code reads the real contract from this merged code"
      verify_pattern: "git log --oneline master | grep 'issue-48'"
---

<objective>
Merge PR #81 into the blazing master branch and verify all workspace REST API tests pass.

Purpose: The workspace REST API is a hard dependency for the TypeScript adapter work in Phases 22-25. Until PR #81 is merged, the adapter cannot be tested against the real contract. CI is billing-blocked so an admin-merge is required.

Output: Merged master branch with all workspace REST API code and passing tests.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-blazing-api-merge/21-RESEARCH.md

The blazing repo is at /Users/jonathanborduas/code/blazing.
PR #81 is on branch `feature/issue-48-workspace-rest`, targeting `master`.
PR state: OPEN, mergeable, BLOCKED by CI billing.
The PR adds 7 endpoints (POST /v1/workspace, GET /v1/workspace/{id}, DELETE /v1/workspace/{id}, POST /v1/workspace/{id}/exec, GET /v1/workspaces, GET /v1/workspaces/capacity, GET /v1/health), 62 test functions, and a kill switch via WORKSPACE_API_ENABLED env var.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin-merge PR #81 into blazing master</name>
  <files>
    /Users/jonathanborduas/code/blazing/src/blazing_service/workspace/rest_api.py
    /Users/jonathanborduas/code/blazing/src/blazing_service/server.py
    /Users/jonathanborduas/code/blazing/tests/test_workspace_rest_api.py
  </files>
  <action>
    Working directory: /Users/jonathanborduas/code/blazing

    1. Verify PR #81 is still OPEN and MERGEABLE:
       ```
       gh pr view 81 --json state,mergeable,headRefName
       ```
       Confirm state is "OPEN" and mergeable is "MERGEABLE".

    2. Admin-merge with squash (CI is billing-blocked, skip checks):
       ```
       gh pr merge 81 --squash --admin --delete-branch
       ```
       This squashes all commits into a single merge commit and deletes the feature branch.

    3. Pull the updated master:
       ```
       git checkout master
       git pull origin master
       ```

    4. Verify the merge landed — confirm the key files exist on master:
       ```
       ls src/blazing_service/workspace/rest_api.py
       grep "include_router(workspace_router)" src/blazing_service/server.py
       grep "include_router(workspaces_router)" src/blazing_service/server.py
       ```

    5. Commit the merge confirmation (if any local state needs tracking). No code changes — this is purely a git operation in the blazing repo.

    IMPORTANT: This task operates in /Users/jonathanborduas/code/blazing (the blazing repo), NOT in the lang-nextjs repo.
  </action>
  <verify>
    cd /Users/jonathanborduas/code/blazing && git log --oneline master -5 | grep -i "issue-48\|workspace.*rest\|#48"
    echo "---"
    test -f src/blazing_service/workspace/rest_api.py && echo "rest_api.py exists" || echo "MISSING rest_api.py"
    grep -c "include_router(workspace_router)" src/blazing_service/server.py
  </verify>
  <done>
    - PR #81 is merged (state: MERGED or CLOSED with merged_at set)
    - master branch contains rest_api.py with 7 endpoints
    - server.py includes both workspace_router and workspaces_router
    - Feature branch `feature/issue-48-workspace-rest` is deleted
  </done>
</task>

<task type="auto">
  <name>Task 2: Run workspace REST API test suite against merged master</name>
  <files>
    /Users/jonathanborduas/code/blazing/tests/test_workspace_rest_api.py
  </files>
  <action>
    Working directory: /Users/jonathanborduas/code/blazing

    1. Run the workspace REST API test suite against the merged master:
       ```
       python -m pytest tests/test_workspace_rest_api.py -v --tb=short 2>&1
       ```

    2. Count the tests and verify we have at least 56 passing:
       The output should show "62 passed" (the test file has 62 test functions including adversarial hardening iterations).

    3. If any tests fail, diagnose:
       - Check if failures are due to missing Redis or Docker dependencies (unit tests should not need these — they mock the service)
       - Check if import paths broke during squash merge
       - Report failures with full traceback for triage

    4. Also run a quick import check to confirm no syntax or import errors:
       ```
       python -c "from blazing_service.workspace.rest_api import workspace_router, workspaces_router; print(f'Routes: {len(workspace_router.routes) + len(workspaces_router.routes)}')"
       ```

    IMPORTANT: If tests fail due to environment issues (missing Redis, Docker), document what's needed. The test file uses AsyncClient with mocked dependencies so it should not need external services.
  </action>
  <verify>
    cd /Users/jonathanborduas/code/blazing && python -m pytest tests/test_workspace_rest_api.py --co -q 2>&1 | tail -3
  </verify>
  <done>
    - All 62 workspace REST API tests pass (0 failures)
    - No import errors in rest_api.py
    - Both workspace_router and workspaces_router are importable
    - Test output captured and confirmed passing
  </done>
</task>

</tasks>

<verification>
1. `gh pr view 81 --json state` shows state is MERGED (or CLOSED with merged_at)
2. `git log --oneline master` shows the squash merge commit
3. `test -f src/blazing_service/workspace/rest_api.py` passes
4. `python -m pytest tests/test_workspace_rest_api.py -v` shows 0 failures
5. `python -c "from blazing_service.workspace.rest_api import workspace_router"` succeeds
</verification>

<success_criteria>
PR #81 is merged into blazing master. All workspace REST API tests pass on the merged master branch. The 7-endpoint API code is available for the TypeScript adapter in Phase 22.
</success_criteria>

<output>
After completion, create `.planning/phases/21-blazing-api-merge/21-01-SUMMARY.md`
</output>
