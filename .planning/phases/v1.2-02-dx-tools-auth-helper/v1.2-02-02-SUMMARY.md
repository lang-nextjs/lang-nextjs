---
phase: v1.2-02
plan: "02"
subsystem: test-utils
tags: [tdd, test-utils, mock-server, dx, ai-sdk-v6]
dependency_graph:
  requires: []
  provides: [createMockDeepAgentsServer, "@deepagents-nextjs/test-utils"]
  affects: [consumer test suites]
tech_stack:
  added: ["@deepagents-nextjs/test-utils workspace package", "MockLanguageModelV3 from ai/test", "simulateReadableStream from ai"]
  patterns: ["TDD red-green", "tsup dual ESM/CJS build", "per-package vitest config"]
key_files:
  created:
    - packages/test-utils/package.json
    - packages/test-utils/tsconfig.json
    - packages/test-utils/tsup.config.ts
    - packages/test-utils/vitest.config.ts
    - packages/test-utils/src/index.ts
    - packages/test-utils/src/mock-server.ts
    - packages/test-utils/src/mock-server.test.ts
  modified:
    - pnpm-lock.yaml
decisions:
  - "AI SDK v6 toUIMessageStreamResponse() emits {\"type\":\"finish\"} without finishReason in envelope — test checks type field not finishReason"
  - "MockLanguageModelV3 requires doStream cast as any — matches existing route.mock.ts pattern"
  - "chunkDelayMs defaults to 50ms to match route.mock.ts; set to 0 for fast test suites"
metrics:
  duration: "3.5 min"
  completed: "2026-05-01"
  tasks_completed: 3
  files_created: 7
  tests_added: 7
  total_tests_passing: 140
---

# Phase v1.2-02 Plan 02: @deepagents-nextjs/test-utils Package Summary

**One-liner:** New `@deepagents-nextjs/test-utils` workspace package with `createMockDeepAgentsServer()` factory using AI SDK v6 `MockLanguageModelV3` + `simulateReadableStream`, dual ESM/CJS build, 7 tests passing, publint + attw all green.

## What Was Built

`createMockDeepAgentsServer()` extracted from `apps/example/route.mock.ts` into a standalone workspace package. Consumers can import from `@deepagents-nextjs/test-utils` and get a ready-made `Response` with an AI SDK v6 UIMessageStream body — the same format `useDeepAgentsChat()` consumes — without any real backend.

## Tasks Completed

| Task | Description | Commit | Files |
|------|-------------|--------|-------|
| 1 (RED) | Scaffold package + stub implementation + failing tests | 488bdca | 8 files |
| 2 (GREEN) | Full mock-server implementation — all 7 tests pass | 3d3f3fc | 2 files |
| 3 (Build+Validate) | Build CJS+ESM+DTS, publint + attw all green | (in GREEN commit) | — |

## Test Results

- `@deepagents-nextjs/test-utils`: 7/7 tests pass
- `@deepagents-nextjs/server`: 69/69 tests pass (no regressions)
- `@deepagents-nextjs/react`: 64/64 tests pass (no regressions)
- Total: 140 tests pass

## Success Criteria Verification

- [x] `@deepagents-nextjs/test-utils` workspace package registered (appears in `pnpm list -r`)
- [x] `createMockDeepAgentsServer()` returns a `Response` with status 200 and `x-vercel-ai-ui-message-stream: v1` header
- [x] Response body is a readable stream containing at least one `text-delta` frame and a `finish` frame
- [x] `createMockDeepAgentsServer({ chunkDelayMs: 0 })` returns immediately for fast test suites
- [x] Two sequential calls return independent Response objects (no shared state)
- [x] `createMockDeepAgentsServer` exported from `@deepagents-nextjs/test-utils` public API
- [x] All 7 tests pass
- [x] `pnpm --filter @deepagents-nextjs/test-utils validate` passes (publint + attw)
- [x] Full turbo test run shows no regressions (140 tests pass)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed test expectation for finish frame format**
- **Found during:** Task 2 (GREEN)
- **Issue:** Test expected `finishReason` in the stream body, but AI SDK v6 `toUIMessageStreamResponse()` emits `{"type":"finish"}` in the UIMessageStream envelope — `finishReason` is internal to the model stream layer and not forwarded to the UI stream data frames
- **Fix:** Changed test assertion from `toContain('finishReason')` to `toContain('"type":"finish"')` — accurately tests what the UI stream actually delivers
- **Files modified:** `packages/test-utils/src/mock-server.test.ts`
- **Commit:** 3d3f3fc

## Self-Check: PASSED

- packages/test-utils/src/mock-server.ts: FOUND
- packages/test-utils/src/index.ts: FOUND
- packages/test-utils/src/mock-server.test.ts: FOUND
- packages/test-utils/package.json: FOUND
- Commit 488bdca: FOUND
- Commit 3d3f3fc: FOUND
