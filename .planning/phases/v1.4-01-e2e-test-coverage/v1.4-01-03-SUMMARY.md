---
phase: v1.4-01
plan: "03"
subsystem: testing
tags: [playwright, e2e, reconnect, retry, ci, github-actions]

# Dependency graph
requires:
  - phase: v1.4-01-01
    provides: nextjs.spec.ts E2E-01 through E2E-04
  - phase: v1.4-01-02
    provides: nextjs-extra.spec.ts E2E-07, E2E-08, E2E-10 + remix.spec.ts E2E-05 + sveltekit.spec.ts E2E-06
  - phase: v1.3-03
    provides: createDeepAgentsResumeHandler, useDeepAgentsChat retry/enableReconnect options
provides:
  - e2e/reconnect.spec.ts with E2E-09 retry() reconnection test
  - apps/example/app/api/chat/stream/resume/[resumeId]/route.ts: GET handler via createDeepAgentsResumeHandler()
  - apps/example/app/reconnect-test/page.tsx: test fixture page with retry button and enableReconnect: true
  - package.json test:e2e script (pnpm test:e2e runs all 5 new spec files)
  - .github/workflows/e2e.yml e2e-mocked job: CI runs E2E-01 through E2E-10 without backend secrets
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "page.route mock for retry flow: intercept **/api/chat/stream, vary response by requestCount to simulate interruption then full resume"
    - "Dynamic [resumeId] route segment for Next.js App Router resume handler"
    - "CI e2e-mocked job pattern: start 3 dev servers in background, wait with curl loop, run mocked spec files"

key-files:
  created:
    - e2e/reconnect.spec.ts
    - apps/example/app/api/chat/stream/resume/[resumeId]/route.ts
    - apps/example/app/reconnect-test/page.tsx
  modified:
    - package.json
    - .github/workflows/e2e.yml

key-decisions:
  - "Resume route uses dynamic [resumeId] path segment (not query param) — matches createDeepAgentsResumeHandler() handler signature which reads params.resumeId from App Router params"
  - "E2E-09 simulates interruption via requestCount logic in page.route: first request delivers partial SSE (no finish frame), retry() calls regenerate() → second POST to same endpoint delivers complete response"
  - "chat.spec.ts excluded from CI mocked job — requires real LLM backend (OPENROUTER_API_KEY); existing e2e-django and e2e-fastapi jobs cover that"
  - "e2e-mocked CI job has no 'if' guard (no secrets required) — runs on all PRs including forks, unlike the Django/FastAPI jobs"
  - "curl loop wait pattern used instead of wait-on — avoids adding wait-on as a devDependency"

requirements-completed: [E2E-09]

# Metrics
duration: 2min
completed: 2026-05-04
---

# Phase v1.4-01 Plan 03: Stream Reconnection E2E + CI Wiring Summary

**Resume GET route + reconnect test fixture + E2E-09 retry() flow spec + test:e2e script + mocked CI job covering all 10 E2E scenarios without a backend**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-04T09:09:28Z
- **Completed:** 2026-05-04T09:11:25Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- E2E-09: `retry()` after simulated interruption triggers a second request; full content accumulates; no message duplication
- Resume route at `apps/example/app/api/chat/stream/resume/[resumeId]/route.ts` uses `createDeepAgentsResumeHandler()` — production-ready
- Test fixture page at `apps/example/app/reconnect-test/page.tsx` exposes `[data-testid="retry"]` button wired to `retry()`
- `test:e2e` script added to root `package.json` — `pnpm test:e2e` invokes playwright on all spec files
- `.github/workflows/e2e.yml` gains an `e2e-mocked` job: starts all 3 dev servers, runs the 5 new spec files with `ENABLE_STREAM_RECONNECT=true`, uploads report — no backend secrets needed, runs on all PRs

## Task Commits

Each task was committed atomically:

1. **Task 1: Resume route + reconnect E2E spec (E2E-09)** — `930ed8f`
2. **Task 2: test:e2e script + CI workflow** — `75b02cc`

## Files Created/Modified

- `e2e/reconnect.spec.ts` — E2E-09: retry() flow, requestCount assertion, no-duplication check
- `apps/example/app/api/chat/stream/resume/[resumeId]/route.ts` — GET resume handler via createDeepAgentsResumeHandler()
- `apps/example/app/reconnect-test/page.tsx` — test fixture with enableReconnect, resumeId, retry button
- `package.json` — added `"test:e2e": "playwright test"` script
- `.github/workflows/e2e.yml` — added `e2e-mocked` job for mocked E2E suite

## Decisions Made

- Dynamic `[resumeId]` path segment used (not query param) — handler reads `params.resumeId` from App Router params
- E2E-09 uses `requestCount` in `page.route` to vary response between first (interrupted) and retry (complete) calls
- `chat.spec.ts` excluded from the mocked CI job — it requires a real LLM backend
- `e2e-mocked` job has no `if` guard, so it runs on fork PRs too (no secrets required)
- `curl` loop chosen for server readiness over `wait-on` to avoid adding a new devDependency

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED
- e2e/reconnect.spec.ts: FOUND
- apps/example/app/api/chat/stream/resume/[resumeId]/route.ts: FOUND
- apps/example/app/reconnect-test/page.tsx: FOUND
- package.json test:e2e: FOUND
- .github/workflows/e2e.yml: FOUND
- Commit 930ed8f: FOUND
- Commit 75b02cc: FOUND

---
*Phase: v1.4-01-e2e-test-coverage*
*Completed: 2026-05-04*
