---
phase: v1.5-06
plan: 02
type: execute
wave: 2
depends_on:
  - v1.5-06-01
files_modified:
  - .github/workflows/e2e.yml
autonomous: true
requirements:
  - CI-01
formal_artifacts: none

must_haves:
  truths:
    - "Running `pnpm test:e2e` from the monorepo root starts a Next.js dev server, runs the full Playwright suite (including reconnection), and exits with code 0 when all tests pass"
    - "The GitHub Actions `e2e` job triggers on every PR, runs `pnpm test:e2e`, and reports pass/fail as a required status check — no manual intervention needed"
  artifacts:
    - path: ".github/workflows/e2e.yml"
      provides: "CI workflow that starts 4 dev servers, waits for all ports, and runs the full Playwright suite including open-swe"
      contains: "3001"
  key_links:
    - from: ".github/workflows/e2e.yml"
      to: "pnpm test:e2e"
      via: "Run mocked E2E tests step"
      pattern: "pnpm test:e2e"
    - from: ".github/workflows/e2e.yml"
      to: "open-swe dev server port 3001"
      via: "Start open-swe dev server step + health check loop"
      pattern: "3001"
    - from: ".github/workflows/e2e.yml"
      to: "ENABLE_STREAM_RECONNECT=true"
      via: "env block on Run mocked E2E tests step"
      pattern: "ENABLE_STREAM_RECONNECT"
---

<objective>
Extend .github/workflows/e2e.yml to start the open-swe dev server (port 3001), add port 3001 to the health check loop, and update the "Run mocked E2E tests" step to run `pnpm test:e2e` without explicit spec file arguments (so all projects and all spec files run via playwright.config.ts project routing).

Purpose: CI-01 requires the GitHub Actions e2e job to run the full Playwright suite including the new open-swe tests. The existing workflow already starts 3 servers and runs named spec files; this plan adds the 4th server and switches to project-based routing.
Output: .github/workflows/e2e.yml updated — 4 servers started, port 3001 in health check loop, `pnpm test:e2e` without hardcoded spec list
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v1.5-06-reconnection-e2e-ci-fold/v1.5-06-RESEARCH.md
@.github/workflows/e2e.yml
@.planning/phases/v1.5-06-reconnection-e2e-ci-fold/v1.5-06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update e2e.yml — add open-swe server, extend health check, update run command</name>
  <files>.github/workflows/e2e.yml</files>
  <action>
    Read .github/workflows/e2e.yml. Make three targeted changes to the `e2e-mocked` job only — do NOT touch the e2e-django or e2e-fastapi jobs:

    **Change 1 — Add open-swe server start step** (insert after "Start SvelteKit dev server" step, before "Wait for dev servers"):
    ```yaml
    - name: Start open-swe dev server (port 3001)
      run: pnpm --filter open-swe dev &
      env:
        PORT: 3001
    ```

    **Change 2 — Extend health check to include port 3001**:
    Replace:
    ```yaml
    for port in 3000 5173 5174; do
    ```
    With:
    ```yaml
    for port in 3000 3001 5173 5174; do
    ```

    **Change 3 — Update run command to use full project routing** (remove explicit spec file list):
    Replace:
    ```yaml
    - name: Run mocked E2E tests (E2E-01 through E2E-10)
      run: pnpm test:e2e e2e/nextjs.spec.ts e2e/nextjs-extra.spec.ts e2e/remix.spec.ts e2e/sveltekit.spec.ts e2e/reconnect.spec.ts
      env:
        ENABLE_STREAM_RECONNECT: 'true'
        CI: 'true'
    ```
    With:
    ```yaml
    - name: Run mocked E2E tests (E2E-01 through E2E-11, CI-01)
      run: pnpm test:e2e
      env:
        ENABLE_STREAM_RECONNECT: 'true'
        CI: 'true'
    ```

    The step name change ("E2E-01 through E2E-11, CI-01") documents the new scope without breaking anything. `pnpm test:e2e` maps to `playwright test` which discovers all spec files via playwright.config.ts project routing.

    Pre-flight check: Verify apps/open-swe/package.json has `"dev": "next dev --port 3001"` script and the package name is "open-swe" — the `pnpm --filter open-swe dev` command depends on both. (It does: confirmed from research context.)
  </action>
  <verify>
    1. Verify YAML syntax: `cat .github/workflows/e2e.yml | python3 -c "import sys,yaml; yaml.safe_load(sys.stdin); print('YAML valid')"` — should print "YAML valid"
    2. Check port 3001 in health check: `grep "3001" .github/workflows/e2e.yml` — should show the health check loop line
    3. Check open-swe server step exists: `grep "open-swe dev" .github/workflows/e2e.yml` — should show the new step
    4. Check run command has no spec file list: `grep "pnpm test:e2e e2e/" .github/workflows/e2e.yml` — should return EMPTY (no hardcoded spec files)
    5. Check ENABLE_STREAM_RECONNECT still set: `grep "ENABLE_STREAM_RECONNECT" .github/workflows/e2e.yml` — should show the env var
  </verify>
  <done>.github/workflows/e2e.yml starts 4 dev servers, waits for all ports (3000, 3001, 5173, 5174), runs `pnpm test:e2e` with ENABLE_STREAM_RECONNECT=true; YAML is valid; e2e-django and e2e-fastapi jobs unchanged</done>
</task>

</tasks>

<verification>
1. `cat .github/workflows/e2e.yml | python3 -c "import sys,yaml; yaml.safe_load(sys.stdin); print('YAML valid')"` — prints "YAML valid"
2. `grep "open-swe dev" .github/workflows/e2e.yml` — returns the new server start step
3. `grep "3001" .github/workflows/e2e.yml` — returns both the PORT env var and the health check loop line
4. `grep "pnpm test:e2e e2e/" .github/workflows/e2e.yml` — returns EMPTY (no hardcoded spec file list)
5. `grep "ENABLE_STREAM_RECONNECT" .github/workflows/e2e.yml` — returns the env var (confirms reconnection test will run in CI)
</verification>

<success_criteria>
- .github/workflows/e2e.yml e2e-mocked job starts the open-swe server on port 3001
- Health check loop includes port 3001 alongside 3000, 5173, 5174
- "Run mocked E2E tests" step uses `pnpm test:e2e` (no explicit spec list) with ENABLE_STREAM_RECONNECT=true
- YAML file is syntactically valid
- e2e-django and e2e-fastapi jobs are unchanged
- The CI job (`e2e-mocked`) already triggers on `pull_request` events (confirmed from existing e2e.yml); no branch protection changes needed in code (branch protection is a GitHub UI setting)
</success_criteria>

<output>
After completion, create `.planning/phases/v1.5-06-reconnection-e2e-ci-fold/v1.5-06-02-SUMMARY.md`
</output>
