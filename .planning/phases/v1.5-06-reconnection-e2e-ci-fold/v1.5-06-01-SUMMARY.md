---
phase: v1.5-06
plan: 01
status: complete
completed_at: 2026-05-05
requirements:
  - E2E-11
  - CI-01
---

# Plan 01 Summary — open-swe Playwright smoke tests + config update

## What Was Built

- **playwright.config.ts** updated: chromium project now has explicit `testMatch` restricted to `nextjs.spec.ts`, `nextjs-extra.spec.ts`, `reconnect.spec.ts`, and `chat.spec.ts`. New `open-swe` project added with `baseURL: http://localhost:3001` and `testMatch: /open-swe\.spec\.ts/`.
- **e2e/open-swe.spec.ts** created: 4 tests across 3 describes covering DASH-03 (live stream SSE text rendering), DASH-04 (tool card pending/completed states and expand), DASH-05 (concurrent run isolation via separate browser contexts). All tests use `page.route()` mocks — no real LangGraph Platform required.

## Key Files

- `playwright.config.ts` — 4 projects: chromium (restricted), remix, sveltekit, open-swe (port 3001)
- `e2e/open-swe.spec.ts` — 4 tests for DASH-03/04/05

## Verification

- `pnpm exec playwright test --list` confirmed 19 tests across 7 files:
  - chromium: nextjs, nextjs-extra, reconnect, chat (13 tests)
  - remix: 1 test
  - sveltekit: 1 test
  - open-swe: 4 tests (DASH-03, DASH-04 ×2, DASH-05)
- reconnect.spec.ts confirmed correct (retry(), requestCount tracking, deduplication — no changes needed)

## Deviations

- ToolCard data-testid adjusted from plan's `[data-testid^="tool-card"]` to actual `[data-testid="tool-card"]` and `[data-testid="expand-toggle"]` per component source at `apps/open-swe/components/ToolCard.tsx`
- "file contents here" assertion replaced by `[data-testid="tool-status"]` contains "completed" (SSE mock delivers output but page renders status not raw output text in DASH-04)

## Self-Check: PASSED
