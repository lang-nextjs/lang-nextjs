---
phase: v1.5-02-approval-gating
verified: 2026-05-04T19:02:51Z
status: passed
score: 4/4 must-haves verified
---

# Phase v1.5-02: Approval Gating Verification Report

**Phase Goal:** Developers can opt into tool-level approval gating — the handler emits a pause frame and holds the run until the client sends back an explicit approve or reject signal.

**Verified:** 2026-05-04T19:02:51Z

**Status:** PASSED

## Goal Achievement

All four observable truths are verified through implementation and testing.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | When approval gating is enabled on the handler, the client receives a `data-approval-required` frame containing the pending tool call details before the tool executes | ✓ VERIFIED | `data-approval-required` frame generated in `packages/server/src/adapters/approvalGating.ts:262-276`; includes `id`, `seq`, `actionName`, `description`, `arguments`, `status`, `createdAt`, `expiresAt` matching `ApprovalSchema` from `packages/react/src/schemas.ts` |
| 2 | The run remains paused — no further tool or text frames are emitted — until the handler receives an explicit approval or rejection POST from the client | ✓ VERIFIED | [QUORUM-2] Global pause implemented: `pendingApprovalsByToolCallId.size > 0` buffers ALL frames (including text-delta) into `globalBufferedFrames`; drain only on approval (line 204-205) or rejection (line 208-220); 17 tests validate buffering behavior |
| 3 | When the client sends a rejection, the run is cancelled and a terminal frame is emitted; no further tool execution occurs | ✓ VERIFIED | [QUORUM-3] Rejection emits `data-error` frame with `code: "approval_rejected"` (line 218-219, 385); `pendingApprovalsByToolCallId` entry cleared (line 209, 377) preventing further execution; POST handler at `packages/server/src/approval-routes.ts:118` calls `resolveApproval(approvalId, "reject")` |
| 4 | When approval gating is disabled (default), the adapter behaves identically to the non-gating path — no `data-approval-required` frames are emitted | ✓ VERIFIED | `approvalGating?: ApprovalGatingConfig` optional field in `DeepAgentsHandlerOptions` (packages/server/src/handler.ts); ternary injection pattern: `options.approvalGating ? createApprovalGatingTransform(options.approvalGating) : null`; 8 handler integration tests confirm default pass-through |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/server/src/approval-registry.ts` | In-memory registry with lazy TTL eviction, stale-approved GC | ✓ VERIFIED | 116 lines; implements 5 exported functions: `registerApproval`, `getApproval`, `resolveApproval`, `cleanupApproval`, `cleanupExpiredApprovals`; [QUORUM-4] stale-approved GC confirmed at line 105-112 (removes ALL expired entries regardless of status) |
| `packages/server/src/adapters/approvalGating.ts` | SseTransform factory with global pause, data-error on rejection, cleanup-after-drain | ✓ VERIFIED | 400 lines; `createApprovalGatingTransform` factory; implements [QUORUM-2] global pause (line 173-287), [QUORUM-3] rejection→data-error (line 208-220, 376-387), [QUORUM-4] cleanup-after-drain via `shiftFromReadyQueue` (line 65-73) with `remainingDrainCount` tracking (line 55) |
| `packages/server/src/approval-routes.ts` | GET/POST route handlers for approval decision endpoint | ✓ VERIFIED | 123 lines; `createApprovalRoutes()` factory returns `{ GET, POST }` handlers; GET returns approval status JSON (line 44-62); POST accepts `{ decision: "approve" \| "reject" }`, validates, calls `resolveApproval` only (line 118, [QUORUM-1] safety: no `cleanupApproval` call) |
| `packages/server/src/handler.ts` (modified) | Extended with `approvalGating` option, transform injection, cleanup in finally block | ✓ VERIFIED | Lines 1-3: imports `createApprovalGatingTransform`, `ApprovalGatingConfig`, `cleanupExpiredApprovals`; Lines 34-36: `approvalGating?: ApprovalGatingConfig` added to interface; Lines 357-361: ternary injection after adapter transforms; Lines 389-391: `cleanupExpiredApprovals()` in finally block when option set |
| `packages/server/src/adapters/index.ts` (modified) | Export `createApprovalGatingTransform`, `ApprovalGatingConfig` | ✓ VERIFIED | Lines 8-9: both exports present; imports from `"./approvalGating"` (not re-inlined) |
| `packages/server/src/index.ts` (modified) | Public API exports: `createApprovalRoutes`, `createApprovalGatingTransform`, `ApprovalGatingConfig` | ✓ VERIFIED | Lines 31-33: all three exports present; imports from correct modules (`"./approval-routes"`, `"./adapters/approvalGating"`) |
| `packages/server/README.md` (modified) | Approval Gating section with handler setup, endpoint, frame format, pause behavior | ✓ VERIFIED | ~70 lines; documents: handler setup with `approvalGating.getApprovalConfig`, endpoint wiring pattern, client POST examples, `data-approval-required` and `data-error` frame formats, global pause behavior, default disabled behavior |
| `packages/server/src/approval-registry.test.ts` | 10+ RED tests for registry (register, get, TTL, resolve, cleanup, stale-approved GC) | ✓ VERIFIED | 158 lines; 11 tests: register, get, TTL lazy marking, resolve approve/reject, cleanup, concurrent access, [QUORUM-4] stale-approved GC test present; all 11/11 PASS |
| `packages/server/src/adapters/approvalGating.test.ts` | 15+ RED tests for transform (pass-through, approval-required, buffering, global pause, rejection→data-error, cleanup-after-drain) | ✓ VERIFIED | 448 lines; 17 tests: pass-through (disabled/no require), approval-required emission, tool-keyed buffering, [QUORUM-2] text-delta buffering during pause, [QUORUM-2] text-delta drain after approval, [QUORUM-3] rejection→data-error, [QUORUM-3] rejection clears entry, [QUORUM-4] cleanup-after-drain, timeout handling; all 17/17 PASS |
| `packages/server/src/handler.test.ts` (additions) | 8+ handler integration tests for approvalGating option | ✓ VERIFIED | Lines 1220-1391: new `approvalGating option` describe block with 8 tests: default disabled, approval-required emitted, tool-input-start buffered before approval, per-tool control, payload validation, getApprovalConfig callback destructuring, non-tool frames pass-through; all 8/8 PASS |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `handler.ts` | `adapters/approvalGating.ts` | `createApprovalGatingTransform` injected into `allTransforms` | ✓ WIRED | Import at line 1; injection at line 357-361; transform inserted after adapter transforms, before user transforms |
| `adapters/approvalGating.ts` | `approval-registry.ts` | `registerApproval`, `getApproval`, `cleanupApproval` called in transform closure | ✓ WIRED | Import at line 26-30; `registerApproval` called at line 250-260, 325-335; `getApproval` called at line 116, 190, 237, 359; `cleanupApproval` called at line 69, 94, 205, 373 |
| `approval-routes.ts` | `approval-registry.ts` | `getApproval`, `resolveApproval` called in POST handler | ✓ WIRED | Import at line 26; `getApproval` called at line 47, 96; `resolveApproval` called at line 118 |
| `server/index.ts` | `approval-routes.ts` | `createApprovalRoutes` exported | ✓ WIRED | Line 31: export statement; import from `"./approval-routes"` |
| `server/index.ts` | `adapters/approvalGating.ts` | `createApprovalGatingTransform`, `ApprovalGatingConfig` exported | ✓ WIRED | Lines 32-33: export statements; import from `"./adapters/approvalGating"` |
| `react/schemas.ts` | Frame payload | `ApprovalSchema` defines `data-approval-required` frame shape | ✓ WIRED | `ApprovalSchema` defined at lines 82-93 in packages/react/src/schemas.ts; frame generated at line 265-275 in approvalGating.ts matches schema structure |

### Requirements Coverage

| Requirement | Phase | Description | Status | Evidence |
|-------------|-------|-------------|--------|----------|
| ADAPT-05 | v1.5-02 | Developer can gate tool execution — adapter emits a `data-approval-required` frame; the run pauses until an explicit approval or rejection is sent back by the handler | ✓ SATISFIED | All three plans executed: v1.5-02-01 (37 RED tests), v1.5-02-02 (GREEN implementation), v1.5-02-03 (route factory + exports). Server package exports `createApprovalGatingTransform`, `createApprovalRoutes`, `ApprovalGatingConfig`. Handler option available. All 234 server tests pass |

### Anti-Patterns Found

None. Files contain no TODO, FIXME, XXX, HACK, or placeholder comments. All implementations are substantive.

### Test Coverage

**Full Server Test Suite:** 234 tests, 0 failures

- `approval-registry.test.ts`: 11/11 PASS
- `adapters/approvalGating.test.ts`: 17/17 PASS
- `handler.test.ts` (approvalGating section): 8/8 PASS
- All other adapter tests: 198/198 PASS
- **Build:** ESM, CJS, DTS all succeed with zero errors

### Quorum-Identified Behaviors

All four quorum behaviors from phase planning are implemented and tested:

1. **[QUORUM-1] No cleanup in approval-routes.ts POST handler:** Verified — `cleanupApproval()` NOT called; resolveApproval only (line 118); JSDoc explains why (lines 10-15, 73, 117)

2. **[QUORUM-2] Global pause for text-delta and other non-tool frames:** Verified — `pendingApprovalsByToolCallId.size > 0` gates ALL frames into `globalBufferedFrames` (line 173-287); text-delta test at line 215-233 in approvalGating.test.ts; drain test at line 235-256

3. **[QUORUM-3] Rejection emits data-error frame (not null):** Verified — frame returned with `code: "approval_rejected"` (line 218, 385); entry cleared from pendingMap (line 209, 377); rejection test at line 277-311 in approvalGating.test.ts

4. **[QUORUM-4] Cleanup-after-drain and stale-approved GC:** Verified — `remainingDrainCount` tracking (line 55) with `shiftFromReadyQueue` cleanup (line 65-73); `cleanupExpiredApprovals` removes ALL expired entries regardless of status (line 105-112 in approval-registry.ts); stale-approved test at line 107-121 in approval-registry.test.ts; cleanup-after-drain test at line 357-386 in approvalGating.test.ts

---

## Verification Summary

**Phase v1.5-02 achieves its goal entirely.** All four observable truths are verified through:

1. **Implementation completeness:** 3 new files (approval-registry.ts, approvalGating.ts, approval-routes.ts) with 639 lines of substantive code
2. **Full test coverage:** 37 RED tests (plan 01), all GREEN (plan 02-03); 234 total server tests pass
3. **Integration:** Handler option properly wired; three ADAPT-05 exports available from server package
4. **Documentation:** README section with end-to-end usage pattern
5. **Quorum safety:** All four quorum-identified behaviors implemented and tested

**Developers can now opt into approval gating.** The control loop is complete: data-approval-required frames emitted → stream paused → approval routes accept decisions → transform drains buffered frames on approval or emits data-error on rejection.

---

_Verified: 2026-05-04T19:02:51Z_
_Verifier: Claude (nf-verifier)_
