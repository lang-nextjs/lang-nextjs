---
phase: v1.5-02
plan: "01"
subsystem: testing
tags: [approval-gating, tdd, red-tests, sse, hitl, adapt-05]

requires:
  - phase: v1.5-01
    provides: openSweAdapter with tool-input-start/tool-output-available frame types and readyQueue drain pattern

provides:
  - RED test suite for approval-registry (10 tests)
  - RED test suite for approvalGating transform (17 tests)
  - RED integration tests for handler approvalGating option (8 tests)
  - Contract for ADAPT-05 approval gating API surface including 4 quorum behaviors

affects: [v1.5-02-02, v1.5-02-03]

tech-stack:
  added: []
  patterns:
    - "TDD RED: test files import from non-existent modules so tests fail with 'Cannot find module'"
    - "approval-registry singleton tests use cleanupApproval() in afterEach for isolation (not vi.resetModules)"
    - "approvalGating transform tests use real registry collaborators (Option B), same pattern as openSwe.test.ts"

key-files:
  created:
    - packages/server/src/approval-registry.test.ts
    - packages/server/src/adapters/approvalGating.test.ts
  modified:
    - packages/server/src/handler.test.ts

key-decisions:
  - "approval-registry tests use cleanup-by-known-ID pattern (cleanupApproval in afterEach) rather than vi.resetModules() — avoids module cache churn while keeping tests isolated"
  - "approvalGating transform tests use real registry (Option B) not mocked — matches openSwe.test.ts pattern of testing transform in isolation with real collaborators"
  - "handler.test.ts approvalGating option describe block placed at file end with its own drainResponse helper — avoids polluting existing helper setup"
  - "ApprovalGatingConfig type imported from './approvalGating' (to-be-created) — not inlined in test file to match final API surface"

patterns-established:
  - "Quorum-identified behaviors tagged with [QUORUM-N] comments in test descriptions for traceability"
  - "makeFrame() helper in approvalGating.test.ts mirrors toSseFrame() from openSwe.test.ts for consistency"

requirements-completed: [ADAPT-05]

duration: 5min
completed: 2026-05-04
---

# Phase v1.5-02 Plan 01: Approval Gating RED Tests Summary

**37-test RED suite (approval-registry + approvalGating transform + handler integration) defining ADAPT-05 approval gating API including QUORUM-identified behaviors: rejection→data-error, global text-delta pause, cleanup-after-drain, stale-approved GC**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-04T18:43:00Z
- **Completed:** 2026-05-04T18:48:00Z
- **Tasks:** 1 (single RED commit)
- **Files modified:** 3

## Accomplishments
- Created `approval-registry.test.ts` with 10 RED tests covering register/get/TTL/resolve/cleanup/GC/concurrent access
- Created `adapters/approvalGating.test.ts` with 17 RED tests covering pass-through, approval-required emission, buffering, global text-delta pause (QUORUM-2), rejection→data-error (QUORUM-3), cleanup-after-drain (QUORUM-4)
- Added 8 RED integration tests to `handler.test.ts` in a new `approvalGating option` describe block verifying handler-layer wiring

## Task Commits

1. **Task 1: RED test suite for approval-registry, approvalGating transform, handler option** - `2ff88c4` (test)

**Plan metadata:** (to be added in final docs commit)

## Files Created/Modified
- `packages/server/src/approval-registry.test.ts` - 10 RED tests for the in-memory approval registry (158 lines)
- `packages/server/src/adapters/approvalGating.test.ts` - 17 RED tests for the approval gating SseTransform factory (299 lines)
- `packages/server/src/handler.test.ts` - Added 8 RED integration tests in `approvalGating option` describe block

## Decisions Made
- approval-registry tests isolate via `cleanupApproval(id)` in body/afterEach rather than `vi.resetModules()` — avoids test framework overhead while keeping state isolated
- approvalGating transform tests use real registry collaborators (Option B) — matches existing openSwe.test.ts pattern
- Handler tests added a local `drainResponse()` helper inside the new describe block to avoid scope pollution

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All RED tests committed — v1.5-02-02 (GREEN) can now implement:
  - `packages/server/src/approval-registry.ts` (registry functions + PendingApproval type)
  - `packages/server/src/adapters/approvalGating.ts` (createApprovalGatingTransform + ApprovalGatingConfig)
  - `packages/server/src/handler.ts` additions (approvalGating option in DeepAgentsHandlerOptions)
- No blockers; implementation contract fully defined by test suite

---
*Phase: v1.5-02*
*Completed: 2026-05-04*
