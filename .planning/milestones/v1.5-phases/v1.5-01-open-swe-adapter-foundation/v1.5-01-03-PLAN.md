---
phase: v1.5-01
plan: 03
type: execute
wave: 3
depends_on: [v1.5-01-02]
files_modified:
  - packages/server/src/adapters/openSweHeartbeat.ts
  - packages/server/src/adapters/openSweHeartbeat.test.ts
  - packages/server/src/adapters/index.ts
  - packages/server/src/index.ts
autonomous: true
requirements: [ADAPT-03]
formal_artifacts: none

must_haves:
  truths:
    - "A developer passing openSweAdapter to createDeepAgentsHandler sees tool-input-start frames in the AI SDK v6 stream when the LangGraph Platform emits on_tool_start events"
    - "A developer sees tool-output-available frames in the stream when the upstream emits on_tool_end events, with the correct tool_call_id linking each pair"
    - "A run that produces no upstream events for 30 seconds still holds the SSE connection open — a heartbeat comment frame is received by the client and no timeout or connection reset occurs"
    - "When two tool calls arrive in reversed order (simulated in a unit test), the buffered output is emitted in the correct tool_call_id order — no out-of-order frames reach the client"
  artifacts:
    - path: "packages/server/src/adapters/openSweHeartbeat.ts"
      provides: "createHeartbeatStream() — ReadableStream transform that injects SSE comment frames when no upstream events arrive within the interval"
      min_lines: 50
      exports: ["createHeartbeatStream"]
    - path: "packages/server/src/adapters/openSweHeartbeat.test.ts"
      provides: "Unit tests for heartbeat stream: emits comment after idle, resets on activity, does not emit when events flow"
      min_lines: 40
  key_links:
    - from: "packages/server/src/adapters/openSweHeartbeat.ts"
      to: "packages/server/src/index.ts"
      via: "export { createHeartbeatStream } from './adapters/openSweHeartbeat'"
      pattern: "createHeartbeatStream"
---

<objective>
Implement ADAPT-03: SSE heartbeat keep-alive to prevent 30-second idle timeouts on long-running
open-swe runs.

The heartbeat MUST NOT be a module-level timer or inside the stateless SseTransform pipeline
(transforms are synchronous and cannot schedule timers). Instead it is a ReadableStream transform
factory (createHeartbeatStream) that wraps the upstream ReadableStream and injects SSE comment
frames (": keep-alive\n\n") when no upstream bytes have arrived within the configured interval.

Purpose: ADAPT-03 — long-running runs (>2 min) hold the SSE connection open via heartbeat.
Output: openSweHeartbeat.ts + openSweHeartbeat.test.ts + export wiring
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@packages/server/src/adapters/openSwe.ts
@packages/server/src/adapters/index.ts
@packages/server/src/index.ts
@packages/server/src/accumulator.ts
@.planning/phases/v1.5-01-open-swe-adapter-foundation/v1.5-01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement createHeartbeatStream with tests (TDD inline)</name>
  <files>
    packages/server/src/adapters/openSweHeartbeat.ts
    packages/server/src/adapters/openSweHeartbeat.test.ts
  </files>
  <action>
WRITE THE TEST FILE FIRST (RED), then the implementation (GREEN), within this single task.

--- TEST FILE: packages/server/src/adapters/openSweHeartbeat.test.ts ---

import { describe, it, expect, vi, afterEach } from "vitest";
import { createHeartbeatStream } from "./openSweHeartbeat";

describe("createHeartbeatStream", () => {
  afterEach(() => {
    vi.useRealTimers();
  });

  it("passes upstream bytes through unchanged when events arrive within interval", async () => {
    // Arrange: upstream that emits one chunk immediately
    const upstream = new ReadableStream<Uint8Array>({
      start(ctrl) {
        ctrl.enqueue(new TextEncoder().encode("data: hello\n\n"));
        ctrl.close();
      },
    });
    const heartbeat = createHeartbeatStream(upstream, { intervalMs: 30_000 });
    const chunks: string[] = [];
    const reader = heartbeat.getReader();
    const decoder = new TextDecoder();
    let done = false;
    while (!done) {
      const result = await reader.read();
      done = result.done;
      if (!done && result.value) chunks.push(decoder.decode(result.value));
    }
    expect(chunks.join("")).toBe("data: hello\n\n");
  });

  it("emits a heartbeat comment frame when upstream is idle beyond intervalMs", async () => {
    vi.useFakeTimers();
    // Arrange: upstream that never emits (simulates long-running quiet task)
    let resolveClose!: () => void;
    const upstream = new ReadableStream<Uint8Array>({
      start(ctrl) {
        // Close after test advances timers
        new Promise<void>(res => { resolveClose = res; }).then(() => ctrl.close());
      },
    });
    const heartbeat = createHeartbeatStream(upstream, { intervalMs: 30_000 });
    const reader = heartbeat.getReader();
    const decoder = new TextDecoder();

    // Read one chunk — before advancing timers, nothing comes
    const readPromise = reader.read();

    // Advance time past the interval
    await vi.advanceTimersByTimeAsync(30_001);

    const result = await readPromise;
    expect(result.done).toBe(false);
    const text = decoder.decode(result.value);
    // SSE comment frame: ": keep-alive\n\n"
    expect(text).toBe(": keep-alive\n\n");

    // Cleanup
    resolveClose();
    reader.releaseLock();
  });

  it("does not emit heartbeat when upstream continuously provides data", async () => {
    vi.useFakeTimers();
    const encoder = new TextEncoder();
    // Upstream that emits 3 chunks at t=0, t=10s, t=20s — all within 30s interval
    let ctrl!: ReadableStreamDefaultController<Uint8Array>;
    const upstream = new ReadableStream<Uint8Array>({
      start(c) { ctrl = c; },
    });
    const heartbeat = createHeartbeatStream(upstream, { intervalMs: 30_000 });
    const reader = heartbeat.getReader();
    const decoder = new TextDecoder();
    const received: string[] = [];

    // Emit chunks within the interval — heartbeat must NOT fire
    ctrl.enqueue(encoder.encode("data: chunk1\n\n"));
    await vi.advanceTimersByTimeAsync(10_000);
    ctrl.enqueue(encoder.encode("data: chunk2\n\n"));
    await vi.advanceTimersByTimeAsync(10_000);
    ctrl.enqueue(encoder.encode("data: chunk3\n\n"));
    await vi.advanceTimersByTimeAsync(5_000);
    ctrl.close();

    let done = false;
    while (!done) {
      const r = await reader.read();
      done = r.done;
      if (!done && r.value) received.push(decoder.decode(r.value));
    }
    // Should have only the 3 data chunks, no heartbeat
    expect(received).toHaveLength(3);
    expect(received.every(c => !c.startsWith(":"))).toBe(true);
  });
});

--- IMPLEMENTATION FILE: packages/server/src/adapters/openSweHeartbeat.ts ---

/**
 * createHeartbeatStream — SSE keep-alive for long-running open-swe runs.
 *
 * Wraps an upstream ReadableStream<Uint8Array> and injects an SSE comment frame
 * (": keep-alive\n\n") when no upstream bytes have arrived within intervalMs.
 * Resets the heartbeat timer each time an upstream chunk is received.
 *
 * DESIGN CONSTRAINT (from v1.5 roadmap):
 *   Heartbeat MUST NOT be a module-level timer — it is per-request state.
 *   MUST NOT be implemented inside a synchronous SseTransform (transforms cannot
 *   schedule timers). Correct placement: ReadableStream transform wrapping the
 *   upstream response body before the SseFrameAccumulator processes it.
 *
 * Usage in a Next.js App Router route:
 *   const lgResponse = await client.streamRun(...);
 *   const withHeartbeat = createHeartbeatStream(lgResponse.body!, { intervalMs: 25_000 });
 *   // Pass withHeartbeat as the upstream body to createDeepAgentsHandler or transform directly.
 */

export interface HeartbeatOptions {
  /** How long (ms) of upstream silence triggers a heartbeat. Default: 25_000 (25s). */
  intervalMs?: number;
}

const HEARTBEAT_FRAME = new TextEncoder().encode(": keep-alive\n\n");

/**
 * Wraps an upstream ReadableStream<Uint8Array> with SSE heartbeat injection.
 * Returns a new ReadableStream<Uint8Array> that is a drop-in replacement for the upstream.
 */
export function createHeartbeatStream(
  upstream: ReadableStream<Uint8Array>,
  options?: HeartbeatOptions
): ReadableStream<Uint8Array> {
  const intervalMs = options?.intervalMs ?? 25_000;

  return new ReadableStream<Uint8Array>({
    async start(controller) {
      const reader = upstream.getReader();
      let heartbeatTimer: ReturnType<typeof setTimeout> | null = null;

      function scheduleHeartbeat() {
        if (heartbeatTimer !== null) clearTimeout(heartbeatTimer);
        heartbeatTimer = setTimeout(() => {
          // Emit SSE comment keep-alive frame
          try {
            controller.enqueue(HEARTBEAT_FRAME);
          } catch {
            // Controller may be closed — ignore
          }
          // Reschedule until upstream closes
          scheduleHeartbeat();
        }, intervalMs);
      }

      scheduleHeartbeat();

      try {
        while (true) {
          const { done, value } = await reader.read();
          if (done) break;
          // Reset heartbeat timer: upstream is active
          scheduleHeartbeat();
          controller.enqueue(value);
        }
      } finally {
        if (heartbeatTimer !== null) clearTimeout(heartbeatTimer);
        reader.releaseLock();
        controller.close();
      }
    },

    cancel() {
      // Upstream cancellation propagates automatically via reader.releaseLock() → close chain
    },
  });
}

After writing both files, run the tests:
  pnpm --filter @deepagents-nextjs/server test -- --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|Heartbeat|heartbeat)"
Expected: all three heartbeat tests pass GREEN.

GUARD: verify no module-level `let timer` or `setInterval` at the top of openSweHeartbeat.ts.
  grep -n 'setInterval\|let timer\|const timer' packages/server/src/adapters/openSweHeartbeat.ts
  Should return no matches (all timer state is inside the ReadableStream start() callback).
  </action>
  <verify>
    pnpm --filter @deepagents-nextjs/server test
    Expected: all tests pass (existing openSwe.test.ts + new openSweHeartbeat.test.ts)
    pnpm --filter @deepagents-nextjs/server typecheck
    Expected: exits 0
  </verify>
  <done>
    openSweHeartbeat.ts and openSweHeartbeat.test.ts exist.
    All 3 heartbeat tests pass. All existing adapter tests still pass.
    Typecheck exits 0.
    Commit: feat(v1.5-01-03): implement createHeartbeatStream for SSE keep-alive (ADAPT-03)
  </done>
</task>

<task type="auto">
  <name>Task 2: Export createHeartbeatStream from package public API</name>
  <files>
    packages/server/src/adapters/index.ts
    packages/server/src/index.ts
  </files>
  <action>
Wire createHeartbeatStream into the barrel exports following the same pattern as openSweAdapter
wiring in plan 02.

packages/server/src/adapters/index.ts — append:
  export { createHeartbeatStream } from './openSweHeartbeat'
  export type { HeartbeatOptions } from './openSweHeartbeat'

packages/server/src/index.ts — append:
  export { createHeartbeatStream } from "./adapters/openSweHeartbeat";
  export type { HeartbeatOptions } from "./adapters/openSweHeartbeat";

GUARD AGAINST RE-INLINING:
  grep 'createHeartbeatStream' packages/server/src/adapters/index.ts   → match
  grep 'createHeartbeatStream' packages/server/src/index.ts            → match

Final full-suite verification:
  pnpm --filter @deepagents-nextjs/server test
  pnpm --filter @deepagents-nextjs/server typecheck
  pnpm --filter @deepagents-nextjs/server build
All three must succeed.
  </action>
  <verify>
    pnpm --filter @deepagents-nextjs/server test
    pnpm --filter @deepagents-nextjs/server typecheck
    pnpm --filter @deepagents-nextjs/server build
    grep 'createHeartbeatStream' packages/server/src/index.ts
  </verify>
  <done>
    createHeartbeatStream is exported from @deepagents-nextjs/server public API.
    All tests pass, typecheck exits 0, build succeeds.
    Commit: feat(v1.5-01-03): export createHeartbeatStream from server package public API
  </done>
</task>

</tasks>

<verification>
1. packages/server/src/adapters/openSweHeartbeat.ts exports createHeartbeatStream
2. packages/server/src/adapters/openSweHeartbeat.test.ts — all 3 tests pass
3. pnpm --filter @deepagents-nextjs/server test — all tests pass (openSwe + heartbeat)
4. pnpm --filter @deepagents-nextjs/server typecheck — exits 0
5. pnpm --filter @deepagents-nextjs/server build — exits 0
6. createHeartbeatStream exported from packages/server/src/index.ts
7. No module-level timer state in openSweHeartbeat.ts (all state is per-stream inside start())
</verification>

<success_criteria>
ADAPT-03 is implemented: createHeartbeatStream wraps upstream ReadableStream and emits
": keep-alive\n\n" SSE comment frames when no upstream bytes arrive within intervalMs.
The heartbeat is stateless at module level (per-request state only). All tests pass.
The full phase v1.5-01 success criteria are now met:
  - ADAPT-01: tool-input-start frames emitted on on_tool_start
  - ADAPT-02: tool-output-available frames emitted on on_tool_end with correct toolCallId
  - ADAPT-03: heartbeat prevents 30-second idle timeout
  - ADAPT-04: reorder buffer emits parallel tool ends in start order
</success_criteria>

<output>
After completion, create .planning/phases/v1.5-01-open-swe-adapter-foundation/v1.5-01-03-SUMMARY.md
</output>
