---
phase: v1.5-01-open-swe-adapter-foundation
verified: 2026-05-04T22:47:00Z
status: passed
score: 4/4 must-haves verified
---

# Phase v1.5-01: openSweAdapter Foundation — Verification Report

**Phase Goal:** Developers can stream open-swe tool events (start and end) from a LangGraph Platform run through the adapter, with heartbeat keep-alive and correct reordering for parallel tool calls

**Verified:** 2026-05-04T22:47:00Z
**Status:** PASSED
**Requirements Met:** ADAPT-01, ADAPT-02, ADAPT-03, ADAPT-04

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A developer passing `openSweAdapter` to `createDeepAgentsHandler` sees `tool-input-start` frames in the AI SDK v6 stream when the LangGraph Platform emits `on_tool_start` events | ✓ VERIFIED | `packages/server/src/adapters/openSwe.ts` implements `on_tool_start` → `tool-input-start` mapping; 5 tests verify correct type, toolName, input, and toolCallId fields (ADAPT-01) |
| 2 | A developer sees `tool-output-available` frames in the stream when the upstream emits `on_tool_end` events, with the correct `tool_call_id` linking each pair | ✓ VERIFIED | `openSwe.ts` implements `on_tool_end` → `tool-output-available` mapping with LIFO queue pairing; 3 tests verify correct type, output fields, and toolCallId linking between paired events (ADAPT-02) |
| 3 | A run that produces no upstream events for 30 seconds still holds the SSE connection open — a heartbeat comment frame is received by the client and no timeout or connection reset occurs | ✓ VERIFIED | `packages/server/src/adapters/openSweHeartbeat.ts` implements `createHeartbeatStream()` ReadableStream transform that injects `: keep-alive\n\n` SSE comment frames on idle; 3 tests verify heartbeat emission on idle, no heartbeat on active stream, and correct frame passthrough (ADAPT-03) |
| 4 | When two tool calls arrive in reversed order (simulated in a unit test), the buffered output is emitted in the correct `tool_call_id` order — no out-of-order frames reach the client | ✓ VERIFIED | `openSwe.ts` implements reorder buffer with `startOrder` list and `endBuffer` map; 2 tests verify correct emission order for different tools and same-tool LIFO queue pairing when ends arrive out-of-order (ADAPT-04) |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/server/src/adapters/openSwe.ts` | openSweAdapter implementation with on_tool_start/end mapping and reorder buffer | ✓ EXISTS | 194 lines; exports `openSweAdapter` and `createOpenSweTransform`; uses LIFO queue pairing and reorder buffer logic |
| `packages/server/src/adapters/openSwe.test.ts` | Comprehensive test suite covering tool event mapping, toolCallId linking, reorder buffer, and FIFO queue pairing | ✓ EXISTS | 409 lines; 19 tests passing covering adapter contract, on_tool_start mapping, on_tool_end mapping, toolCallId linking, reorder buffer, FIFO queue pairing, and pass-throughs |
| `packages/server/src/adapters/openSweHeartbeat.ts` | createHeartbeatStream factory and HeartbeatOptions export | ✓ EXISTS | 78 lines; exports `createHeartbeatStream()` function and `HeartbeatOptions` interface; implements per-request timer state in ReadableStream start() callback |
| `packages/server/src/adapters/openSweHeartbeat.test.ts` | Tests for heartbeat stream behavior under idle, active, and boundary conditions | ✓ EXISTS | 75 lines; 3 tests passing covering passthrough on active stream, heartbeat emission on idle, and continuous-data scenarios |
| `packages/server/src/adapters/index.ts` | Re-exports openSweAdapter and createHeartbeatStream | ✓ EXPORTS | Lines 5-7 export both openSweAdapter/createOpenSweTransform and createHeartbeatStream/HeartbeatOptions |
| `packages/server/src/index.ts` | Public exports of openSweAdapter and createHeartbeatStream | ✓ EXPORTS | Lines 22-24 export both openSweAdapter/createOpenSweTransform and createHeartbeatStream/HeartbeatOptions |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| openSwe.ts | deepagents.ts | `import type { SseAdapter }` | ✓ WIRED | Line 28 imports SseAdapter type; openSweAdapter object conforms to SseAdapter shape |
| openSwe.ts | accumulator.ts | `import type { SseFrame, SseTransform }` | ✓ WIRED | Line 27 imports frame and transform types; createOpenSweTransform returns SseTransform |
| openSweAdapter | package exports | adapters/index.ts line 5 + src/index.ts line 22 | ✓ WIRED | Re-exported in barrel; public API includes openSweAdapter and createOpenSweTransform |
| createHeartbeatStream | package exports | adapters/index.ts line 6 + src/index.ts line 23 | ✓ WIRED | Re-exported with HeartbeatOptions type; public API includes both function and type |
| openSweHeartbeat.ts | ReadableStream | Web Streams API (native) | ✓ WIRED | Uses standard ReadableStream<Uint8Array> with start() callback for per-request timer state |

### Requirements Coverage

| Requirement | Phase | Description | Status | Evidence |
|-------------|-------|-------------|--------|----------|
| ADAPT-01 | v1.5-01 | Developer can stream `on_tool_start` events as AI SDK v6 `tool-input-start` frames | ✓ SATISFIED | openSwe.ts case "on_tool_start" (lines 99-127) maps to tool-input-start with correct fields; 5 openSwe.test.ts tests verify correct behavior |
| ADAPT-02 | v1.5-01 | Developer can stream `on_tool_end` events as AI SDK v6 `tool-output-available` frames | ✓ SATISFIED | openSwe.ts case "on_tool_end" (lines 129-172) maps to tool-output-available; toolCallId linking via LIFO queue shift(); 3 openSwe.test.ts tests verify paired toolCallIds |
| ADAPT-03 | v1.5-01 | Long-running runs (>2 min) do not timeout — adapter emits SSE heartbeat frame every 15–30 seconds when no events arrive from upstream | ✓ SATISFIED | openSweHeartbeat.ts createHeartbeatStream wraps upstream with per-request setTimeout; default 25_000ms interval; 3 openSweHeartbeat.test.ts tests verify idle heartbeat emission |
| ADAPT-04 | v1.5-01 | Parallel tool calls that arrive out-of-order are buffered and reordered correctly by `tool_call_id` before emission to the client | ✓ SATISFIED | openSwe.ts reorder buffer (lines 145-172) maintains startOrder and endBuffer; LIFO queue pairing ensures correct pairing; 2 openSwe.test.ts tests (reorder buffer + FIFO queue pairing) verify correct emission order |

### Anti-Patterns Found

| File | Line(s) | Pattern | Severity | Impact |
|------|---------|---------|----------|--------|
| None detected | — | — | — | All code implements intended behavior without placeholders, stubs, or debug logging |

### Implementation Quality

**Code completeness:**
- openSwe.ts: Fully implemented with all required event mappings, reorder buffer logic, and stateless adapter pattern
- openSweHeartbeat.ts: Fully implemented with per-request timer state in ReadableStream callback (no module-level timers)
- Both files: Zero placeholder comments, stub implementations, or incomplete handlers

**Test coverage:**
- openSwe.test.ts: 19 tests covering adapter contract, tool event mapping, toolCallId linking, reorder buffer, FIFO queue pairing, and pass-throughs
- openSweHeartbeat.test.ts: 3 tests covering idle heartbeat, active stream passthrough, and continuous-data scenarios
- All tests pass (198/198 server tests pass)

**Type safety:**
- Both files import types from accumulator.ts and deepagents.ts (no inline type definitions)
- Full TypeScript compilation succeeds (no errors in openSwe/openSweHeartbeat code)
- Generic types (SseFrame, SseTransform, SseAdapter) properly imported and used

**Export wiring:**
- openSweAdapter and createOpenSweTransform exported from adapters/index.ts and src/index.ts
- createHeartbeatStream and HeartbeatOptions exported from adapters/index.ts and src/index.ts
- Build succeeds (ESM + CJS + DTS artifacts generated)

### Formal Verification

No formal scope matched this phase. Formal verification skipped.

## Phase Completion Summary

All four requirements (ADAPT-01, ADAPT-02, ADAPT-03, ADAPT-04) are implemented, tested, and exported from the @deepagents-nextjs/server package public API.

**Key deliverables:**
1. openSweAdapter — LangGraph Platform tool event adapter with reorder buffer and LIFO queue pairing
2. createHeartbeatStream — SSE keep-alive factory for long-running runs
3. 19 + 3 = 22 passing unit tests covering all behaviors
4. Complete TypeScript types and public API exports
5. ESM + CJS + DTS builds generated and functional

**Goal status:** ACHIEVED — Developers can now:
- Import `openSweAdapter` from `@deepagents-nextjs/server` and pass to `createDeepAgentsHandler`
- Stream `tool-input-start` frames when LangGraph Platform emits `on_tool_start` events
- Stream `tool-output-available` frames when LangGraph Platform emits `on_tool_end` events
- Keep SSE connections alive on long-running runs via `createHeartbeatStream` heartbeat wrapper
- Correctly handle parallel tool calls that arrive out-of-order via reorder buffer logic

---

_Verified: 2026-05-04T22:47:00Z_
_Verifier: Claude (nf-verifier)_
