# Docker Compose Environment Variables
# Copy this to .env and modify as needed

# PostgreSQL Configuration
POSTGRES_PASSWORD=langgraph
POSTGRES_DB=langgraph_checkpoints
POSTGRES_USER=postgres

# pgAdmin Configuration (only used with --profile full)
PGADMIN_DEFAULT_EMAIL=admin@openswe.local
PGADMIN_DEFAULT_PASSWORD=admin

# Port Configuration (uncomment to override defaults)
# DYNAMODB_PORT=8000
# POSTGRES_PORT=5432
# REDIS_PORT=6379
# PGADMIN_PORT=5050

# Volume Configuration
# POSTGRES_DATA_PATH=./docker/postgres-data
# DYNAMODB_DATA_PATH=./docker/dynamodb
# REDIS_DATA_PATH=./docker/redis-data
