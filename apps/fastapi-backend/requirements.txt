fastapi>=0.115.0
uvicorn[standard]>=0.30.0
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai
langchain-anthropic
# PINNED, NOT FLOORED (#10). This was `deepagents>=0.0.1` — a floor of nothing
# on the library rung 3 is DEFINED by, which makes "the two planes are
# equivalent" an assertion against a moving target. The TS/Python parity matrix
# on #10 was measured against exactly 0.7.11: identical builtin tools, subagents
# and task delegation on both, interrupt_on withholding execution on both. A
# bump is a deliberate re-measurement, not a routine update.
#
# BOTH RUNTIMES MUST DECLARE THE SAME VERSION. django and fastapi are compared
# by check:run-axes-parity one layer up; two different deepagents underneath
# that comparison would make it a lie.
deepagents==0.7.11
python-dotenv
ddgs

# Langfuse tracing (#118). The two runtimes MUST declare this identically —
# _common.langfuse_callbacks() is byte-identical in both, so a version present
# in one and not the other would make one runtime silently untraced.
# v3, NOT v2. v2's SDK imports `langchain.callbacks`, removed in LangChain 1.x
# — this repo runs langchain 1.3.17, so v2 raises ModuleNotFoundError on import.
# v3 imports cleanly against langchain-core 1.6 and is the version exercised
# against the local fixture in scripts/langfuse-local/.
langfuse>=3,<4

# Wire-format conformance tests (#550). scripts/sse_frame_conformance.py
# validates this plane's streamed frames against docs/sse-frame-schema.json —
# the schema has been Ajv-validated since #59 and no PYTHON response had ever
# been checked against it.
#
# DECLARED, NOT INHERITED. Measured: with both backends' requirements installed,
# Name: jsonschema
Version: 4.16.0
Summary: An implementation of JSON Schema validation for Python
Home-page: https://github.com/python-jsonschema/jsonschema
Author: Julian Berman
Author-email: Julian+jsonschema@GrayVines.com
License: MIT
Location: /opt/anaconda3/lib/python3.9/site-packages
Requires: attrs, pyrsistent
Required-by: jupyterlab-server, nbformat, semgrep, spyder reports an EMPTY Required-by — nothing here pulls it in,
# so without this line the conformance tests do not fail, they cannot import.
# #544 is the same shape one layer over: prettier was a hoisted transitive and
# its verdict was going to be set by whatever an unrelated package depended on.
#
# BOTH RUNTIMES MUST DECLARE IT. The two planes are compared against one schema;
# a version present in one and not the other would silently check one plane.
jsonschema>=4.20
