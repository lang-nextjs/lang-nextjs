django==5.1.*
langgraph>=0.2.0
# PINNED, NOT FLOORED (#669). This was `langchain>=0.3.0` — a floor, with the
# approval DECISION VOCABULARY living inside it. HumanInTheLoopMiddleware
# expands `interrupt_on={tool: True}` into a concrete `allowed_decisions` list;
# that list is what the pass-through rungs put on the wire and what
# packages/react's ApprovalPauseCard renders as buttons. Measured by
# CONSTRUCTING the middleware, not by reading its source:
#
#     langchain 1.2.11   True -> ["approve", "edit", "reject"]
#     langchain 1.3.18   True -> ["approve", "edit", "reject", "respond"]
#
# Both satisfy `>=0.3.0`. So the set of controls a user is offered could change
# under a fully compliant install with nothing in this repo going red, and both
# of those installs exist on a developer machine here today.
#
# THIS RECORDS WHAT ALREADY RUNS RATHER THAN CHANGING IT: a fresh
# `pip install -r requirements.txt` on a clean CI runner resolves 1.3.18 today,
# measured in the job that runs this file. A bump is a deliberate
# re-measurement of the vocabulary, not a routine update.
#
# BOTH RUNTIMES MUST DECLARE THE SAME VERSION, for the deepagents reason below:
# check:run-axes-parity compares the two planes one layer up, and two different
# langchains underneath that comparison would make it a lie.
#
# scripts/assert-approval-vocabulary-agrees.mjs keeps this honest from the other
# side — it reads the INSTALLED expansion and fails when upstream can produce a
# decision `parse_approval_decisions` would refuse.
langchain==1.3.18
langchain-openai
langchain-anthropic
# PINNED, NOT FLOORED (#10). This was `deepagents>=0.0.1` — a floor of nothing
# on the library rung 3 is DEFINED by, which makes "the two planes are
# equivalent" an assertion against a moving target. The TS/Python parity matrix
# on #10 was measured against exactly 0.7.11: identical builtin tools, subagents
# and task delegation on both, interrupt_on withholding execution on both. A
# bump is a deliberate re-measurement, not a routine update.
#
# BOTH RUNTIMES MUST DECLARE THE SAME VERSION. django and fastapi are compared
# by check:run-axes-parity one layer up; two different deepagents underneath
# that comparison would make it a lie.
deepagents==0.7.11
uvicorn[standard]
psycopg[binary]
redis
django-cors-headers
python-dotenv
# DeepResearch topology's web_search tool. Same pin as the FastAPI backend —
# the two runtimes now declare the same deepagents topologies, so they need the
# same tool available or the manifest would claim a topology this runtime
# cannot dispatch.
ddgs

# Langfuse tracing (#118). The two runtimes MUST declare this identically —
# _common.langfuse_callbacks() is byte-identical in both, so a version present
# in one and not the other would make one runtime silently untraced.
# v3, NOT v2. v2's SDK imports `langchain.callbacks`, which LangChain 1.x
# removed — so on any 1.x, v2 raises ModuleNotFoundError on import. That is a
# property of the MAJOR line and needs no patch version: naming one dated this
# comment to a release nobody was running (#669). v3 imports cleanly against
# langchain-core 1.6 and is the version exercised against the local fixture in
# scripts/langfuse-local/.
langfuse>=3,<4

# Wire-format conformance tests (#550). scripts/sse_frame_conformance.py
# validates this plane's streamed frames against docs/sse-frame-schema.json.
# The schema has been Ajv-validated since #59 and no Python response had ever
# been checked against it.
#
# DECLARED, NOT INHERITED, for #544's reason: a dependency that arrives only as
# somebody else's transitive has its version set by an unrelated package, and
# disappears when that package moves. Nothing in either backend's requirements
# pulls jsonschema in, so without this line the conformance tests cannot import.
#
# BOTH RUNTIMES MUST DECLARE IT. The two planes are validated against one schema;
# a version present in one and not the other would silently check one plane.
jsonschema>=4.20
